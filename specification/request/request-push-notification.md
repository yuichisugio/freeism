# push通知の仕様書

## 1.(済み)push通知自体の実装

1. (済み)push通知自体の実装ログインが必要な画面に入ったら、push通知の許諾を表示して、service-workerをインストールする
2. (済み)通知を拒否して無効化していたが通知を受け取りたくなれば、dashboard/settings/page.tsx画面に、通知toggleを設定して、ONにすることで、OSの通知許諾画面にリダイレクトさせてONにできるような実装

## 2. 追加実装したいpush通知の仕様

1. (済み)アプリ内通知の作成form内に、push通知Toggleを配置して、ONにすると、push通知も送れるようにする
2. endpointが有効かの事前チェックを実装

   - 有効かチェックしてから通知を送る処理を開始する
   - 有効チェックしたら、pushSubscriptionテーブルの、last_healthcheck_timestampカラムに、チェックした日を記載
   - last_healthcheck_timestampカラムが1日以上前のユーザーにだけ事前チェックを入れる
   - さらに、通知送信前に事前チェックして、送信後にerrorになれば、その通知idを保存して、push通知のendpointなどの情報更新処理してretryする処理を入れる
   - https://claude.ai/chat/c6739b9d-9a58-440b-8fef-b29618c0e043

3. pushSubscriptionのidカラム(cuid)をlocalstorageに保存する処理を追加
4. push通知の文字数の上限が制限になるよう、zodスキーマを修正。

   - アプリ内通知・メール通知で共通にする

5. endpointは削除せず更新する
   - endpointは削除せず、再度更新処理を行い、retryを3回まで行う
   - エンドポイントのエラー(410？)が発生したら、エラー発生したらエンドポイントを再取得
6. エラーが発生したら、全てをリセットする処理
7. push通知のUI改善
   - アイコン・バッジをfaviconと同じデザインにしたい

## push通知を実装するファイルのPATH

1. public/service-worker.js
2. src/app/push-notification.ts
3. src/hooks/push-notification.ts
4. 新規作成してもOK

## push通知の使用場面

1. アプリ内通知の作成form
2. オークション関連の通知

## 考慮したいこと

1. push-notification.tsのsendNotification()にデータを渡せば、いつでもpush通知を送信できるようにしたいので、componentとは疎結合な設計にしたい

## 実装しない内容

1. 送れなかった内容をmessageQueテーブルに保存して後から送る仕組み

   - 通知が届かない場合に備えてメール通知とアプリ通知を実装する
   - さらに、通知送信前に事前チェックして、送信後にerrorになれば、その通知idを保存して、push通知のendpointなどの情報更新処理してretryする処理を入れる

## 詳しい仕様書

- デバイスフィンガープリント
  - ユーザー && デバイスの組み合わせでユニークなレコードをPushSubscriptionテーブルに作成しないと、どのユーザーのどの端末のエンドポイントの更新が必要か確認できない
  - push通知のエンドポイント

## 検討した仕様

- ユーザーと端末でユニークな値を作成する方法

  1. push通知のendPointをlocalstrageとDBに保存する方法

     - メリット

       1. デバイスのバージョンやOSバージョンが変わっても値が普遍

     - デメリット

       1. ↑のユーザーと端末でユニークにする方法だと、デバイスフィンガープリントばかりオススメ記事がでるので、この方法で良いか不安

  2. デバイスフィンガープリントを使用する方法

     - メリット

       1. ライブラリがあるし、

     - デメリット

       1. デバイスのバージョンやOSバージョンが変わっても値が変わったらダメ

  3. cookie

     - メリット

       1. 記事もたくさんあり、ユーザーと端末の一意性が高い

     - デメリット

       1. cookieの許諾などで、実装が大変そう
