# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets).
Use `pnpm changeset` for release-relevant application changes. This repository versions private applications only; it does not publish packages.
