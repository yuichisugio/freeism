# 「無料主義アプリ v2」の設計

- [「無料主義アプリ v2」の設計](#無料主義アプリ-v2の設計)
  - [v2：実装する理由](#v2実装する理由)
  - [v2：方針](#v2方針)
  - [v2：新しい仕様](#v2新しい仕様)
    - [v2：概要](#v2概要)
    - [v2：便利ライブラリを導入](#v2便利ライブラリを導入)
    - [v2：URLの修正](#v2urlの修正)
    - [v2：複数プロフィールの切り替え](#v2複数プロフィールの切り替え)
    - [v2：プロフィール画面](#v2プロフィール画面)
    - [v2：設定画面](#v2設定画面)
    - [v2：パブリックAPI](#v2パブリックapi)
    - [v2：出品・商材一覧・オークション・落札証明](#v2出品商材一覧オークション落札証明)
    - [v2：評価軸の作成・更新・プロフィール](#v2評価軸の作成更新プロフィール)
    - [v2：パッケージを作成・更新・プロフィール](#v2パッケージを作成更新プロフィール)
    - [v2：セキュリティ](#v2セキュリティ)
    - [v2：利用規約](#v2利用規約)
    - [v2：プライバシーポリシー](#v2プライバシーポリシー)
    - [v2：ホーム画面の修正](#v2ホーム画面の修正)
    - [v2：ログイン](#v2ログイン)
    - [v2：バンドルサイズを少なくする](#v2バンドルサイズを少なくする)
  - [v2：テーブル構造](#v2テーブル構造)
  - [v2：廃止する機能](#v2廃止する機能)
  - [v2：実装しない機能](#v2実装しない機能)
  - [v2：ライブラリ](#v2ライブラリ)
    - [v2：使用するライブラリ](#v2使用するライブラリ)
    - [v2：使用しないライブラリ](#v2使用しないライブラリ)

## v2：実装する理由

1. 無料主義ドキュメントv3に、大幅な仕様変更があったため

## v2：方針

1. **v2の目的は、無料主義の内容を伝えること**
   - 説明
     - v2の目的は、無料主義の内容を伝えることなので、開発者のデプロイやテスト環境の設定が面倒になる実装・機能は避ける
     - 実践寄りではなく、伝えることが目的
   - メモ
     - なので、画像の添付機能など設定が増えることは避ける
2. `v2`**は、「実用性」・「移植性」・「仕様理解の簡単さ」を優先する**
   - 説明
     - 本質的な機能のみ実装する
     - `ver1.0`はポートフォリオ的にいろいろな機能を実装したが、`ver2.0`は本質的な機能のみ残す
     - 他者がコードを読んで、「無料主義の仕様・したいこと」を理解してもらう必要がある
3. **設計の大原則**
   1. 疎結合
      - 評価軸、評価ロジック、データ取得元が、いつでも簡単に差し替えられるように設計
      - ファイルもタスクごとではなく、評価軸ごとに分けた
      - 「評価ロジック」、「評価軸」のフォルダごと削除しても、他は通常通り動作するよう設計
      - 冗長的な部分があるが、疎結合さを優先
   2. 可読性
      - 可読性のために、シェルスクリプトはファイルを分ける。
   3. 簡潔さ
      - より多くの人に理解してもらうために、堅牢性を求めず、簡単にすぐ理解できて短い必要最小限のコードにする
      - リッチな機能は提供せず、簡単に素早く理解できるようにシンプルな機能のみにする
      - アプリとして実用するのではなく、どんなサービスか体験してもらうだけ
      - リッチなUIは不要

4. **バンドルサイズを小さくする**
   - 説明
     - バンドルサイズを可能な限り小さくなる工夫をする
5. **定数管理**
   - 説明
     - それぞれのパラメータは、すぐに変更できるように、定数ファイルを作成して管理する
6. **できる限りサーバーの負荷をかけず、サーバーのアクセス回数も減らす設計**
   - 説明
     - 可能な限りキャッシュを行い、できる限りState管理で最終タイミングのみサーバーへリクエストして登録する

7. **商品発送・住所の管理などは不要**
   - 説明
     - モノの発送が必要な場合は、他サービスを使用して行う。
     - その発送の証拠のみをアプリ内のレビュー画面で記載するのみ

## v2：新しい仕様

### v2：概要

- 説明
  - ここに記載されていない仕様に関しては、無料主義アプリv1と同じ仕様

### v2：便利ライブラリを導入

1. `Docker`, `Docker Compose`
   - 導入する理由
     1. 本番環境と同じ環境をローカルにも作りたい
     2. どのOSでも同じ環境を立ち上げられる状態を作りたい
     3. グローバルインストールするようなライブラリを使用する場合に、それが入っていないので起動できない環境構築に時間がかかる状態を無くしたい
2. `mise`
   - 導入する理由
     1. タスクランナーとして、一コマンドですべてが完了する状態にしたい
        - 一つの目的を達成するために複数コマンドの実行が必要な状態をやめたい
        - 開発サーバーの立ち上げ（Docker
          Compose→移動→開発サーバーの立ち上げのコマンド実行など）をワンコマンドで実行したい
3. `direnv`
   - 導入する理由
     1. ディレクトリに移動するだけで環境構築が完了する状態を作りたい
4. Better Auth（`better-auth`）
   - 導入する理由
     1. 認証ライブラリで、よりシンプルで多機能なため
     2. 使用している`Auth.js v5`が Better Auth に合併したため
5. `Vite+`
   - 導入する理由
     1. `Vite+` エコシステムに寄せたいため
6. `TanStack Start`
   - 導入する理由
     1. `Vite+`を使う際のルーターなどで使用したいため

### v2：URLの修正

1. `www`を抜く
   - 説明
     - 正規のURLは **`https://freeism.app/`**（`www` なし）に統一する
   - 実装したい理由
     1. 必要な情報のみ表示したいため

2. アプリ内パスに `dashboard` を含める
   - 説明
     - ログイン後の画面・ドキュメント本文（アプリ配下）は **`https://freeism.app/dashboard/...`**
       の形式に統一する（例：設定、プロフィール、評価軸・パッケージの各URL、ドキュメントは
       `https://freeism.app/dashboard/docs`）

### v2：複数プロフィールの切り替え

- 概要
  - 複数プロフィールを簡単に切り替えられる機能
    - 毎回ログアウトする必要がない機能

- 使用場面
  - 「個人名義」と「Reactなどソフトウェア名義」ですぐ切り替えられるようにしたい

- 実装したい理由
  1.  Twitterのように複数プロフィールを簡単に切り替えられるようにしたい。
      - 複数名義で参加することがある想定のため

- 設計
  - フロントエンド
    1. フロントエンドでアカウント名ごとにセッションを保持して、切り替えボタンを押した際に、その押したアカウント名のCookieを投げる
    2. 切り替えUIは、Cookie内に複数のアカウントIDがあれば、その分だけアカウント名を表示する
  - バックエンド
    - 特別な実装は不要
  - メモ
    - セッションの管理なので、ブラウザごとにログインが必要なのは変わらないし、アカウントごとにブラウザからログインしないと切り替え先アカウントは表示されない

### v2：プロフィール画面

- 概要
  - 無料主義アプリのそれぞれのアカウントごとのプロフィール画面を設定できるようにしたい

- 必要な理由
  1.  このプロフィールに記載されている情報をもとに、本人かどうか確認したり、保有ポイントを確認するため

- 使用場面
  1.  ユーザーのプロフィール内のその人の貢献度などを見るために使用する

- 要件
  1.  **プロフィールの検索機能**
      - 目的・使用場面
        1.  貢献したユーザーのプロフィールを探す
      - 説明
        - ユーザーを検索できる機能・画面
        1.  全文検索できるよう設定
        2.  ユーザーの名前、ユーザーIDで検索できるようにする
        3.  ヘッダーの検索欄から検索できるようにする
            - 検索欄は、プロフィール・評価軸・パッケージの検索結果や他の検索結果と一緒に表示する
  2.  **閲覧権限**
      - 要件
        - ログインしなくても全員が見れるようにする
      - この設計の理由
        1.  誰でも見えるようにしないと、プロフィール画面の意味をなさない
  3.  **プロフィールURL**
      - 要件
        1.  プロフィールURLのPATHは、無料主義アプリ内ユーザーIDを入れる
            - ユーザー名を変更しても同じURLを使用できる様にする為
        2.  プロフィール画面のURLを作成する
            - `https://freeism.app/dashboard/profile/<ユーザーID>`にする
  4.  **表示項目**
      1. **ユーザーID**
         - UUID
      2. **ユーザー名**
         - 表示・設定の文字数上限を100文字にする
      3. **ユーザー説明**
         - 表示・設定の文字数上限を500文字にする
      4. **OAuth以外のアカウント連携の一覧**
         - 表示項目
           1. サービス名
           2. サービス内ユーザーID
           3. サービス内ユーザー名
         - 要件
           1. 更新処理は入れない
              - 各アカウントサービスごとのユーザー名変更後に更新する処理（リッチな処理）は入れない。
           2. 表示デザイン
              - `<GitHubアイコン:ユーザー名>,<x:ユーザー名>`
           3. 認証マーク
              - 認証された場合は、✅マークを入れる
              - 連携日を表示
              - 「B:@name（検証済み: 2025-09-01 12:34 JST）」を表示
      5. **「✏編集」ボタン**
         - 要件
           1. 設定画面に遷移して、編集状態にしたい
           2. 自分のプロフィール画面の時のみ表示する
      6. **公式パッケージの表示**
         - 説明
           - このプロフィールに紐づく公式パッケージを表示する（0件のときは当該ブロックを空表示または非表示。複数件のときは一覧ですべて表示）
         - 表示項目
           1. 公式パッケージ名の表示
           2. 公式パッケージのIDの表示
           3. 公式パッケージを構成する評価軸のそれぞれの割合の表示
              - 評価軸を一つ以上を組み合わせたパッケージを登録する
           4. 組み合わせている評価軸の名前・IDの表示
         - 要件
           1. 名前・IDを押したら、その評価軸のプロフィールへ飛べるようにする
         - 使用場面
           1. 参考にされた場合に、この公式評価軸の貢献度を元にポイントを付与することを示す。
      7. **OAuthのアカウント連携の一覧**
         - 表示項目
           1. サービス名
           2. サービス内ユーザーID
           3. サービス内ユーザー名
         - 要件
           1. 表示デザイン
              - `✅GitHubアイコン:プロフィールURL全体`
           2. 認証マーク
              - 認証された場合は、✅マークを入れる
      8. **各評価軸の保有ポイントの一覧**
         - 表示項目
           1. 評価軸のID
           2. 評価軸の名前
           3. 評価軸のポイント数
         - 要件
           1. 開示OKのみ表示
           2. ID・名前を、評価軸の画面に遷移リンク化する
      9. **貢献度のアップロード履歴**
         - 説明
           - アップロードされたCSVのレコードごとのデータを表示する
           - 貢献者の突き合わせは**連携先のプロフィールURL**による**照合**に統一する（`OAuth`以外の相互リンク、OIDC連携、評価軸の「貢献者アップロード」で定める、正規化したアップロードURLと登録連携URLの照合方針と同じ）
           - 各タスクごとの貢献度の算出結果は、各評価軸コミュニティ内で確認してもらう。
           - アップロードされた単位にすることで、一定期間ごとの合算になりそう。
           - 丁寧な評価軸であれば、タスクごとにレコードを分けてアップロードしてくれるかも
         - 表示する項目
           1. **連携・認証したサービスのプロフィールURL**（貢献アップロードに記載したURL。突き合わせは上記のURL照合方針に従う）
              - 例）`https://x.com/sugi_sugi_329` のように、当該ユーザーを表す**プロフィールURL**を表示する
           2. **タスクの実行年月**
              - **年月は必須・日時は任意**（評価軸の貢献者アップロードのCSV項目、および「貢献評価を代用する仕組み」の二重付与防止の前提と整合）
              - 日時まで含めて記録されている場合は、そのまま（または`YYYY-MM`に集約した表示等）表示する
           3. 貢献度の数値
              - マイナス評価もアップロード可能
              - 保有ポイントのマイナス表示も可能にする
              - 「-15」のようにハイフンを前に入れるとマイナスにする
           4. 評価軸ID
              - 無料主義アプリ内の評価軸IDを入れる
           5. 評価軸内の管理ID
              - 「アップロード内容の修正」や「ポイント付与履歴」の際に、「評価軸内の管理ID」を参考に確認できるようにしたい
           6. （任意）メモ
              - 200文字以下のバリデーションを入れる
      10. **ポイント交換する機能**
          - **処理の流れ**
            1. プロフィールURLの保有ポイント一覧の上にある「ポイント交換アップロード」ボタンを押す
            2. CSVをアップロード（ファイルの送信を完了する）
            3. **アップロード完了後**（下記）に、交換内容の確認ダイアログを表示する
            4. ユーザーが承認したら交換を実行する
          - **「アップロード完了後」の定義**
            - クライアントからサーバーへのファイル送信が完了し、サーバー側でCSVのパースとバリデーション（4のチェック、エラーの有無の判定）が終わった後、取り込み確定**直前**のタイミングを指す。いわゆる「ファイルを選んで即ダイアログ」ではなく、**検証のあと**に内容確認を出す。バリデーションエラー時は本ダイアログは出さず、5のエラー表示に従う。
          - **要件**
            1. GUIは用意せず、CSVアップロードのみ対応
            2. ドラッグ＆ドロップ機能は廃止
               - 「アップロード」ボタンを押してファイル選択画面から選択
            3. 一回のアップロード上限は100件
            4. アップロード時に、↓をチェックする
               - 交換先との交換比率が設定されているポイントか
               - 保有ポイント以下の額を交換するか
            5. エラー時の処理
               - エラーが出た初めの処理だけでなく、全部を見て、直すべき点をすべて指摘する
            6. 交換履歴の表示
               - 検索やフィルターなどのリッチなUIは不要。
               - プルダウンで履歴を開けるようにしたい
            7. 交換内容の確認ダイアログ（3〜4の内容）
               - 文言の例：「OOさん」に「OO評価軸」を「OOポイント分」だけ交換するけどOK？の確認
            8. アップロードCSVに記載する内容
               1. 交換元の評価軸ID
               2. 交換元の額（任意）
               3. 交換先の評価軸ID
               4. 交換先の額（任意）
                  - 「交換元の額」or「交換先の額」のどちらか必須
                  - 「交換先の額」が入力ある場合は、逆算して必要額を算出
      11. **貢献評価を代用する仕組み**
          - 説明
            - 「貢献評価を代用する仕組み」を利用して、他の評価軸のポイントを得る方法の実装
            - 類似度の係数は、「**ポイント交換する機能**」の交換比率を使用する
          - 必要性
            1. 必要性は、「貢献評価を代用する仕組み」ドキュメントに記載
          - 仕様
            1. キーの粒度は、「貢献評価を代用する仕組み」の類似度で付与したポイント or 正規な評価しか選べない設計にする
               - 後から何度でも、どの評価軸のポイントでも採用可能で、更新・変更できるようにする
               - 粒度が粗すぎるが、一旦は実装の簡略化のために年月日などの分け方はしない
            2. 「貢献評価を代用する仕組み」の類似度は、どの評価軸を選択するか指定できるようにしたい
            3. 「付与し直す」ボタンを用意
               - 途中で類似度によって付与された場合に追加ポイント付与があった場合は、「付与し直す」ボタンで更新できるようにする
            4. 計算式
               - A評価軸の付与ポイントを「貢献評価を代用する仕組み」で、B評価軸ポイントを取得した場合は、↓計算式で算出する
               - A評価軸の月ごとのポイント合計額×類似度＝その月のB評価軸ポイント額
            5. 二重付与を防止
               - 二重付与を防ぐために、貢献度アップロードの「タスクの実行年月」は**年月は必須・日時は任意**とし、タスク評価は更新できつつ二重の評価が発生しない仕組みにする
          - 処理の流れ
            1. プロフィールURLの保有ポイント一覧の上にある「評価代用アップロード」ボタンを押す
            2. CSVをアップロード（ファイルの送信を完了する）
            3. **アップロード完了後**（下記）に、内容の確認ダイアログを表示する
            4. ユーザーが承認したら実行する
          - **「アップロード完了後」の定義**
            - **ポイント交換する機能**の「「アップロード完了後」の定義」に従う（**ポイント交換・ポイント譲渡**と**同じ**タイミング。CSVの内容は評価代用のパース・バリデーションに読み替える）。バリデーションエラー時は本ダイアログは出さず、エラー表示に従う。
      12. **ポイント譲渡する機能**
          - **処理の流れ**
            1. プロフィールURLの保有ポイント一覧の上にある「ポイント譲渡アップロード」ボタンを押す
            2. CSVをアップロード（ファイルの送信を完了する）
            3. **アップロード完了後**（下記）に、譲渡内容の確認ダイアログを表示する
            4. ユーザーが承認したら譲渡を実行する
          - **「アップロード完了後」の定義**
            - ポイント交換と同じ。**ポイント交換する機能**の「「アップロード完了後」の定義」に従う（交換↔譲渡の文言差のみ）。バリデーションエラー時は本ダイアログは出さず、エラー表示に従う。
          - **要件**
            1. GUIは用意せず、CSVアップロードのみ対応
            2. ドラッグ＆ドロップ機能は廃止
               - 「アップロード」ボタンを押してファイル選択画面から選択
            3. 一回のアップロード上限は100件
            4. アップロード時に、↓をチェックする
               - 譲渡可能なポイントか
               - 保有ポイント以下の額を譲渡するか
               - **マイナス残高（マイナス帳簿）のとき**
                 - 貢献度のマイナス付与などで残高が0未満になったあとも、**さらに0未満へ減算する処理（よりマイナスに積み上がること）は許可する**。
                 - ただし
                   **マイナス帳簿（保有ポイントが0未満）の状態では、他者へのポイント譲渡は行えない**（譲渡は非対応とするバリデーションを入れる）。
            5. エラー時の処理
               - エラーが出た初めの処理だけでなく、全部を見て、直すべき点をすべて指摘する
            6. 譲渡履歴の表示
               - 検索やフィルターなどのリッチなUIは不要。
               - プルダウンで履歴を開けるようにしたい
            7. 譲渡内容の確認ダイアログ（3〜4の内容）
               - 文言の例：「OOさん」に「OO評価軸」を「OOポイント分」だけ譲渡するけどOK？の確認
            8. アップロードCSVに記載する内容
               1. 譲渡するポイントの評価軸ID
               2. 譲渡するポイントの額
               3. 譲渡先のユーザーID

### v2：設定画面

- 概要
  - プロフィールの設定画面に関する仕様

1. **プロフィール項目のプレビューボタン**
   - 要件
     1. ボタンを押したら、その人のプロフィールURLに遷移する
2. **公式パッケージの登録・編集**
   - 説明
     - このプロフィールに紐づく公式パッケージを設定する
     - ここに設定した公式の評価軸が、自分が他の評価軸に貢献した際に、自分へ貢献した人たちへポイントを分配する時に使用する評価軸になる
   - 使用場面
     - 参考にされた場合に、この公式評価軸の貢献度を元にポイントを付与する
   - 要件
     1. 公式パッケージは**1個以上・複数**を設定できる（UIも配列として扱い、追加・削除・並べ替え等で複数登録可能にする）
     2. 選択できるパッケージは、無料主義アプリ内に存在するパッケージのみにする
     3. ここに設定したとしても自動で分配するわけではない
        - 自動分配機能をONにしないと分配しない
   - 例
     - Reactの無料主義アプリのアカウントとしてログインしている場合に、Reactの公式パッケージをここで設定する
3. **非表示設定**
   - 要件
     1. 以下の項目は、デフォルトは「非表示」
   - 設定項目
     1. ポイント譲渡履歴
     2. ポイント交換履歴
     3. 貢献度のアップロード履歴
     4. 各種の保有ポイントの非表示設定
   - 要件
     1. それぞれの項目ごとに、それぞれの保有ポイントの種類ごとに、非表示にするか選べるようにしたい
   - 必要な理由
     - 相手の思想がわかっている状態なら、相手の思想が嫌だから提供しない、協力しないと言う人がいる
4. **プロフィール自体の非表示設定**
   - 要件
     1. プロフィール自体の非表示設定にすると、検索結果に表示されない
     2. プロフィールURLを開いても「存在しないプロフィールです」文言を表示する
     3. デフォルトは「表示」
5. **評価軸ポイントの自動分配の設定**
   - 説明
     - 商材に付与されたポイントを、その商材に紐付けられた公式パッケージを構成する評価軸の数値を元に、設定した割合や固定値を残したうえで自動分配する機能
     - 無料主義ドキュメントv3の「公式パッケージの仕組み」の内容
   - 要件
     1. 公式パッケージの登録をしていないと設定できないバリデーションを入れる
     2. 付与ポイントを商材に残す額は、「割合」or「固定値」のどちらにするかRadioボタンで選べるようにする
     3. 「割合」の場合は、0.001%〜100%まで選べるようにする
     4. 「固定値」の場合は、0以上から選べるようにする
     5. 自動分配の機能を`ON`にするかトグルで選択できるようにしたい
        - ONになった場合は、Toggleだけではわかりづらいので、「自動分配を設定中」と表示したい
     6. 複雑な条件にも対応できるDB設計にしたい
        - 「割合」と「固定値」を組み合わせて、「100ポイント以上の場合は5%だけ分配せずに残す」など、あらゆる設定に対応させたい
   - 使用場面
     1. 個人に貢献した人に分配したいときの設定
     2. 「OSS業界全体の発展の評価軸（A）」に、React開発グループ（B）が貢献した場合に、「React発展の評価軸（C）」ポイントの累計獲得割合をもとに、Reactに貢献したメンバーに、（A）ポイントを分配する
6. **OAuth以外のアカウント認証方法**
   - 目的
     1. ポイント付与の為に、指定サービスの指定ユーザーが、自分のアカウントであることを示したい為。
   - 使用する方式
     1. URLの双方向リンク（Bi-directional link）
        - 説明
          - Mastodonアカウント認証方法の改良版を採用
            - Mastodonは、Mastodonプロフィール記載のURL先に、`rel="me"`属性付きのMastodon URLがあるか確認で認証する
            - Markdownは、`rel="me"`属性を持つリンクが見つからない場合に最初に見つかったリンクをチェックし、その`href`値がMastodonプロフィールのURLにリダイレクトされるようにします（これにより、リンク短縮サービスを使用してrel-me属性を使用していないウェブページに対しても限定的な対応が可能になります）。
        - 採用した理由
          1. 連携方法が、あらゆるサービスで対応でき、知識が無くても連携が簡単
   - 使用場面
     1. Zenn記事の執筆者である証明
        - Zennは、OIDCログインを用意していない。
        - なのでZenn執筆者か検証する方法は、相互リンクしか無さそう
   - 処理の流れ
     1. 「無料主義アプリのプロフィールURL」を、「認証したいサイトのユーザーIDがあるURL」内にコピペして設定する
     2. 「認証したいサイトのユーザーIDがあるURL」を、「無料主義アプリのプロフィール」内の「連携URL」欄にコピペして追加＆保存する
     3. 上記の追加＆保存をトリガーにして、「認証したいサイトURL」をスクレイピング（HTMLを解析）する
        - 先に連携したいサービスに「無料主義アプリのプロフィールURL」を記載するのがポイント
          - そうしないと、無料主義アプリ内にURLを保存したときのトリガーで走ったときにURLが見つからないため
     4. スクレイピング結果に「無料主義アプリのプロフィールURL」の記載がある場合は、認証マークを追加する
        - 要件
          1. 認証URLを、末尾にパラメータやスラッシュがあれば削除
        - 注意
          1. `/`区切りで取得して、区切った最後の文字列をユーザーIDとして抽出したりしない
             - しない理由
               1. **URLにユーザー名・ユーザーIDが含まれない独自ドメインのサイト等があるため、URLさえ一致したら、そのURL文字列名義で貢献者としてアップロードされたポイントを付与する**
                  - **貢献者アップロードの際も、ユーザー名ではなく、そのユーザーを示す認証可能なURL文字列でアップロードする必要がある**
        - 例
          - 独自ドメインのサイトを`https://blog.example.com/writing/my-post`のように連携した場合、**パスを`/`区切りにして末尾のセグメントをユーザーIDとして抽出する処理は行わない**。貢献アップロードに記載するURLと、プロフィールの連携URLを**それぞれ正規化した文字列同士で一致するか**で照合する。
     5. 「貢献としてアップロードされた CSV 内のユーザー URL」を**正規化**し、同一ユーザーがプロフィールに登録した**複数の連携 URL**（各々**正規化**）の**いずれか**と一致するか確認する
        - 要件
          1. まず**正規化**する（スキーム/ホストの大小文字、末尾スラッシュ、ポート省略等）。そのうえで、**登録済み連携 URL を列挙して**照合する（**単一の完全一致のみ**ではなく、**複数登録分をすべて候補に含める**）
     6. 一致した場合は、そのポイントを、その無料主義アプリのアカウントに紐付けて、ポイント付与する
        - 要件
          1. 貢献度アップロードした内容を保存するDBテーブルのレコードに、「無料主義アプリ内ユーザーID」を保存するカラムに、紐づけたユーザーIDを入れる。
             - 認証したサービスのアカウント名を変更したときの対応に備える仕組み
             - 連携した外部アカウントの名前を変更した場合に、変更前のアカウント名で貢献度を付与していたポイントを保有したままにしたいため。
             - 「ユーザーID」と「貢献度アップロード時の貢献度の数字」を紐づけることで、外部アカウント名を変更しても、ユーザーIDにポイントが紐づくので、ポイントが維持される
   - 便利ライブラリ
     1. SSRF対策（IP判定・Agent）
        - [https://github.com/whitequark/ipaddr.js](https://github.com/whitequark/ipaddr.js)
     2. HTMLパース & セレクタ
        - [https://github.com/inikulin/parse5](https://github.com/inikulin/parse5)
     3. 他はChatGPTに調査してもらう
   - 要件
     1. 認証済みのサービスURLは、他アカウントでは連携できないようにする
        - 連携できないようにする理由
          1. 連携できた場合に報酬を付与するので、複数のアカウントで同じURLが連携できてしまうと二重のポイント付与になるため
        - 設計
          1. 認証するURLを保存するテーブルを作成し、認証URLごとに`used`カラムを置く。`used`は**「付与済みフラグ」かつ「連携成功時の占有」**を兼ねる（同一URLの二重連携防止と、付与状態の記録）。`false`のときのみ当該URLでの新規連携を成功させる
     2. 連携アカウントごとに非表示の設定ができるようにする
     3. `rel="me"`属性チェックの条件
        1. `rel="me"`属性が一つでもある場合は、`rel="me"`属性のURLのみチェックする。
        2. `rel="me"`属性が一つも無い場合は、`rel="me"`属性なしURLもチェックする。
        - 上記実装の理由
          - Mastodonは`rel="me"`属性があるURLのみ確認する設計。
          - 無料主義アプリでは、`rel="me"`属性なしサイトに対応するために、`rel="me"`属性なしURLの連携も認める
          - でも、万が一の「成りすまし」や「意図しない連携」を防ぐために、`rel="me"`属性があるサイトであれば、`rel="me"`属性ありURLのみを見る
        - `rel="me"`属性チェックしない理由
          - `Zenn`,`Instagram`などは、`rel="me"`の属性に対応していないサービスが多数あるため
   - 懸念点
     1. `rel="me"`属性チェックが無い場合は、別人Aが自分BのURLを記事に載せて掲載した場合に、その記事URLをBの無料主義プロフィールに載せるとBの成果になってしまう
        - コード例
          - `<p>最近、<a href="https://yourapp.com/@famous_user">@famous_user</a>さんの記事を読んで感銘を受けました。</p>`
            - ↑は、悪意を持ってBが記事URLをプロフィールURLに入れないと発生しない問題
            - **悪意が無い場合は発生しない**
              - 悪意があって上記の方法で連携した場合でも、記事URLをアカウントURLとして貢献度のアップロードが行われないと、貢献度の付与が行われない
              - **つまり、貢献度の分析結果をアップロードする人も操作ミスしないとポイントの付与が発生しない**
        - 結論
          - 一旦は心配しないで良いと思う。
          - 問題が多発したら、そのサービス限定で`rel="me"`属性チェックを入れる。
   - 採用しない別の解決方法
     1. 連携専用のURLを使用する
        - 説明
          - 連携専用のURLを生成して、それをプロフィールURLに入れる。
          - そうすることで、記事にプロフィールURLを入れたとしても連携用URLとは別なので連携が発生しない
          - 連携用URLのパスを、`/profile/`から`/check/`にして、検証専用のURLを連携先に記載してもらい、`/check/`にアクセスすると`/profile/`にリダイレクトされる形式にする
        - その方法デメリット
          1. URLを間違えてprofileに登録してしまう場合があること
             - でも、URL認証する際は絶対に無料主義アプリの相互リンク検証が完了したかチェックするはずで、もしいつになっても検証されない場合はURLが間違っているのではないかとチェックするはず。
             - なので、変える方法で対応するのが良いと思う
             - URLを間違わないように、検証用のURLを目立つ場所にボタン1つでコピペできるボタンを設置してそのURLを別サービスに設定することで検証できるように説明。欄もしっかりプロフィールのところに記載する。それで目立つように記載すれば大丈夫だと思う。
   - セキュリティ系
     1. **SSRF（Server-Side Request Forgery）対策**
        - 説明
          - ローカルIPアドレス（127.0.0.1, 192.168.x.x等）へのアクセスをブロック
        - 必要な理由
          1. これにより、内部ネットワークへの攻撃を防ぐ
     2. **タイムアウト**
        - 説明
          - 10秒でタイムアウト
        - 必要な理由
          1. レスポンスの遅いサーバーによるリソース占有を防ぐ
     3. **リダイレクト制限**
        - 説明
          - 最大5回まで
        - 必要な理由
          1. 無限リダイレクトループを防ぐ
     4. HTTPSのみ受け付け
        - 必要な理由
          - 中間者改ざんを回避するため
          - [https://docs.joinmastodon.org/user/profile/](https://docs.joinmastodon.org/user/profile/)
     5. IDN正規化後にホスト名が変わらないことを確認
        - 必要な理由
          1. ホモグリフ対策
             - 混合文字列内で一部のASCII文字と紛らわしい見た目のUnicode文字を使用する「同形異義語攻撃」を防ぐため
             - 例えば、U+0061（英語小文字の"a"）とU+0430（キリル文字の"а"）はほぼ同一に見えますが、実際には全く異なる文字です。
        - 要件
          1. IDN（国際化ドメイン）はPunycode 正規化後にホスト名一致を見る[https://docs.joinmastodon.org/user/profile/](https://docs.joinmastodon.org/user/profile/)
        - 説明
          - IDN（国際化ドメイン名）の正規化とは、日本語（ひらがな、カタカナ、漢字）や発音区別符号付きラテン文字などのUnicode文字を含むドメイン名を、DNS（ドメインネームシステム）が扱えるASCII文字（英数字・ハイフン）のみの形式（Punycode）へ安全かつ一貫性を持って変換する一連の処理プロセスです
     6. iframe 内のリンクは無視
        - 必要な理由
          1. “そのページ自身の編集権限”を担保できないため
        - 参考
          - [https://docs.joinmastodon.org/user/profile/](https://docs.joinmastodon.org/user/profile/)
        - 説明
          - このようなリンクはiframe内に配置されていてはなりません。
          - iframeを使用すると、実質的にそのリンクは現在のウェブページとは別の外部ページに存在することになり、現在のページに埋め込まれている状態となります
     7. 短縮URLは最終リダイレクト先で判断
        - 参考
          - [https://docs.joinmastodon.org/user/profile/](https://docs.joinmastodon.org/user/profile/)
        - 必要な理由
          1. 検証したいユーザーIDが含まれていない可能性があるため
     8. HTTPレスポンスHTMLの上限1MB
        - 必要な理由
          1. サイズが大きいとレスポンスに時間がかかるため
          2. DoS攻撃やメモリ枯渇を防ぐため
        - 説明
          - Mastodonは1MBを目安にしている
     9. Link発見
        - 要件
          - Link発見は、HTMLの `<a>`, `<link>`, `<head>`, `<p>` に加え、HTTP `Link:` ヘッダもサポートする
        - 参考
          - [https://indieweb.org/discovery-algorithms](https://indieweb.org/discovery-algorithms)
          - Mastodonは`<link>`タグ,`<a>`タグ,タグを見ているっぽいが、URLの正規表現で探すようにしたい
     10. URLは最大30個まで追加できるようにする
7. **OpenID Connect（OIDC）アカウント連携機能**
   - 目的
     1. 貢献アップロードに記載された**プロフィールURL**と、当該ユーザーがアカウント連携で登録した**各連携先のプロフィールURL**を、**それぞれ正規化した文字列同士で比較し、一致した場合**にポイント付与するため（**トークン応答のユーザーID／アカウント名の生文字列同士を直接比較するのではない**）
   - 処理の流れ
     1. 無料主義アプリのユーザーのプロフィールURLを開く
     2. OIDC認証できるサービスの一覧から、連携したいサービスを選択
     3. 選択したサービスでログイン
     4. OIDC /
        OAuth2 の認可フローで得たトークン応答（プロフィールのクレーム等）を用いて、その連携したサービスのプロフィールURLを作成し、DBに保存する。
        - 上記の説明
          - プロバイダによってはユーザー識別子のみ返るため、貢献アップロードと突き合わせやすいよう**プロフィール URL 形式**で保存する
        - **レスポンスの「ユーザーID or アカウント名」ではなく、URLにして保存する理由**
          1. OAuth以外の連携方法（相互リンク）のアカウントが貢献した場合は、URLを貢献アップロードの貢献者として記載する
          - なので、**OAuthの場合のみアカウント名だと、アップロード時に混乱が生まれる可能性があるため**
            - OAuth以外の連携方法で、貢献アップロード時に、「アカウント名」ではなく「URL」を貢献者の欄に記載する理由
            1. 独自ドメインのブログでは、アカウント名自体が無い
            2. はてなブログなどのURLでは、URLからアカウント名を柔軟に抜き出すことが難しい
            3. 「アカウント名」or「メンション名」or「アカウントID」など表記ゆれがあるため
        - 例
          1. GitHub：[https://github.com/yuichisugio](https://github.com/yuichisugio)
          2. ORCID：[https://orcid.org/0000-0002-6435-8736](https://orcid.org/0000-0002-6435-8736)
        - 注意
          - 編集画面で、以下のURLもあるので注意
            - [https://orcid.org/my-orcid?orcid=0000-0002-6435-8736](https://orcid.org/my-orcid?orcid=0000-0002-6435-8736)
     5. 「その作成した連携サービスURL」が「貢献アップロード時のプロフィールURL」に**相当する**場合は、ポイント付与する。照合は**双方の URL を正規化**したうえで行い、プロフィールに**複数の連携 URL**がある場合は、**正規化したアップロード側 URL が、そのいずれかと一致すれば**付与する（相互リンク方式の節と同じ方針）
   - 要件
     1. Better Auth へ移行
        - ドキュメント
          - Auth.js から Better Auth へ移行する方法の公式ドキュメント（URLに `next-auth`
            が含まれるが、**フレームワークは TanStack Start + Vite+** に読み替えて参照する）
          - [https://www.better-auth.com/docs/guides/next-auth-migration-guide](https://www.better-auth.com/docs/guides/next-auth-migration-guide)
        - Better Authの設定内容
          1. 連携は、ログインの`signIn.social()`系の呼び出しではなく、`linkAccount()` を使用する
             - [https://www.better-auth.com/docs/concepts/oauth#link-account](https://www.better-auth.com/docs/concepts/oauth#link-account)
          2. ログイン関数で認証するとSNSアカウントごとに個別のアカウントが作成される。
          3. 一つのアカウントに複数認証するには、リンク関数を使用する必要がある。
             - リンク関数が必要な理由の解説（Auth0ドキュメント）
               - [https://auth0.com/docs/ja-jp/manage-users/user-accounts/user-account-linking](https://auth0.com/docs/ja-jp/manage-users/user-accounts/user-account-linking)
          4. Adaptersは、Prisma ORMを指定
          5. Databaseは、PostgreSQL
          6. フレームワークは、**TanStack Start + Vite+** を前提に、Better
             Auth のドキュメントのうち該当する手順（フレームワーク別の案内があればそれに従う）を適用する
          7. アプリ名表示などの詳細オプション
             - [https://www.better-auth.com/docs/reference/options](https://www.better-auth.com/docs/reference/options)
     2. Googleログインのみ用意する
        - 無料主義アプリの連携は豊富だが、ログインはGoogleのみ対応する
          - Googleのみの理由
            1. Googleのみ管理が楽＆必要な情報が返ってくることが確認できている
            2. ORCIDは、メアド登録が任意で必要な情報が返ってこない場合がある
     3. `GitHub`、`ORCID`連携を用意する
        - 実装しないOIDC認証
          1. X(Twitter)
             - 有料の為
          2. Facebook
             - 実装しても、Facebook名義の貢献は少なく利用場面が無さそう
          3. Google
             - ログイン時のOIDC認証で使用しているので、リンク部分まで実装不要
        - 実装するOIDC認証
          1. ORCID
             - `ORCID`のOIDC認証は、Better Auth の`GenericOAuth`という仕組みで実装可能
          2. GitHub
             - `GitHub`のOIDC/OAuth連携は、Better Auth の`GenericOAuth`等で実装可能（ログイン用Google
               OIDCとは別に、`linkAccount`でプロフィールURL一致用に紐づける）
     4. 別々のアカウントでログインしたとしても、一つのアカウントに全ての連携を繋げられるようにしたい
        - アカウント連携の場合は、`login`ではなく`link`で実装する
     5. 連携解除ボタンを追加
        - それでユーザー名の変更に対応したい
     6. 連携アカウントごとに非表示の設定ができるようにする
     7. 一種類のSNSで、複数のアカウントの連携可能
        - 一つのSNSで複数アカウントを運用する場合がある為
8. **項目を削除する**
   - 削除する項目
     1. 自分の人生の目標
     2. 通知の設定
9. **「アプリ退会」ボタンの設置**
   - 要件
     1. その人に関するすべてのデータを削除してアプリをログアウトさせる
     2. 「自分一人のだけ管理者権限」の場合は、「権限譲渡」or「評価軸のアーカイブ」をしてから退会が必要にする

### v2：パブリックAPI

- 概要
  - 他のサービスから、無料主義アプリの情報にアクセスする手段を作りたい

- API全体の要件
  1.  REST APIで実装
      - 実装が複雑になるためGraphQL APIは採用しない。
  2.  RateLimitの実装
      - [https://kinsta.com/jp/blog/api-rate-limit/](https://kinsta.com/jp/blog/api-rate-limit/)
  3.  無料主義アプリのパブリックAPIの秘密鍵を発行して、その鍵と必要な情報をヘッダーに入れてAPIリクエストする
  4.  Server Functions 等には寄せず、Route handler（HTTP エンドポイント）＋ REST API として実装する

- **v2における範囲**
  - **読み取り専用**のエンドポイント（本節の**指定ユーザーの保有ポイント取得**、**落札証明向けの取得**、**ユーザー情報取得**など）はv2で実装する。
  - 一方、**外部システムから入札・出品・購入を自動化**する、あるいは**落札の通知とポイント減少を一括で外部連携**する、といった**書き込み・取引操作用のパブリックAPI**はv2の対象外とし、別途「v2：実装しない機能」の**出品・落札・購入のパブリックAPI化**の項に整理する。

- APIの種類
  1.  **指定ユーザーの保有ポイントを取得**
      - 目的
        1.  GitHubのIssuesなどに表示するバッジで、保有ポイントが高い人から優先対応するなどで、保有ポイントを証明するために必要
      - 要件
        1.  「無料主義アプリのユーザーID」と「評価軸のID」（Nano ID）を指定して、そのユーザーの保有ポイントを取得
        2.  Json形式で返す
            - Shields.io を使ってバッジを表示できるJSON
  2.  **指定ユーザーが落札したか示す情報を取得**
      - 目的
        1.  GitHubのIssuesなどに表示するバッジで、落札したことを証明するために必要
      - 要件
        1.  「無料主義アプリのユーザーID」と「落札ID」（オークション ID）を指定して、当該ユーザーがその落札の落札者であることの**落札証明情報**を取得する（例：落札商材の概要、落札日時、落札ID、表示用の出品者・購入者情報など。外部に見せるのに足る最小限のフィールドに絞る）
        2.  Json形式で返す
            - Shields.io を使ってバッジを表示できるJSON
        3.  **GitHub 上での明示向け**の場合の補足
            - 事前に
              **GitHub の OIDC 連携**を行い、レスポンスに**GitHub アカウント名**等を含められること（Issue コメントのコメント者と、API 上の身元照合に使う）
            - 落札者であることを Issue 上で示す用に、**落札商材名**等に**リポジトリ名・Issue
              ID**の記載を求める使い方に対応しうる形にする
            - 例：API の戻りを shields.io 形式のバッジとして Issue に貼り、**API レスポンスの GitHub アカウント名**と**コメント者の GitHub アカウント**、**オークション名**等で出品者（開発者）が照合する流れ
  3.  **ユーザー情報を取得**
      - 目的
        1.  「無料主義アプリの発展の評価軸」に必要なデータ
      - 要件
        1.  以下の情報のみ情報を返す
            - 「ユーザー名」、「ユーザーID」、「アプリ登録日」、「連携SNS」、「連携SNSアカウント名」、「ブラックリスト行為カウント」
        2.  ページネーション機能あり
            - `{"page":2}`
        3.  ユーザー指定あり
            - `{"userId": ["ewfrgthsngbdfvewgtw", "efgrhtjytiuytsegzdfbv"]}`
        4.  ソート順も指定可能
            - デフォルトではサイト登録順に返す
              1.  ソートで順番を固定しないと、ページネーションした場合の取得データが変わるため
            - `{"sortColumn":"createdAt", "sortDirection": "desc"}`
        5.  Json形式で返す

### v2：出品・商材一覧・オークション・落札証明

- 概要
  - 無料主義アプリで、商材を出品・商材一覧・オークション・落札証明する際の仕様について定める

- 仕様
  1.  **オークションに出品されている商材一覧画面**
      - 要件
        1.  カード形式の中に表示する項目に、以下の項目を追加表示する
            1. パッケージ名
            2. パッケージID
            3. 構成する評価軸名
            4. 構成する各ポイントの比率
        2.  全文検索は、生SQLではなく、Prisma ORMのデフォのクエリを使用する
      - 実装する理由
        1.  どのポイントで入札できるかすぐわかるようにしたい
        2.  保守性を高める
  2.  **オークションの出品する際の仕様**
      - 要件
        1.  **CSVアップロードのみ対応**
            - GUIフォームは設けない
            - 必要な理由
              1.  登録する労力を削減するため
                  - 登録が面倒なので、一括登録できるようにするため
        2.  **出品作成処理のリファクタリング**
            - リファクタリングの要件
              1.  Tanstack Query v5を使用するよう修正
              2.  出品作成ロジックは、CSVの各行を処理する共通関数に集約する
              3.  Modalは、`prisma.$transaction()`の`Promise.all()`内で、↑の共通の関数を呼び出す別関数を使用する
        3.  **「CSVアップロード」の一回のアップロード上限は100件**
            - メモ
              - 100件で負荷がかかる場合は、50件等に制限する
        4.  **以下の入力項目を追加**
            1. 入札できるポイントを指定するために、パッケージを指定する
               - 公式パッケージの指定を必須にする
               - 評価軸の組み合わせや割合をここでは指定せず、先にパッケージを作成or既存パッケージで指定してもらう
            2. 販売個数
               - 一つの出品枠で複数商品を出品できる
            3. 即決価格の設定
               - 要件
                 1. 指定したパッケージを構成する評価軸の設定として「即決価格」が可能な場合のみ出品時に選択できる
            4. 購入方式
               - 選択できる購入方式
                 1. 消費
               - 無料主義アプリv2で実装しない購入方式
                 1. ポイント還元
                 2. 消費なし
                 3. ポイント譲渡
                    - ポイント譲渡は、入札額を供給者に与える方式
               - 「消費」に対応する理由
                 1. 一番ニーズがありそうなため
  3.  **オークションの仕様**
      - 参考
        1.  Multi-unit auction
      - 説明
        - 無料主義アプリv2の販売方法は「オークション」のみ
          - その理由
            1.  オークションに「即決価格」機能も入れることで通常の販売機能も内包するため
            2.  オークションに絞って実装することで管理コストの削減
      - 実装する理由
        1.  オークションが無料主義の根幹の仕組みなため
      - 要件
        1.  **商材は、一回の出品で、一つ以上の、一人以上への、販売を可能にする**
            - 例
              - 複数個の出品、複数人へ出品、一個の出品、一人へ出品
        2.  **出品時に、入力欄として「販売個数」を指定可能**
            - v2では、「販売人数」を指定できない。無料主義アプリv3で実装予定
            - 「販売数量が一つ以上」かつ「1番手だけでは販売数が余る」場合は、マルチユニットオークションになる
        3.  **落札者を選ぶ条件**
            1. 同じ入札額なら先着順
            2. 違う入札額なら、高い入札額
            - つまり、枠より多いときに入札があった場合は、一番安い入札額の最後尾の入札者から落選させる
        4.  **落札価格の条件**
            - 要件
              - 落札できなかった入札者達の中で、一番高い入札額の人を、「ギリギリ落選者」と呼ぶ
              - 「ギリギリ落選者」より「入札日時が前の人」は、「ギリギリ落選者」と同じ額
              - 「ギリギリ落選者」より「入札日時が後の人」は、「ギリギリ落選者」の入札額に1単位だけ加算した額
              - 落札者は一律で、上記の額・条件で支払う
            - ポイント
              1.  「落札者を選ぶ条件」に沿って、入札日時が前なら同じ額でも落札できるのであれば、同じ額で支払ってもらう条件にしている。
            - 注意
              1.  「落札できない入札者の中で最高入札額+1単位だけ加算した額を、落札者全員が一律で払う」方式だけだと、無料で入札して落札して、先着順で取得できた場合に1ポイント支払う必要が出てきてしまう
        5.  **入札単位は、各評価軸が設定している最小単位**
        6.  **一人につき複数個を購入する場合**
            1. 優先度が高い人から順に購入枠を占めて、希望数だけ得られる
            2. 希望数が残っていない場合は、最後尾の人は希望数が残っている分だけ配布
        7.  **v2では、複数人への出品で「シングルプライス」方式のみ対応**
            - 説明
              - 上記以外の、「マルチプライス」や「Pay as
                Bid」や「VCG系」などの形式には対応しない。無料主義アプリv3で実装する予定
        8.  **即決価格**
            - 説明
              1.  出品者が設定した「即決価格」ですぐ落札できるようにする機能
            - 必要な理由
              1.  オークションだと時間がかかるが、即決価格だと通常の販売と同じように使用できるため
        9.  **入札時の保有ポイントチェックはしない**
            - 要件
              1.  入札時は消費せず、落札時にポイントを保有ポイントから差し引く
              2.  もし落札額を保有していない場合は、次の人の入札者が落札になる
        10. **入札額を保有していない場合の対応**
            - 説明
              - 入札したのに、実際に落札して消費するときに保有ポイントを保有していない場合のペナルティーを用意したい
            - 要件
              1.  「ブラックリスト行為」カウントのカラムを用意する
              2.  落札額を保有していない場合は、「ブラックリスト行為」カウントを一つ増やす
        11. **複数の種類の評価軸ポイントを組み合わせて入札する場合の処理**
            - v2の範囲
              - 各評価軸のポイント方式は「消費」のみ
                - 落札時に保有から差し引く
              - 「一定期間預けて還元」「消費なし」「ポイント譲渡」はv2では実装しない
            - 要件
              1.  （無料主義アプリv3以降を含む一般形）それぞれの評価軸ごとのルールでポイントを処理し得るが、v2では実質すべて「消費」として扱う
                  1. 即決価格
                     - 即決価格が設定されている場合で、即決価格NGの評価軸ポイントがある場合は、即決価格OKのポイントのみで入札できる
                  2. ポイントの扱い方（無料主義アプリv3以降で「預けて還元」「消費なし」「ポイント譲渡」を追加する場合の一般形）
                     - 一定期間預けて還元 or 消費 or 消費なし or ポイント譲渡
                     - それぞれの評価軸ポイントごとに、設定されているルールで扱う。全部一つで統一が必要なことはない。それぞれ別処理として扱う
                  3. 評価軸ごとに、出品やポイントの扱い方のルールが異なる
                     - 複数種類の評価軸ポイントを組み合わせて入札する必要がある場合には、それぞれのポイントごとに、それぞれのルールを適用する
              2.  ルール（消費・還元・消費なし等）は、**評価軸ごと**に（上記1の方針どおり）別々に適用しうる。割合の大小に基づいて特定の1評価軸のルールに集約はしない
        12. **複数ポイントの組み合わせ入札**
            - 要件
              1.  出品者が指定した、入札に使用できるパッケージの「ポイントの種類」や「ポイントの組み合わせの割合」でのみ入札可能
              2.  入札時に、他のポイントから交換可能にする機能は実装しない
                  - 本質的な機能ではない。
                  - 別画面で実装する予定なので、そちらから行ってもらう
        13. **入札単位**
            - 要件
              1.  入札単位は、評価軸の最小単位にする
        14. **出品者本人は自身の出品に入札できない**
        15. **オークションが既に終了している場合、新規入札は受け付けない**
        16. **時間は現地時間を表示**
            - それぞれのユーザーが利用している場所の現地時間に変換して表示
        17. **できる限りの、サーバー負荷・データベースやストレージの保存容量の削減**
        18. **入札タイプ（同額入札）の対応**
            - race conditionの同じ入札額のユーザーが複数いた場合は、入札日時が早い人が落札する先着順で決める
        19. **自動入札機能**
            - ユーザーが事前に上限額を設定し、システムが自動で入札を行う仕組み。
            - できる限り State で管理し、画面から離れるときに DB に保存するなどの工夫を行う。
            - **流れ**
              1.  まず現状の価格（初めは0ポイント）で入札する
              2.  0ポイントで購入できる権利が枠いっぱいになった場合は、ポイントを一単位増やして入札することで、先着順の並びで最後尾の人の商材購入の権利を奪える
                  - 1単位増やした場合は、一番後ろに追加して
              3.  落札者は、優先度が上の人から順に、その上の人が求めた数量ずつ割り当てていき、まだ残っている場合は次の人に割り当てる
                  - 落札者の最後の人は、希望数量が残っていない可能性がある注意文言を入れる
              4.  落札したが、消費する額のポイントを保有していない場合は、次の優先度の人に落札権を回す（手動入札の要件9・10と同様。保有ポイントがマイナス残高のときも、落札時に不足すればブラックリスト行為カウントの対象になりうる）。
            - **例**
              1.  3人の購入枠があり、a→b→cの順番で0ポイントで入札した場合は、優先度はa→b→cになる
              2.  その後、dが1ポイントで入札したら、cが枠から外れて、優先度はd→a→bになり、d,a,bが商材を得られる
                  - この際、すでに3人の枠を占めているため、dは0ポイントで入札できない。1ポイント以上になる
              3.  その後、cが5ポイントで入札したら、bが枠から外れて、優先度はc→d→aになる
              4.  その後、bが3ポイントで入札したら、aが枠から外れて、優先度はc→b→dになる
                  - cは5ポイントなので、優先度が一番高いまま
  4.  **落札後の画面**
      - 説明
        - 自分自身が、指定商材を落札したことを証明するページを作成したい。
        - そのため、証明に必要な情報が一つの画面にすべて記載された状態にしたい
      - 目的
        1.  自分がこの商材の購入者であることを示す仕組みを実装したい
        2.  落札情報URLを、自身が購入した事を示したいサイトにコピペすれば完了にしたい
      - 表示する項目
        1.  「**購入者**」の「認証しているサイト・サービス・SNS」と「認証サイト・サービス・SNSのユーザー名」一覧を表示
            - 必要な理由
              1.  外部の人が、その認証サイトとユーザー名を見て、ちゃんと同じ人物であることを確認できるようにしたい
        2.  「**出品者**」の「認証しているサイト・サービス・SNS」と「認証サイト・サービス・SNSのユーザー名」一覧を表示
            - 必要な理由
              1.  外部の人が、その認証サイトとユーザー名を見て、ちゃんと同じ人物であることを確認できるようにしたい
        3.  落札商材の内容
        4.  非公開設定は実装しない
            - 一旦は、全員が見える状態にする
        5.  複数人・複数個向けの出品（マルチユニット）の場合の画面表示にも対応する必要がある
            - 現状の実装は、**1人分・1個分の落札**を想定した表示に留まり、**複数人・複数個**の出し分けには未対応である

### v2：評価軸の作成・更新・プロフィール

- 概要
  - 評価軸に関する画面についての仕様

- 使用場面
  1.  評価軸ポイントの付与
  2.  ポイント分配する際の貢献度の参考

- 要件
  1.  **評価軸のプロフィール画面**
      - 目的・使用場面
        1.  評価軸を使用したいときに、この評価軸がどのような内容なのか確認するために使用する
      - 説明
        - 評価軸の設定事項などをすべて一覧表示できる機能
      - 要件
        1.  評価軸のプロフィールURL
            - 要件
              1.  URLは、評価軸名ではなく、評価軸IDを入れる
                  - 評価軸の名前が変わっても同じURLを使えるようにしたい
              2.  URLのPATHは、`/criteria/<nano_id>`にする
                  - 例）`https://freeism.app/dashboard/criteria/V1StGXR8_Z5jdHi6B-myT`
              3.  IDは、標準Nano IDにする
                  - UUIDは桁数が長過ぎてURLが長くなるため
        2.  非公開の設定は一旦は無し
            - 今後、非公開の設定の実装を検討する
        3.  基本的には、評価軸の設定事項をすべて表示
        4.  追加で表示する項目
            1. 評価軸のID
               - 使用場面
                 1. データ取得APIで使用する
               - 要件
                 1. IDコピペのボタンアイコンを表示
                 2. IDは、プロフィールURLと同じく**標準Nano ID**（文字列）にする
            2. アドミン権限保有者
               - 表示項目
                 1. オーナー名
                 2. オーナーのユーザーID
                 3. オーナーのプロフィールURL
            3. この評価軸から交換可能なポイント一覧
               - 説明
                 - この評価軸から、別の評価軸ポイントに変換できるポイントや比率を確認する
               - 表示する項目
                 1. 他の評価軸との交換比率一覧
                    - 「評価軸ID、評価軸の名前、交換比率」の一覧
                 2. 「他の評価軸ポイントに交換」ボタンを表示
               - 要件
                 1. ユーザーのプロフィールで行う交換と同じ要件
                    - CSVアップロードで行うなど
            4. この評価軸に交換可能なポイント一覧
               - 説明
                 - このプロフィールの評価軸に変換したい場合に使用
               - 表示する項目
                 1. 他の評価軸との交換比率一覧
                    - 「評価軸ID、評価軸の名前、交換比率」の一覧
                 2. 「この評価軸ポイントに交換」ボタンを表示
               - 要件
                 1. ユーザーのプロフィールで行う交換と同じ要件
                    - CSVアップロードで行うなど
        5.  **評価軸ごとに「他の評価軸との交換比率」をアップロード機能**
            - 説明
              - 他の評価軸との交換比率を登録する機能
            - 目的・使用場面
              1.  他の評価軸との交換比率を仕組みを成り立たせるために必要
            - 要件
              1.  アップロード形式は、CSVファイルのみ対応
                  - その理由
                    1.  一括で登録するのに対応したいため
                  - JSONに対応しない理由
                    1.  開発者以外は使い慣れていないため
              2.  「GUIフォーム」は非対応
                  - その理由
                    1.  GUIで設定するのは大変なため
              3.  交換可能か、各評価軸ごとに決められる
                  1. 「交換比率のアップロードがない」 or 「`0`と記載」の評価軸とは交換できないようにする
              4.  この操作ができる権限
                  1. 評価軸のアドミン権限保有者のみ可
                  2. 初回は、評価軸を作成した人のみアドミン権限を保有
              5.  アップロードに必要な項目
                  1. 交換比率を設定する評価軸のID
                  2. 交換比率の数値
              6.  設定できる画面
                  - 評価軸のプロフィール画面の「交換比率アップロード」ボタンから可能
        6.  **貢献評価を代用する仕組み**
            - 説明
              - 評価軸が貢献者に「貢献評価を代用する仕組み」を使用したポイントを付与するための機能
            - 目的・使用場面
              1.  評価軸の評価が追いつかないときに使用する
            - 処理の流れ
              1.  評価軸のプロフィール画面上の「評価代用アップロード」ボタンを押す
              2.  CSVをアップロード（ファイルの送信を完了する）
              3.  **アップロード完了後**（下記）に、内容の確認ダイアログを表示する
              4.  ユーザーが承認したら実行する
            - **「アップロード完了後」の定義**
              - **ポイント交換する機能**の「「アップロード完了後」の定義」に従う（**ポイント交換・ポイント譲渡**と**同じ**タイミング。CSVの内容は評価代用のパース・バリデーションに読み替える）。バリデーションエラー時は本ダイアログは出さず、エラー表示に従う。
            - 要件
              1.  CSVアップロードに対応する
              2.  類似度の係数は、「**ポイント交換する機能**」の交換比率を使用する
              3.  ユーザーのプロフィール画面からユーザー自身が交換しても良いし、評価軸のアドミン権限保有者が、「貢献評価を代用する仕組み」でポイント付与してもOKにする
              4.  キーの粒度は、「貢献評価を代用する仕組み」の類似度で付与したポイント or 正規な評価しか選べない設計にする
                  - 後から何度でも、どの評価軸のポイントでも採用可能で、更新・変更できるようにする
                  - 粒度が粗すぎるが、一旦は実装の簡略化のために年月日などの分け方はしない
              5.  「貢献評価を代用する仕組み」の類似度は、どの評価軸を選択するか指定できるようにしたい
              6.  「付与し直す」ボタンを用意
                  - 途中で類似度によって付与された場合に追加ポイント付与があった場合は、「付与し直す」ボタンで更新できるようにする
              7.  計算式
                  - A評価軸の付与ポイントを「貢献評価を代用する仕組み」で、B評価軸ポイントを取得した場合は、↓計算式で算出する
                  - A評価軸の月ごとのポイント合計額×類似度＝その月のB評価軸ポイント額
              8.  二重付与を防止
                  - 二重付与を防ぐために、貢献度アップロードの「タスクの実行年月」は**年月は必須・日時は任意**とし、タスク評価は更新できつつ二重の評価が発生しない仕組みにする
        7.  **評価軸ごとに「貢献したユーザー」をアップロード機能**
            - 説明
              - 評価軸が貢献者にポイントを付与するための機能
            - 目的・使用場面
              1.  評価軸が貢献者にポイントを付与するために必要
            - 処理の流れ
              1.  評価軸のプロフィール画面の「貢献者アップロード」ボタンを押す
              2.  「貢献者アップロード」のモーダルが開く
              3.  「アップロードに必要な項目」のヘッダーが順番通りに記載されたCSVファイルのテンプレをダウンロード
              4.  「アップロードに必要な項目」の内容が記載されたCSVファイルを作成
              5.  「貢献者アップロード」のモーダルの「ポイントの付与」ボタンを押す
              6.  開いたファイル選択画面から、作成したCSVファイルを選択
              7.  アプリ内で、正しいデータ構造かバリデーションする
              8.  不正な構造であれば、エラー表示
              9.  正しい構造の場合は、アップロード内容の確認ダイアログを表示
              10. 「OK」を押したら、実際にデータベースに登録
              11. 「プロフィールのSNS連携したURL」と「貢献度のアップロードのプロフィールのURL」の一致をみる
              12. 一致する場合は、貢献者にポイント付与
            - 要件
              1.  **設定できる画面**
                  1. 評価軸のプロフィールの「貢献者アップロード」ボタンから可能
              2.  **アップロード形式は、CSVファイルのみ対応**
                  - その理由
                    1.  一括で登録するのに対応したいため
                  - JSONに対応しない理由
                    1.  開発者以外は使い慣れていないため
              3.  **「GUIフォーム」は非対応**
                  - その理由
                    1.  GUIで設定するのは大変なため
              4.  **この操作ができる権限**
                  1. 評価軸のアドミン権限保有者のみ可
                  2. 初回は、評価軸を作成した人のみアドミン権限を保有
              5.  **アップロードに必要な項目**
                  1. **連携・認証したサービスのプロフィールURL**
                     - 要件
                       1. URLかどうかバリデーションする
                          - 正規表現でURLではない場合はエラーを返すようにしたい
                       2. ポイント付与したい場合は、**正規化した**アップロード URL が、当該ユーザーの**プロフィールに登録した連携 URL（複数可、各々正規化）のいずれか**に一致するか、確認文言を入れる
                     - 貢献度のアップロードで「アカウント名」ではなく、「URL」にする理由
                       1. 独自ドメインのブログでは、アカウント名自体が無い
                       2. はてなブログなどのURLでは、URLからアカウント名を柔軟に抜き出すことが難しい
                       3. 「アカウント名」or「メンション名」or「アカウントID」など表記ゆれがあるため
                       4. 独自ドメインのブログではアカウント名自体が無い
                       - **なので、「プロフィールに登録した連携 URL（複数可）」と「貢献アップロードの URL」を、正規化したうえで突き合わせる**
                     - 例
                       - `https://x.com/sugi_sugi_329`のようなプロフィールURL
                     - 注意
                       1. プロフィールURLが無い場合は、その人を表す別のSNSなどのプロフィールURLを貢献者アップロードのURL文字列に記載する
                       2. 「アップロードURL」と「連携URL」の**表記が微妙に異なる**場合
                          - 説明
                            - 貢献アップロードで指定する URL と、プロフィール連携で登録した URL が、末尾パスや略称の違いで**一致しないことがある**
                          - 方針
                            - **まず双方を正規化**する。それでも一致しない場合は、**プロフィール側に、想定しうる URL を複数登録**しておき、**正規化したアップロード URL が、そのいずれかと一致すれば**付与する
                          - 例
                            - はてなブログでは、プロフィールURLが↓のようにある
                              1. https://freeism.hatenablog.com/
                              2. https://freeism.hatenablog.com/about/
                              3. https://profile.hatena.ne.jp/freeism/
                            - 想定する表記が複数あるサービスでは、**該当しうる URL をプロフィールの連携に複数登録**する
                  2. **貢献度の数値**
                     - 要件
                       1. 負の数も記載可能
                          - その理由
                            1. 負の数が来たら、マイナス評価（貢献度ポイントを差し引く処理）にするために対応する
                          - 例）「-15」のようにハイフンを前に入れるとマイナスにする
                  3. **評価軸ID**
                  4. **評価軸内の管理ID**
                     - 要件
                       1. 記載は任意
                     - 実装する理由
                       1. 評価軸内で管理しているIDと突合するために使用する
                          - これとは別で、無料主義アプリ側で発行したレコードのIDもあるので、これを使用すれば問題ない場合は不要だが、一緒に評価軸側で管理しているIDも登録したい場合に使用する
                          - 「貢献者アップロードの内容を修正したり、ポイント付与履歴で付与結果を確認する際に、「評価軸内の管理ID」を参考に確認できるようにしたい」
                  5. **タスクの実行年月**
                     - 使用場面
                       1. 「貢献評価を代用する仕組み」で二重評価しないために使用
                       2. 履歴を遡るためにも使用
                     - 要件
                       1. **年月は必須・日時は任意**：タイムゾーンがUTCの、`年`と`月`は必ず記載する
                       2. 日付・時刻までは必須としない。任意で日時まで記載してもよい（記載する場合は`00:00:00`等で登録する想定）
                  6. 無料主義アプリが発行する貢献者アップロードした際に返されるID
                     - 使用場面
                       1. 貢献者アップロードのデータを上書きするために使用
                     - 要件
                       1. 記載は任意
                          - 記載した場合に、データが上書きされる
                  7. メモ
                     - 要件
                       1. 記載は任意
                       2. 型は文字列
                       3. 200文字以内の登録が可能
                       4. バリデーションする
              6.  **評価軸のアドミン権限を持つ人のみ貢献者アップロード可能**
              7.  **CSVアップロードを再度提出したら結果を上書き**
                  - 要件
                    1.  上書きする旨を表示する
                    2.  「無料主義アプリが発行する貢献者アップロードした際に返されるID」を記載したら上書きされる
                  - v2の方針
                    1.  上記**返却ID付き再アップロードによる上書き**はv2で実装する
                    2.  一覧から**1件ずつ**数値を直接修正する**リッチな修正UI**はv2では作らない（必要になったら将来検討する）
              8.  **一人あたり複数アカウントをアップロード可能**
                  - その目的
                    - 一つのアカウント名が変更されてても、他のアカウントでチェックできる様にしたい
                    - アカウント名を変更すると、アップロードしたURL文字列も変化する。その際にも確実に登録できるようにしたい
                    - 例）`https://note.com/freeism,https://x.com/sugi_sugi_329`
                  - 実装方法
                    1.  報酬額のテーブルとアカウントのテーブルを分けてリレーションを持たせて、アカウントのテーブルに外部キーを持たせて、その外部キー指定でアカウント名を取得
                    2.  アップロード時は、カンマ区切りで複数アカウントのアップロード可能
                  - **連携したアカウント名を変更したときの対応に備える仕組み**
                    - 懸念
                      - 連携した外部アカウントの名前を変更した場合は、以前に変更前のアカウント名で貢献度を付与していた場合は、どう対応すれば良い？
                    - 結論
                      - ポイント取り消ししない。
                      - 貢献度アップロード時のアカウント名と一致してポイント付与した後は、貢献度アップロードした内容を保存するDBテーブルのレコードに、「無料主義アプリ内ユーザーID」を保存するカラムを入れて、「ユーザーID」と「貢献度アップロード時の貢献度の数字」を紐づけることで、外部アカウント名を変更してもポイントが維持されるようにしたい。
              9.  **「保有ポイント」と「付与履歴」と「累計獲得ポイント」と「ポイント残高」は、「都度合算」しない**
                  - 説明
                    - 貢献度ポイントを付与された場合は、「付与履歴」と「累計獲得ポイント」と「ポイント残高」の別々のカラムで足し算する
                      - 「累計獲得ポイント」は、自動分配で割合を決める際に使用する
                      - 「保有ポイント」だと消費した分が省かれてしまうため、別途必要
                      - 同じポイントを別々で管理するので、処理漏れ対策
                        - 「履歴」と「残高」の両方の更新処理をまとめた関数を用意して、それですべてを更新するよう徹底する
                  - 要件
                    1.  「累計獲得ポイント」と「残高」と「付与履歴」は別で管理
  2.  **評価軸の作成画面**
      - 使用場面
        1.  評価軸を新規作成するために使用する
      - 説明
        - 評価軸を新規作成する画面
      - 要件
        1.  作成画面は、「商材の出品」、「評価軸の作成」等と同じ画面で行う
            - 1つ目の設問（作成する内容）で「評価軸」を選択すると以降の流れでCSVアップロードにより作成する
        2.  CSVアップロードによる登録
            - 説明
              - CSVアップロードによって作成する機能（GUIフォームによる登録は行わない）
            - CSVアップロードを実装する理由
              1.  一括で複数登録したい
              2.  登録操作をCSVに統一するため
            - 要件
              1.  CSVアップロード結果を確認できるようにする
                  - 説明
                    - CSVアップロード結果をモーダルとして表示して、一つの評価軸ごとに登録内容を確認できるようにしたい
              2.  CSVアップロード上限
                  - 説明
                    - 一回のCSVアップロードで、20個まで登録できるよう制限する
        3.  設定項目
            1. 評価軸の名前
               - 説明
                 - 評価軸の名前を指定する
               - 要件
                 1. 名前は、評価軸の中でユニークにする必要がある
                 2. 30文字以下のバリデーションを入れる
                 3. 必須項目
            2. 評価軸の説明
               - 説明
                 - 評価軸の目指す目標などを自由に記載する場所
               - 要件
                 1. 200文字以下のバリデーションを入れる
                 2. 必須項目
                 3. URLのハイパーリンク化も行う
            3. 評価軸に関連するURL
               - 説明
                 - Discordサーバー、GitHubアカウント、Xアカウント、ホームページなどの評価軸に関するURLを記載する
               - 要件
                 1. 20個まで設定可能なバリデーション
            4. 評価軸のアドミン権限の設定
               - 説明
                 - アドミン権限を付与する人の設定
                 - デフォルトでは、作成者のみがアドミン権限を保有する
               - 要件
                 1. 最大50名までアドミン権限を設定できる
                 2. ユーザーIDで指定する
                 3. カンマ区切りで指定する
                 4. 設定は任意
                    - 設定しない場合は、作成者のみがアドミン権限を持つ
            5. 評価軸の設定項目
               - 説明
                 - 評価軸に関するルールを設定する
               - 設定項目
                 1. 評価軸ポイントの他者への譲渡を許可するか
                    1. 許可する
                    2. 許可しない
                 2. 評価軸ポイントの他者との交換を許可するか
                    1. 許可する
                    2. 許可しない
                 3. オークション方式
                    1. シングルプライス
                    2. VCG系
                    3. pay-as-bid
                    - 無料主義アプリv2では、「シングルプライス」のみ選択可能
                 4. 「即決価格」を許可するか
                    1. 許可する
                    2. 許可しない
                 5. ポイント方式
                    1. 一定期間だけ預けて還元
                    2. 消費
                    3. 消費なし
                    4. ポイント譲渡
                    - 無料主義アプリv2では、「消費」のみ選択可能
                 6. ポイントの最小単位
                    - 必要な理由
                      1. この最小単位に到達するまで分配し続けるため
  3.  **評価軸の編集**
      - 目的・使用場面
        1.  評価軸の情報を更新するために使用する
      - 説明
        - 評価軸の情報を更新するために使用する画面
      - 要件
        1.  アドミン権限を持つ場合のみ、評価軸のプロフィール画面からCSVアップロードにより更新できるようにする
        2.  更新はCSVアップロードのみとする（GUIフォームやインライン編集は行わない）
        3.  アドミン権限を持つ場合のみ更新できる
  4.  **評価軸を検索する機能**
      - 目的・使用場面
        1.  使用したい評価軸を探すために使用する
        2.  自分がアドミン権限を持つ評価軸を探す
        3.  ポイント交換する評価軸を探す
      - 説明
        - 評価軸を検索できる機能・画面
      - 要件
        1.  全文検索できるよう設定
        2.  評価軸の名前、評価軸IDで検索できるようにする
        3.  ヘッダーの検索欄から検索できるようにする
            - 検索欄は、プロフィール・評価軸・パッケージの検索結果や他の検索結果と一緒に表示する
        4.  検索結果の一覧で、アドミン権限を持つ場合のみ「管理者・編集可能」バッジを表示する
        5.  アドミン権限を持つ評価軸・パッケージのみ検索できるフィルターを用意したい

### v2：パッケージを作成・更新・プロフィール

- 概要
  - パッケージに関する画面についての仕様
  - ここで設定したパッケージを、商材のプロフィールに紐づけるパッケージとして設定する

- 使用場面
  1.  入札時にパッケージで指定する
  2.  各プロフィールごとに使用する公式パッケージを明示する
  3.  ポイントを分配する際の組み合わせ

- 要件
  1.  **パッケージのプロフィール画面**
      - 目的・使用場面
        1.  使用したいときに、パッケージの詳細を確認するために使用する
      - 説明
        - パッケージの設定事項などをすべて一覧表示できる機能
      - 要件
        1.  パッケージのプロフィールURL
            1. URLは、名前ではなく、パッケージIDを入れる
               - 名前が変わっても同じURLを使えるようにしたい
            2. URLのPATHは、`/package/<nano_id>`にする
               - 例）`https://freeism.app/dashboard/package/V1StGXR8_Z5jdHi6B-myT`
            3. IDは、標準Nano IDにする
               - UUIDは桁数が長過ぎてURLが長くなるため
        2.  非公開の設定は一旦は無し
            - 今後、非公開の設定の実装を検討する
        3.  パッケージの設定事項をすべて表示
  2.  **パッケージの編集画面**
      - 目的・使用場面
        1.  パッケージの情報を更新するために使用する
      - 説明
        - パッケージの情報を更新するために使用する画面
      - 要件
        1.  アドミン権限を持つ場合のみ、パッケージのプロフィール画面からCSVアップロードにより更新できるようにする
        2.  更新はCSVアップロードのみとする（GUIフォームやインライン編集は行わない）
        3.  アドミン権限を持つ場合のみ更新できる
  3.  **パッケージの作成画面**
      - 使用場面
        1.  パッケージを新規作成するために使用する
      - 説明
        - パッケージを新規作成する画面
      - 要件
        1.  作成画面は、「商材の出品」、「評価軸の作成」等と同じ画面で行う
            - 1つ目の設問（作成する内容）で「パッケージ」を選択すると以降の流れでCSVアップロードにより作成する
        2.  CSVアップロードによる登録
            - 説明
              - CSVアップロードによって、パッケージを作成する機能（GUIフォームによる登録は行わない）
            - CSVアップロードを実装する理由
              1.  一括で複数登録したい
              2.  登録操作をCSVに統一するため
            - 要件
              1.  CSVアップロード結果を確認できるようにする
                  - 説明
                    - CSVアップロード結果をモーダルとして表示して、一つのパッケージごとに登録内容を確認できるようにしたい
              2.  CSVアップロード上限
                  - 説明
                    - 一回のCSVアップロードで、20個まで登録できるよう制限する
        3.  パッケージの設定項目
            1. パッケージの名前
               - 要件
                 1. 名前は、パッケージの中でユニークにする必要がある
                 2. 30文字以下のバリデーションを入れる
                 3. 必須項目
            2. パッケージの説明
               - 要件
                 1. 200文字以下のバリデーションを入れる
                 2. 必須項目
                 3. URLのハイパーリンク化も行う
            3. パッケージに関連するURL
               - 説明
                 - Discordサーバー、GitHubアカウント、Xアカウントなど
               - 要件
                 1. 20個まで設定可能なバリデーション
            4. **構成する評価軸の割合の指定**
               - 例
                 - `a : b : c = 1 : 3 : 5`
               - 説明
                 - ↑のように最小の値が最小の整数になるよう正規化してDBに保存する
               - 要件
                 1. 「評価軸ID」と「割合」を指定する
        4.  パッケージのアドミン権限の設定
            - 要件
              1.  最大50名までアドミン権限を設定できる
              2.  ユーザーIDで指定する
              3.  カンマ区切りで指定する
              4.  設定は任意
                  - 設定しない場合は、作成者のみがアドミン権限を持つ

3. **パッケージを検索する機能**
   - 目的・使用場面
     1. 使用したいパッケージを探すために使用する
     2. 自分がアドミン権限を持つパッケージを探す
   - 説明
     - パッケージを検索できる機能・画面
   - 要件
     1. 全文検索できるよう設定
     2. パッケージ名、パッケージIDで検索できるようにする
     3. ヘッダーの検索欄から検索できるようにする
        - 検索欄は、プロフィール・評価軸・パッケージの検索結果や他の検索結果と一緒に表示する
     4. 検索結果の一覧で、アドミン権限を持つ場合のみ「管理者・編集可能」バッジを表示する
     5. アドミン権限を持つ評価軸・パッケージのみ検索できるフィルターを用意したい

### v2：セキュリティ

- 概要
  - セキュリティを強化したい

- 要件
  1.  SupabaseのRLSの設定

### v2：利用規約

- 概要
  - 利用規約のページについて定める

- ↓を参考
  1.  [https://www.hayabusatrip.com/](https://www.hayabusatrip.com/)
  2.  [https://nani.now/ja/terms](https://nani.now/ja/terms)

- 利用規約のテンプレート
  1.  [https://kiyaku.jp/index.html](https://kiyaku.jp/index.html)

- 要件
  1.  利用規約のページを作成・更新して、ちゃんと文言を決める

### v2：プライバシーポリシー

- 概要
  - プライバシーポリシーのページについて定める

- ↓を参考
  - [https://www.hayabusatrip.com/](https://www.hayabusatrip.com/)
  - [https://nani.now/ja/terms?tab=privacy](https://nani.now/ja/terms?tab=privacy)

- プライバシーポリシーのテンプレート
  - [https://kiyaku.jp/index.html](https://kiyaku.jp/index.html)

- 要件
  1.  プライバシーポリシーのページを作成・更新して、ちゃんと文言を決める

### v2：ホーム画面の修正

- 概要
  - ホーム画面の修正について
  - [https://freeism.app/](https://freeism.app/)

- 要件
  1.  ホーム画面は、ログイン不要
  2.  ホーム画面の無料主義の記載は、ドキュメントの全文を載せない
      - 説明
        - 全文を載せず、初心者向けの解説部分と丁寧な図解のみ表示する
  3.  SSG（静的）で素早く表示されることを意識する
  4.  ドキュメントの詳細は、以下のURLに記載する
      - `https://freeism.app/dashboard/docs`
  5.  Googleドキュメント埋め込みを、Markdownに移行したい
      - その理由
        1.  読み込みを早くする
  6.  デザインの修正
      - 参考
        1.  [https://hidane.app/](https://hidane.app/)
        2.  [https://nani.now/ja](https://nani.now/ja)
        3.  [https://www.sociomedia.co.jp/category/shig](https://www.sociomedia.co.jp/category/shig)
        4.  [https://www.shokasonjuku.com/ux-psychology](https://www.shokasonjuku.com/ux-psychology)

### v2：ログイン

- 概要
  - ログイン部分の実装について定める

- 要件
  1.  Google One Tap SignIn/SignUp
      - 説明
        - 既にGoogleにログイン中の場合に、アプリにOne TapでGoogle認証ログインができる機能（Google One Tap
          SignIn/SignUp）を実装したい
        - 「canva.comみたいに、画面に入った瞬間、chromeの右上に、canva.comにgoogle.comの情報を使用してログイン」で簡単にログインできる仕組みを作ってみたい
      - 参考
        - [https://developers.google.com/identity/one-tap/android/overview?hl=ja](https://developers.google.com/identity/one-tap/android/overview?hl=ja)
  2.  ログインのモーダルを使用する
      - 説明
        - ログイン画面に遷移させず、ログインするモーダルを表示させる。それにより遷移による面倒さを減らしたい。
  3.  ログインのモーダルの背景にログイン後の画面を表示
      - 説明
        - ログインのモーダルの背景はログイン後の画面にして、モザイクを掛けた状態にする
        - これにより、利用率が上がるらしい

### v2：バンドルサイズを少なくする

- 概要
  - サービスの表示が早くなるよう実装したい

- 参考
  - [https://zenn.dev/catnose99/articles/nani-translate](https://zenn.dev/catnose99/articles/nani-translate)

- 内容
  1.  未使用コードを残さない
  2.  不要なライブラリは入れない
      - `lodash` 全体、巨大な icon pack、日付ライブラリの locale、chart/editor/map 系ライブラリなど
  3.  Tailwindのクラス名を増やさない
  4.  遅延読み込み
      - `React.lazy(() => import(...))` と `Suspense`
        で、コンポーネントが初めて描画されるタイミングまでコード読み込みを遅らせる
  5.  キャッシュ
      - HTTP caching、ETag、長期キャッシュ、filename hashing によって、変更されていない script を再転送しない
  6.  SVGもサイズが大きいものは`<img>`として読み込む

## v2：テーブル構造

- **ユーザー主キー・作成者/更新者のユーザー参照**
  - 本節の **UUID** と **文字列** が併記されている箇所は、DB 上 **UUID 型**（アプリ上では **文字列**
    として扱う）の意図で統一する。評価軸・パッケージ等の**業務上のID**は **Nano ID**（文字列）と混同しない。

- **フィールド命名（Prisma／アプリ層）**
  - 本節に列挙するカラム名は **Prisma モデルのフィールド名として camelCase**
    で統一する。PostgreSQL の物理カラムを snake_case とする場合は、`@map("snake_case")` で対応する。

- `evaluation-criteria`
  - 説明
    - 評価軸のデータを保存するテーブル
  - カラム
    1. `id`
       - 型
         - 文字列（標準Nano ID）
       - 説明
         - テーブルの主キー。プロフィールURLの`<nano_id>`と同一のNano ID
    2. `name`
       - 型
         - 文字列
       - 説明
         - 名前
    3. `description`
       - 型
         - 文字列
       - 説明
         - 説明
    4. `role`
       - 型
         - 文字列の配列（ユーザー`id`）
       - 説明
         - 更新・削除できる権限を持つ人の`id`の配列
    5. `createdAt`
       - 型
         - DateTime
       - 説明
         - 作成日
    6. `createdBy`
       - 型
         - UUID
         - 文字列
       - 説明
         - 作成者の`id`
    7. `updatedAt`
       - 型
         - DateTime
       - 説明
         - 更新日
    8. `updatedBy`
       - 型
         - UUID
         - 文字列
       - 説明
         - 更新者の`id`
    9. `deletedAt`
       - 型
         - DateTime（Nullable）
       - 説明
         - 削除日
    10. `deletedBy`
        - 型
          - UUID
          - 文字列（Nullable）
        - 説明
          - 削除者の`id`

- `packages`
  - 説明
    - パッケージのデータを保存するテーブル
  - カラム
    1. `id`
       - 型
         - 文字列（標準Nano ID）
       - 説明
         - テーブルの主キー。プロフィールURLの`<nano_id>`と同一のNano ID
    2. `name`
       - 型
         - 文字列
       - 説明
         - パッケージ名
    3. `combine-evaluation-criteria`
       - 型
         - JSON配列
       - 説明
         - 組み合わせる評価軸のデータを割合と共に保存する（要素ごとにJSONオブジェクト）。`evaluation-criteria-id`には`evaluation-criteria.id`（Nano
           ID）を格納する
       - JSONのキー
         1. `ratio`
         2. `evaluation-criteria-id`（評価軸のNano ID）

- `bidirectional_link`
  - タスク
    - [ ] テーブル・カラムの新規作成
  - 説明
    - 相互リンクの設定を保存するテーブル
  - このテーブルが必要な理由
    1. 認証URLごとに、`used`で**付与済み状態**と**連携成功時の占有**を管理するため
  - カラム
    1. `id`
       - 型
         - String
       - 説明
         - ID
    2. `url`
       - 型
         - String
       - 説明
         - 認証するサービスのURL
    3. `used`
       - 型
         - Boolean
       - 説明
         - **付与済みフラグ**としての意味と、**連携成功時に当該認証URLを他アカウントが奪えない占有**の意味を兼ねる。`true`の間は同一URLの新規連携を拒否し、付与・連携の整合にも使う
       - 必要な理由
         - 同一URLへの二重連携・重複付与を防ぐため

- `account-settings`
  - タスク
    - [ ] テーブル・カラムの新規作成
  - 説明
    - アカウントの設定を保存するテーブル
  - カラム
    1. `id`
       - 型
         - UUID
         - 文字列
       - 説明
         - テーブルのID
    2. `name`
       - 型
         - 文字列
       - 説明
         - 名前
    3. `description`
       - 型
         - 文字列
       - 説明
         - 説明
    4. `official-packages`
       - 型
         - 配列（オプション）
       - 説明
         - 公式パッケージを0個以上登録可能。複数登録可能で配列で保存する（各要素は`packages.id`のNano IDなど）
    5. `oidc-link`
       - 型
         - JSON
       - 説明
         - OIDC連携設定
    6. `bidirectionalLinkIds`
       - 型
         - 文字列の配列（各要素は`bidirectional_link.id`）
       - 説明
         - 相互リンク（認証URL）を**複数個**登録できる。OAuth以外の連携で「URLは最大30個まで」に合わせ、配列の最大件数も同様に制限する想定

- `UploadContributionPointHistory`
  - タスク
    - [ ] テーブル・カラムの新規作成
  - 説明
    - アップロードされた貢献度ポイントの履歴
  - カラム
    1. `grantedUserId`
       - 型
         - UUID
         - 文字列
       - 説明
         - ポイント付与先ユーザーの`id`
    2. `grantedPoint`
       - 型
         - 数値
       - 説明
         - 付与ポイント
    3. `evaluationCriteriaId`
       - 型
         - 文字列（標準Nano ID）
       - 説明
         - 評価軸（`evaluation-criteria`）の`id`（Nano ID）
    4. `uploaderUserId`
       - 型
         - UUID
         - 文字列
       - 説明
         - アップロード実行者の`id`
    5. `createdAt`
       - 型
         - DateTime
       - 説明
         - 作成日時
    6. `updatedAt`
       - 型
         - DateTime
       - 説明
         - 更新日時

- `UserContributionBalance`
  - タスク
    - [ ] テーブル・カラムの新規作成
  - 説明
    - 保有ポイント残高
  - このテーブルが必要な理由
    1. アップロードごとのポイント付与履歴と現在の合計額の両方が必要
    2. 毎回付与履歴から計算するとデータ取得量が大量になるため、別テーブルで合計額を管理しておきたい
  - カラム
    1. `id`
       - 型
         - UUID
         - 文字列
       - 説明
         - テーブルのID
    2. `userId`
       - 型
         - UUID
         - 文字列
       - 説明
         - 対象ユーザーの`id`
    3. `evaluationCriteriaId`
       - 型
         - 文字列（標準Nano ID）
       - 説明
         - 評価軸の`id`（Nano ID）
    4. `balance`
       - 型
         - 数値
       - 説明
         - 現在の保有残高（仕様確定後に意味を固定）

- `EarnedTotalPoint`
  - タスク
    - [ ] テーブル・カラムの新規作成
  - 説明
    - 累計獲得ポイント
  - このテーブルが必要な理由
    1. アップロードごとのポイント付与履歴と現在の合計額の両方が必要
    2. 毎回付与履歴から計算するとデータ取得量が大量になるため、別テーブルで合計額を管理しておきたい
  - カラム
    1. `id`
       - 型
         - UUID
         - 文字列
       - 説明
         - テーブルのID
    2. `userId`
       - 型
         - UUID
         - 文字列
       - 説明
         - 対象ユーザーの`id`
    3. `totalPoint`
       - 型
         - 数値
       - 説明
         - 累計獲得ポイント
    4. `evaluationCriteriaId`
       - 型
         - 文字列（標準Nano ID）
       - 説明
         - 評価軸の`id`（Nano ID）

- `BidHistory`
  - 説明
    - 入札履歴を保存するテーブル
  - カラム
    1. `userId`
       - 型
         - UUID
         - 文字列
       - 説明
         - 入札ユーザーの`id`
    2. `amount`（入札額）
       - 型
         - 数値
       - 説明
         - 入札額
    3. `bidAt`（入札日時）
       - 型
         - DateTime
       - 説明
         - 入札日時
    4. `bidType`
       - 型
         - Enum（通常入札 / 自動入札）
       - 説明
         - 入札タイプ
    5. `status`
       - 型
         - Enum（入札中 / 落札済み など）
       - 説明
         - 入札レコードの状態
    6. その他
       - 説明
         - 監査・取消し・オークション`id`（FK）など、実装時に必要な項目を追加

- `AutoBid`
  - 説明
    - 自動入札の設定を保存するテーブル
  - カラム
    1. `userId`
       - 型
         - UUID
         - 文字列
       - 説明
         - 設定ユーザーの`id`
    2. `auctionId`
       - 型
         - UUID
         - 文字列
       - 説明
         - 対象オークション（`Auction.id`）の`id`
    3. `autobidPoint`
       - 型
         - 数値
       - 説明
         - 自動入札の上限額等（仕様に合わせて定義）
    4. その他
       - 説明
         - 有効フラグ、更新日時など推奨項目を追加

- `AuctionReview`（`Review`）
  - 説明
    - オークション取引後の相互評価（出品者・落札者のレビュー）を保存するテーブル
  - カラム
    1. `id`
       - 型
         - UUID（PK）
       - 説明
         - テーブルのID
    2. `auctionId`
       - 型
         - UUID（FK to Auction）
       - 説明
         - 対象オークションの`id`
    3. `reviewerId`
       - 型
         - UUID（FK to User）
       - 説明
         - 評価者
    4. `revieweeId`
       - 型
         - UUID（FK to User）
       - 説明
         - 評価される人
    5. `rating`
       - 型
         - Integer（1〜5）
       - 説明
         - 星評価
    6. `comment`
       - 型
         - Text
       - 説明
         - 評価コメント
    7. `completionProofUrl`
       - 型
         - String（Nullable）
       - 説明
         - 完了証明の添付URL
    8. `isSellerReview`
       - 型
         - Boolean
       - 説明
         - 出品者による評価か買い手による評価か
    9. `createdAt`
       - 型
         - DateTime
       - 説明
         - 作成日時
  - ユニーク制約
    - `(auctionId, reviewerId, revieweeId)` — 1つのオークションにつき1回のみ評価可能

- `Auction`
  - 説明
    - オークションに出品される商品の情報を入れるテーブル
  - カラム
    1. `id`
       - 型
         - UUID
         - 文字列
       - 説明
         - テーブルのID
    2. `startTime`
       - 型
         - DateTime
       - 説明
         - 開始日時
    3. `endTime`
       - 型
         - DateTime
       - 説明
         - 終了日時
    4. `currentHighestBid`
       - 型
         - 数値（Int）
       - 説明
         - 現在の最高入札額
    5. `currentHighestBidderId`
       - 型
         - UUID
         - 文字列
       - 説明
         - 現在の最高入札者のユーザー`id`（`BidHistory.userId`・`User.id` と同一の UUID 方針）
    6. `winnerIds`
       - 型
         - **配列**（スカラーの UUID **ではない**）。
           - PostgreSQL: **`uuid[]`**（未確定・未落札時は **`'{}'`（空配列）または `NULL`** を採用する想定）。
           - Prisma: 例として **`String[]`** に `@db.Uuid` を付与できる形で
             **配列列**として定義（スキーマ実装時に確定）。
           - TypeScript / アプリ上: **`string[]`**（または `null`。空配列との使い分けは実装で決める）。
       - 説明
         - 落札確定後、当該オークションの**落札者であるユーザーの`id`を、配分結果の優先度順（単一枠・マルチユニット共通）で列挙したもの**。**単一落札でも要素は1つの配列**。**マルチユニットで複数名が落札する場合も、本カラムだけで落札者の一覧を表す**（落札結果用の別テーブル／中間テーブルは用意しない）。各入札の金額・落札数量の内訳などの詳細は
           **`BidHistory`** 側の確定レコードを正とし、本配列は一覧・表示用の参照（必要に応じて落札確定処理で
           `BidHistory` と同期して更新する）。
    7. `status`
       - 型
         - Enum（`PENDING`, `ACTIVE`, `ENDED`, `CANCELED`）
       - 説明
         - オークションの状態。デフォルト`PENDING`
    8. `createdAt`
       - 型
         - DateTime
       - 説明
         - 作成日時
    9. `updatedAt`
       - 型
         - DateTime
       - 説明
         - 更新日時
  - リレーション
    1. `bids`
       - 説明
         - `BidHistory`（1:N）。落札内容の詳細・数量はこちらを正とする。落札終了後、`winnerIds`
           と整合するよう落札確定状態の行を更新する。
    2. `currentHighestBidder`
       - 説明
         - 入札進行中の最高入札者（`User`）。単一参照。
    3. （任意）落札者ユーザーへの参照
       - 説明
         - `winnerIds` はユーザー`id`の配列のため、Prisma では
           **複数 FK のリレーション自動生成はしない**。必要ならクエリで `User` を `winnerIds` に含まれる `id`
           でまとめて取得する。別テーブルを増やさず、この配列列と `BidHistory` で足りる。

- その他
  - 説明
    - 他の仕様を見て、カラム追加・テーブル作成を行う

## v2：廃止する機能

1. **Server-Sent Events(SSE)は廃止する**
   - 廃止する理由
     1. `Redis Pub/Sub`では、ユーザー数が増えた際の課金が発生したり管理も大変
     2. 無料主義アプリv2の目的が、無料主義の内容を伝えることなので、開発者の負担になる設定を増やしたくない
     3. 本質的な機能ではない
   - 説明
     - クライアントが能動的にリロードしなくても、サーバーのデータ変更があった場合に反映されるようにしたいが、課金や管理の関係で廃止する。サーバー側では最高額かチェックする機構入れる。
     - `Redis`のPub/Subが一定以上の利用料で有料になるため実践的ではない。
     - 別アカウントで入札しているが現在の画面では最高額は更新されないので、最高額ではない額で入札してしまうとエラーになるがフロントエンドにも`最高額が更新されたのでリロードして価格を更新してください。`と表示する。
     - 利便性は悪いがRedisの価格を払えなさそう

2. **チャット機能**
   - 廃止する理由
     1. 本質的な機能ではないため

3. **画像管理の廃止**
   - 廃止する理由
     1. 画像の管理が大変。
     2. 他の開発者の環境セットアップの作業が増えることは避けたい
        - v2も、無料主義を伝えることが目的なため
   - 説明
     - 画像の表示機能や管理機能は廃止する

4. **グループ機能**
   - 廃止
     1. メンバーの登録
     2. グループ作成フォーム
5. **タスク系**
   - 廃止
     1. タスク報告機能
     2. タスクの「作成者」と「報告者」と「実行者」を分ける機能
     3. その他のタスク系の機能
   - メモ
     1. 出品機能のみ残す
6. **PWA**
   - 廃止する理由
     1. 本質的な機能ではないため
   - メモ
     - PWA 用マニフェスト・Service Worker（v2 では PWA 自体を廃止するため、これらも使わない）

7. **通知系のすべて（トースト除く）**
   - 廃止する理由
     1. 本質的な機能ではないため
     2. 無料主義アプリv2の目的が、無料主義の内容を伝えることなので、開発者の負担になる設定を増やしたくない
   - 説明
     - **廃止するもの**：メール通知・**通知センター／一覧型のアプリ内通知**・PUSH通知・通知作成機能など、継続的に溜まる通知インフラ。
     - **v2で実装するもの**：操作結果のフィードバック用の**アプリ内トースト**（短時間表示のトースト。`sonner` /
       `react-hot-toast` 等。ライブラリ節の採用方針と整合）。
     - メール系（廃止の参考）：`Resend` / `@react-email/*` / `react-email`
     - プッシュ通知（廃止の参考）：`web-push`
8. **リッチなUI**
   - 廃止
     - 全画面表示・Framer Motion・**リッチなフォーム**による一括登録系の**項目入力**（一括系は**CSV**に寄せる）
   - 説明
     - コード数も最小にして、仕様を理解できる最低限のコードにしたい
     - 一括登録・設定の本体は**CSVアップロード**とし、**操作のためのボタン、確認ダイアログ、必要最小限の入力**は置いてよい。

## v2：実装しない機能

1. **URLのパスから `dashboard` を取り除く改修**
   - v2では実装しない
   - 方針
     - 無料主義アプリの画面・ドキュメント用URLは **`https://freeism.app`（`www` なし）** とし、**`dashboard`
       をパスに含める**（例：`https://freeism.app/dashboard/settings/`、`https://freeism.app/dashboard/docs`）形で設計・実装する。取り除く改修は行わない。
   - 採用理由
     1. アプリ領域の切り分けと同一ホスト内のパス一貫性のため。

2. **指定ユーザーが設定する公式パッケージのデータを取得するパブリックAPI**
   - v2で実装しない理由
     1. 無料主義を説明する上で必須ではない
     2. 必要性はありそうなので、あとから実装する
   - 説明
     - 自分自身の無料主義アカウントに設定している公式パッケージのデータを、無料主義アプリのパブリックAPIから取得して、表示内容を出し分ける。
   - 使用理由
     1. 「非干渉の仕組み」で使用したい
3. **逆オークション**
   - v2で実装しない理由
     1. 無料主義を説明する上で必須ではない
     2. 必要性はありそうなので、あとから実装する
   - 必要な理由
     - 無料主義の評価軸ポイントを使用した逆オークションを実施できる場がない
     - 多くのプラットフォームは既存通貨を対象としていて、気軽にいろいろなポイントを使用した逆オークションの場がない
   - 説明
     - 需要者が支払う事前の固定報酬額を決めるために、供給者が報酬を下げる方向で争う逆オークションする
     - 買い手がオークションを作成して、供給側が報酬額を下げていくオークション
     - 供給側の報酬を得る方法
       1. 買い手からの落札額のポイント譲渡
       2. 供給側の行為による評価軸からの評価による成果報酬
4. **市場を作る機能**
   - v2で実装しない理由
     1. 無料主義を機能させるうえで全く不要な機能
   - 説明
     - **需要側が支払う事前の固定報酬額は、供給者が受託まで報酬を徐々に高める機能**
     - オークションではなく、市場を作っている
   - 実装は不要な理由
     - 評価軸コミュニティ内で、時間単位ごとに報酬を増加して貢献度を評価するルールで運用すれば良いだけ。
     - 無料主義アプリに実装する必要は全くない
   - 使用例
     - React管理者がIssue作成時に自動で↑設定を行い、解決した人に報酬を付与する。
     - **無料主義アプリの機能としては不要だが、評価の方法としては有用**
     - 無料主義では受益者負担がないから、無尽蔵に報酬を増やせるので↑選択を取りやすい。でも、インフレしすぎるかも。
5. **事前の固定報酬を市場で決める仕組み**
   - v2で実装しない理由
     1. 無料主義を説明する上で必須ではない
     2. 一旦は、評価軸コミュニティ内でスプレッドシート管理で対応できそう
   - 説明
     - 各タスクに賞金をかける仕組み
     - その賞金は時間がたつほど徐々に報酬額が上がる
   - 使用場面
     - OSSのIssueの質問回答タスク
     - 15分ごとに一単位づつ報酬が増加する
   - 流れ
     1. （需要側）賞金タスクを作成
        - 作成時に、「タスク名」、「タスク内容説明」、「完了判定方法」、「初期の報酬額」、「一単位の増加時間」、「一単位の増加報酬額」、「提出可能人数」を設定
        - 例）「提出可能人数」は、ごみ拾いなど、何人でもよい場合は複数人設定で可能
     2. （供給側）タスク内容を確認して、タスク完了報告
        - 「提出可能人数」まで先着順で締め切る
     3. （需要側）完了を需要側が確認・承認して、賞金を確定
        - 完了報告ではなく、需要側による完了報告のチェックまで賞金は増え続ける。
          - それにより、早く完了報告の内容をチェックするインセンティブにしたい。
     4. （需要側）完了チェックした人に報酬を自動で付与
   - 工夫
     1. パブリックAPI経由で、タスクを作成できるようにしたい
        - GitHub内OSSでIssueが作成されたら自動で無料主義アプリの賞金タスクが作成されるフローを作成したい
6. **SSI・DID・Web5・Solid（バーナーズ＝リー）**
   - v2で実装しない理由
     1. 無料主義を説明する上で必須ではない
     2. 必要性はありそうなので、あとから実装する
   - 必要な理由
     1. 個人で情報を管理せず、情報は各個人が管理する社会の移行に備えたい
   - 説明
     1. データを自身で管理する仕組み
   - 参考
     - [https://cloud-ace.jp/column/detail376/](https://cloud-ace.jp/column/detail376/)
7. **格差是正の仕組み（枠）**
   - v2で実装しない理由
     1. 無料主義を説明する上で必須ではない
     2. 必要性はありそうなので、あとから実装する
   - 説明
     1. 平方根にする方式
        - 保有ポイントが2500ポイントなら50ポイントだけ使用できる
     2. 当選率を使用する方式
        - 消費額や入札額を預けるのではなく、商材の応募率も掛け合わせた数を預ける仕組みにする？
        - **(1+(1-当選率))×入札額=得るために必要なポイント額**
          - 当選率が10%で購入額が500ポイントの場合は、1.9×500=950ポイント消費する
8. **競争を激化させる仕組み**
   - v2で実装しない理由
     1. 無料主義を説明する上で必須ではない
     2. 必要性はありそうなので、あとから実装する
   - 説明
     - 競争を激化させるために、無料主義アプリ間の移行が簡単になる仕組みを実装したい
   - 激化させる要素
     1. インポート機能
     2. エクスポート機能
        - 要件
          1. CSV形式
          2. 一回につき200件に制限
             - その「200件」という件数は定数管理して、すぐに変更できるようにする
          3. ページネーション
             - 次の200件が欲しい場合は、エクスポート専用のページネーションの入力欄に2と指定。
             - なしだと1ページ目が出てくる
     3. 全コードのOSS化
     4. ドキュメント全てを公開
     5. Fediverse的設計
     6. WebFingerプロトコルの対応
        - Mastodonのように相互運用性を作るための、データ取得場所を返すWebFingerの実装
9. **ルールの粒度を「取引」にする設定**
   - v2で実装しない理由
     1. 無料主義を説明する上で必須ではない
     2. 実装が大変
   - 説明
     - 評価軸ごと「取引ごと」に、「オークション」や「即決価格」などの購入方法や「消費あり」や「一定期間預ける」などポイントの扱い方を選べるようにしたい
     - 評価軸で2択まで絞って、出品時にどちらかを選べるようにしてもよさそう
10. **共同所有自己申告税COST**
    - v2で実装しない理由
      1. 無料主義を説明する上で必須ではない
      2. 資産を登録する方法や実装が大変
    - 「共同所有自己申告税COST」の説明
      - 所有財産に一定の税率を掛け算した額を納税する
      - その人より多くの財産価値を提示した人に、強制的に引き渡す仕組み
      - 財産はブロックチェーンに登録して、上回る提示額の人にスマートコントラクトで自動で所有権が引き渡されるようにする
    - COSTの無料主義版として、自分が所有する財産価値の分のポイントを預ける。預けたポイントより多く預ける人がいたら、他に引き渡す必要がある仕組みでも良さそう
11. **2要素認証・パスキー**
    - v2で実装しない理由
      1. 無料主義を説明する上で必須ではない
      2. 必要性はありそうなので、あとから実装する
    - 要件
      1. ２要素認証の実装
      2. パスキーの実装
12. **オークションの入札形式（`Pay as Bid`,`VCG`）**
    - v2で実装しない理由
      1. 無料主義を説明する上で必須ではない
      2. 必要性はありそうなので、あとから実装する
    - 説明
      - v2では、複数人への出品で「シングルプライス」方式のみ対応。
      - 無料主義アプリv3で実装予定として、`Pay as Bid`,`VCG`など別の入札方式にも対応する
13. **ポイントの使用方法として、「預ける」「消費なし」「ポイント譲渡」**
    - v2で実装しない理由
      1. 無料主義を説明する上で必須ではない
      2. 必要性はありそうなので、あとから実装する
14. **落札後の画面の限定公開機能**
    - 機能説明
      - 「出品者」と「購入者」のみが閲覧できる設定の機能
      - 通常はだれでも確認可能
    - v2で実装しない理由
      - 本質的ではなく、必須ではなく、必要になったらあとで実装で大丈夫そうなため
15. **条件付き代理購入する仕組み（権利を提供する側の人に戻ってくる）の機能**
    - v2で実装しない理由
      - 本質的ではなく、必須ではなく、必要になったらあとで実装で大丈夫そうなため
      - レバレッジを利かす機能は、一旦は「譲渡可能なポイント」機能で借金する仕組みのみで良さそう
        - 借金額の返済管理自体は、各評価軸の無料主義アプリの外で管理してもらう
      - **本項の「借金」と下記17項（借入・返済の管理）の関係**
        - 本項の「借金」は、**下記17項のようにアプリ内に借入枠・返済スケジュールの台帳を持つ機能**を指すものではない。v2は**譲渡可能なポイントの移動**に留め、返済の記録・督促は**各評価軸の運用（アプリ外）**に委ねる。17項は、そうした本格の借入管理をv2のスコープから外すための節である。
    - 機能説明
      - レバレッジを利かす機能
      - 付与したら、その人が使用した時に戻ってくる期間が決まる
16. **保有ポイントを操作するパブリックAPI**
    - 使用場面
      - 外部の販売サービスで購入した際に、無料主義のポイントを消費する仕組みにすることで、各業界の販売サービスから無料主義ポイントを使えるようにしたい
    - 要件
      1. 指定ユーザーの、指定の評価軸ポイントを、指定の額だけ消費するAPI
    - 使用する流れ
      1. **（サービスの設定）** 外部サービスと無料主義アプリを API 連携するするために環境変数を設定する
      2. 保有ポイントの取得
         - 保有ポイントがある場合のみ購入できる
      3. **（落札後の供給側の流れ）** 購入者の情報を受け取り、ポイントを消費する
17. **出品・落札・購入のパブリックAPI化**
    - 本項の対象
      - **外部から入札・出品・購入を自動化**する、**落札通知とポイント減少を外部に一括連携**する、など**取引の書き込み・通知に踏み込んだ**パブリックAPI。**「パブリックAPI」節でv2とする読み取り専用API（保有ポイント・落札証明・ユーザー情報）とは別**である。
    - v2で実装しない理由
      1. 無料主義を説明する上で必須ではない
      2. 必要性はありそうなので、あとから実装する
    - 懸念点
      - オークション機能も無料主義アプリに組み込まないほうが良いのでは？
    - 本懸念・外部連携方針の整理
      - **無料主義アプリv3以降**に、オークションをアプリに集約し続けるか、外部化・API中心に振るかを含め再検討する。v2は下記の結論のままとし、**本段の再整理は無料主義アプリv3以降の対応**とする。
    - 結論（v2の当面方針）
      1. 一旦は、埋め込む
      2. 一旦は、貢献度のアップロードで、マイナス評価で、マイナスポイントにすることで消費する代用で済ませる
    - パブリックAPI化したほうが良い理由
      1. 購入方式が大量にある
         - オークション・即決価格・指定価格の購入など色々な方式があり実装が困難
      2. 外部アプリから使えるようにしたい
         - 外部アプリからの購入に無料主義ポイントを使用できるようにしたい
         - Amazon、Yahoo!オークション、メルカリ、他の無料主義アプリなどで、無料主義アプリのポイントを使用して購入して、銀行機能としてのポイント差し引きを
    - 要件
      1. 指定ユーザーの保有ポイント取得
      2. 指定ユーザーの落札通知＆保有ポイントの減少
      - トークンを発行して権限管理が必要
18. **「借入・返済の管理」機能**
    - 上記15項の「借金」との違い
      - 本項は、**本格的な借入・返済の台帳・残高管理をアプリに実装する**想定の機能を指し、v2では実装しない。15項の「譲渡で肩代わり」するレベルとは切り分ける。
    - v2で実装しない理由
      1. 無料主義を説明する上で必須ではない。
         - 評価軸のそれぞれが別々に関すれば問題ない
19. **UIの改善**
    - 無料主義アプリv3以降の対応
      - 本項に挙げる**要件（以下のリスト）は無料主義アプリv3以降の対応**とし、v2では実装しない。v2は本筋の機能に寄せ、過度なUI改修を避ける。
    - v2で実装しない理由
      1. 無料主義を説明する上で必須ではない
      2. **v2までは本質的な機能のみで、UIは過度に強化しない。**
    - 要件
      1. サイドバーを、Shadcn/uiで書き換えて、畳めたり、他のアクセンシビリティに対応したい
         - [https://ui.shadcn.com/docs/components/sidebar](https://ui.shadcn.com/docs/components/sidebar)
      2. 長いリストの場合に、CL側で仮想化して、表示されているコンポーネントのみレンダリングするライブラリ
         - [https://tanstack.com/virtual/latest/docs/introduction](https://tanstack.com/virtual/latest/docs/introduction)
         - 無限スクロールでは、`TanStack Virtual`を使うと良い
      3. 用意できたUIから徐々に表示するStreaming UIを試してみたい
         - [https://zenn.dev/akfm/articles/streaming-ui-practice](https://zenn.dev/akfm/articles/streaming-ui-practice)

## v2：ライブラリ

### v2：使用するライブラリ

- 参考
  - [https://zenn.dev/catnose99/articles/f8a90a1616dfb3](https://zenn.dev/catnose99/articles/f8a90a1616dfb3)
  - [https://zenn.dev/catnose99/articles/nani-translate](https://zenn.dev/catnose99/articles/nani-translate)

- テスト
  1.  `Playwright`
      - 参考
        - [https://reffect.co.jp/html/playwright](https://reffect.co.jp/html/playwright)
      - 説明
        - E2Eテストのライブラリ
      - 使用場面
        - E2Eテスト
      - 使用する理由
        - 他のE2Eと比べてAPIが安定、ブラウザ自動化とトレース、ドキュメントが揃いやすいため
  2.  `prisma-fabbrica`
      - 参考
        - [https://github.com/Quramy/prisma-fabbrica](https://github.com/Quramy/prisma-fabbrica)
      - 説明
        - テスト用データの作成
      - 使用する理由
        1.  テスト用データの作成を簡略化するため
  3.  `@testing-library`
      - 説明
        - 単体テスト
      - 使用する理由
        - 実装の内部ではなくユーザー操作に近い形でUIをテストしやすいため
  4.  `Vitest`
      - 参考
        - [https://reffect.co.jp/react/react-test](https://reffect.co.jp/react/react-test)
          - [https://reffect.co.jp/html/vitest](https://reffect.co.jp/html/vitest)
      - 説明
        - 単体テスト
      - 使用する理由
        1.  Jestに比べて、実行速度がはやいし、設定が簡単

- CI/CD
  - GitHub Actions
    - [https://zenn.dev/kiwichan101kg/articles/2d6850ff72bc98](https://zenn.dev/kiwichan101kg/articles/2d6850ff72bc98)
- 同じ環境
  1.  Docker・Docker Compose
      - 説明
        - アプリやDBをコンテナ化し、依存関係をイメージに閉じ込める仕組み
      - 入れる理由
        1.  ローカルとCI、チーム内で同じ`compose`定義に寄せ、環境差のトラブルを減らすため
- markdownからhtmlへの変換
  1.  `micromark`
      - 説明
        - ホーム画面のmarkdownからhtmlへの変換
      - 参考
        - [https://github.com/micromark/micromark](https://github.com/micromark/micromark)

- エラー通知
  - Sentry
    - 説明
      - Sentryでエラー通知がslackやemailで来るようにする
      - また、リロード時や認証時に、uidを仕込む
      - そうすることによって、より、バグの特定がしやすくなります。
      - 環境`tag`(`production`, `staging`, `development`)も入れることで、通知の出し分けができる
    - 使用する理由
      1. 本番の例外を一箇所に集め、文脈（ユーザー・リリース・環境）付きで追いやすいため
- UIコンポーネント
  - `Shadcn/ui`
    - 説明
      - Radix UIを下敷きにした、コピー＆貼り付けで持ち込むUI部品
    - 使用する理由
      1. 見た目のトークンを揃えつつ、コードをリポジトリ内で手直ししやすいため
- CSSライブラリ
  - Tailwind CSS
    - 説明
      - ユーティリティクラスでスタイルを組み立てるCSSの書き方
    - 使用する理由
      1. 小さなUI調整の記述が速く、余白・タイポの揃え方をルール化しやすいため

- 配色
  - 説明
    - 配色だけで良いデザインに見える
  - 参考1. [https://colorhunt.co/](https://colorhunt.co/) 2.
    [http://hue360.herokuapp.com/](http://hue360.herokuapp.com/) 3.
    [https://color.adobe.com/ja/create](https://color.adobe.com/ja/create)
  - 使用する理由
    1. 良いデザインに見せるため

- 言語
  - TypeScript
    - 説明
      - バックエンド・フロントエンドの両方で使用する
    - 使用する理由
      1. 型安全にしたい
      2. できるだけ読める人が多い言語を使用したい
- ビルド
  - `Vite+`
    - 説明
      - 開発サーバーと本番用バンドルを担うフロントエンド向けビルドツール
    - 使用する理由
      1. 起動とHMRが速く、TypeScript・バンドル周りの初期設定負担が小さいため
- フォーマット
  1.  `Oxfmt`
      - 説明
        - コードの自動整形（フォーマッタ）で、Oxide系の高速実装
      - 使用する理由
        1. 保存時やCIの整形待ちを短くし、差分のノイズを減らすため
- リンター
  1.  `Oxlint`
      - 説明
        - JavaScript/TypeScript向けの静的解析（リント）ツール
      - 使用する理由
        1. 実行速度が早い
- エディタ・AI
  1. Cursor
     - 説明
       - AI補完・チャット付きのコードエディタ
     - 使用する理由
       1. 実装の下書きやリファクタ案の出し方が速く、作業の待ち時間を減らせるため

- デザイン
  1.  `Figma`
      - 説明
        - 画面案・ワイヤー、コンポーネント表をブラウザ上で共有するデザインツール
      - 使用する理由
        1. レイアウトと用語の合意形成を前に出せ、実装前の手戻りを減らせるため

- ORM
  1.  Prisma ORM
      - 使用する理由
        1.  スキーマファイルがあるので、分かり易い
        2.  クエリの構文が分かり易い
      - メモ
        1.  Drizzleも分かり易いため、それに移行を考えても良さそう

- REST API
  - 説明
    - HTTPのメソッドとURLでリソースの取得・更新などを扱うAPI形態
  - 使用する理由
    1.  `GraphQL`より実装がシンプル
- データベース
  - `Supabase`
    - 説明
      - データベースとして使用
    - 使用する理由
      1.  設定が簡単

- データベースの言語
  - PostgreSQL
    - 説明
      - リレーショナルDBの一種。Supabaseの中核がPostgreSQL
    - 使用する理由
      1.  拡張・標準SQL・運用知見が豊富で、小規模からでも伸ばしやすいため
- バリデーション
  - `zod`
    - 説明
      - スキーマで値の形と制約を定義し、型とバリデーションをまとめて扱う
    - 使用する理由
      1.  ランタイム検証とTypeScript型を一つの定義から揃えやすいため

- Better Auth（`better-auth`）
  - 説明
    - ログイン認証のライブラリ
  - 使用する理由
    1. 使用している`Auth.js v5`が Better Auth に合併したため

- ホスティング
  - Vercel
    - 説明
      - フロント（SSR/Edge含む）のデプロイとCDN、プレビューURLを提供するPaaS
    - 使用する理由
      1.  Git連携とプレビュー、環境変数、グローバル配信までを設定少なめにまとめられるため

- フロントエンドのフレームワーク・ライブラリ
  - React
    - 説明
      - UIをコンポーネントに分割し、宣言的に描画を組み立てるライブラリ
    - 使用する理由
      1.  採用例と学習資源、周辺ライブラリが多く、長期的に人を巻き込みやすいため
  - TanStack Start
    - 説明
      - TanStackエコシステムと相性のよいフルスタック/ルーティング基盤
    - 使用する理由
      1.  Query等と同系の考え方で、データ取得とルートを一気通貫しやすいため
- バージョン管理
  - GitHub、Git
    - 説明
      - 変更履歴の記録（Git）と、リモート上での共有・PR・Actions（GitHub）
    - 使用する理由
      1. 差分の追跡、レビュー、自動ビルドまでを一つのフローに乗せられるため
- ドメインを取得
  - `Cloudflare Register`
    - 説明
      - Cloudflare経由のドメイン登録。DNS・プロキシと併用しやすい
    - 使用する理由
      1.  費用が安い
      2.  面倒な売り込みやダークパターンが少ない
    - 懸念事項
      1.  住所の登録などが英語圏向けの設計なので設定がうまくいかない時があった

- Webサイトの利用規約
  - `kiyaku.jp`
    - 説明
      - ポートフォリオの利用規約やプライバシーポリシーのテンプレート
    - 使用する理由
      1. ゼロから条文案を作る工数を抑え、不足しがちな定型項目の見落としを減らせるため
    - 参考
      - [https://kiyaku.jp/index.html](https://kiyaku.jp/index.html)
- 上記以降（クライアント周りの追加ライブラリ）
  1.  `TanStack Query`
      - 説明
        - ステート管理、ローディング処理、エラーハンドリングが簡単になる
      - 参考
        - [https://reffect.co.jp/react/tanstack-query-v5](https://reffect.co.jp/react/tanstack-query-v5)
        - [https://reffect.co.jp/react/tanstack-query](https://reffect.co.jp/react/tanstack-query)
      - 使用する理由
        1.  サーバー状態のキャッシュ、再取得、エラー再試行を宣言的に扱い、各画面のボイラープレートを減らせるため
  2.  `day.js`
      - 説明
        - 日付の比較・加算・フォーマット等。**v2 では日付まわりは `day.js` に統一**する（v1 で `date-fns`
          を使っていた箇所も置き換え想定）
      - 参考
        - [https://github.com/iamkun/dayjs](https://github.com/iamkun/dayjs)
      - 使用する理由
        1.  必要十分でバンドルが軽いため
  3.  `React Hook Form` / `@hookform/resolvers`
      - 説明
        - フォーム状態と`zod`等リゾルバの連携
      - 使用する理由
        1.  入力多めの画面で再レンダー抑止とスキーマ検証をまとめて扱いやすいため
  4.  `@vercel/analytics` / `@vercel/speed-insights`
      - 説明
        - Vercel上の訪問・Web Vitals系の簡易計測
      - 使用する理由
        1.  ホスティングと一体の軽量アナリティクス
  5.  `fishery` / `@faker-js/faker`
      - 説明
        - テスト用のダミーデータ生成
      - 使用する理由
        1.  `fishery`+`faker`でファクトリを書いているため
  6.  `Husky` / `lint-staged` / `commitlint`+`commitizen`（他）
      - 説明
        - コミット前のLint/整形、コミットメッセージ規約
      - 使用する理由
        1.  手元の差分だけを軽く検査し、履歴の粒度を揃えやすいため
  7.  `@t3-oss/env-core`
      - 説明
        - サーバー/クライアント用の環境変数をスキーマで型付け（**Vite+ / TanStack Start**
          向け。フレームワーク非依存のコア）
      - 使用する理由
        1.  ビルド時に不足や誤用を早期に落としやすいため
  8.  `nuqs`
      - 説明
        - 検索パラメータ（URLのクエリ）をReactの状態と双方向に同期
      - 使用する理由
        1.  フィルタ・タブなどをURL化し、共有・リロード耐性のあるUIにしやすいため
  9.  `lucide-react`
      - 説明
        - 線画ベースのSVGアイコンをReactコンポーネントとして提供
      - 使用する理由
        1.  `Shadcn/ui`系のUIと合わせやすく、アイコンの揃いとバンドル分割に向くため
  10. `cmdk`
      - 説明
        - コマンドパレット（⌘K風）のUIを組み立てる primitive
      - 使用する理由
        1.  キーワード絞り込み付きのショートカット一覧を短時間で実装できるため
  11. `sonner` / `react-hot-toast`
      - 説明
        - 画面端に一時的に出すトースト通知（成功・失敗・案内の短文）。**メール／PUSH／通知一覧などは廃止するが、本トーストは v2 で実装する**（「v2：廃止する機能」の通知系（トースト除く）と整合）。
      - 使用する理由
        1.  操作結果のフィードバックをモーダルなしで統一表現しやすいため
  12. `papaparse` / `fflate`+`file-saver`
      - 説明
        - CSVパース、zip/解凍とクライアント保存の補助。ファイル選択は`<input type="file">`等のネイティブUIに統一し、ドラッグ＆ドロップは使わない
      - 使用する理由
        1.  インポート・エクスポートで役割に応じて使い分けるため（D&D非採用方針と整合させる）
  13. `knip` / `ts-prune`
      - 説明
        - 未使用のexport、未参照のファイルや依存の検出補助
      - 使用する理由
        1.  デッドコードと依存のノイズを減らし、保守対象範囲を絞りやすいため

### v2：使用しないライブラリ

- Hono
  - 例
    - Hono + Inertia
  - 使用しない理由
    1. 学びが足りなくて自分がしたいことができるか理解できていない

- ランタイム
  - Bun
    1. 学びが足りなくて自分がしたいことができるか理解できていない

- UI・入力
  - `framer-motion`（Framer Motion）
    - 使用しない理由
      1.  v2ではリッチなモーションを廃止し、仕様理解優先のため（「v2：廃止する機能」の方針と整合）
  - `react-dropzone`
    - 使用しない理由
      1.  ドラッグ＆ドロップは廃止し、ファイル選択はボタン＋`<input type="file">`に統一するため

- RPC
  - tRPC
    - 参考
      - [https://trpc.io/docs](https://trpc.io/docs)
    - 説明
      - スキーマやコード生成なしに、完全にタイプセーフなAPIを簡単に構築できる
    - 使用しない理由
      1. 一旦は、TanStack Startで十分

- Analytics・トラッキング
  - Google Analytics
    - 説明
      - ユーザーが今何人サイトのどのページを見ているかリアルタイムで見れたり、日や月単位で何人に訪問されたのか、直帰率はどのくらいか等々分析することができます。
    - 使用しない理由
      1. 初版は計測ニーズが小さく、Cookie周りの表記と実装の手間を先に払わないため
  - `react-ga`
    - 説明
      - React向けライブラリ
    - 使用しない理由
      1.  上記と同じく、トラッキング導入を後回しにするため

- Terraform
  - 説明
    - クラウドのインフラを、コードで記述することにより自動で構築できます。Terraformを利用することでオペレーションミスが無くなるほか、複数環境でコードを再利用することで効率化を図ることが出来ます。
  1.  使用しない理由
      - 個人規模ではVercel等のPaaSとコンソール設定で足り、IaCの学習・差分管理コストを払わないため

- 広告
  - Google Adsense
    - 説明
      - サイト内に広告枠を設け、表示やクリックに応じて収益化するGoogleのプログラム
    - 使用しない理由
      1. ポートフォリオ表示を優先し、審査・表示ポリシー・プライバシー表記の工数に見合う収益が出にくいため
  - `react-adsense`
    - 説明
      - React向けライブラリ
    - 使用しない理由
      1.  上と同じくAdSense枠を置かないため、埋め込み用のラッパーも導入しない
- 検索エンジンの分析
  - Google Search Console
    - 参考
      - [https://search.google.com/search-console/about](https://search.google.com/search-console/about)
    - 説明
      - 検索エンジンの分析
      - どんな検索ワードでサイトが訪問されたのか、googleにindex(検索結果にでるように認識されていること)されているか、されていない場合はリクエストしたり等々
      - どちらも無料で使用できる
    - 使用しない理由
      1.  本質的に必要な機能ではないため

- DDoS攻撃の対策
  - 説明
    - DDoS攻撃の対策としてCloudFlareやAWSのCloudFrontなどの負荷分散機能付きのCDNサービスを上段に入れると良いらしい
      - Cloudflareの無料枠で十分
  - 使用しない理由
    1. DDoS攻撃は必要そうになってから入れる

- CDN
  - 一旦使用しない

- ページのスピード
  - `PageSpeed Insights`
    - 参考
      - [https://developers.google.com/speed/pagespeed/insights/](https://developers.google.com/speed/pagespeed/insights/)
    - 説明
      - 公開URLを入力し、LCP等の指標と改善のヒントを出す、Googleの無料パフォーマンス診断ツール
    - 使用しない理由
      1.  継続監視用のSaaS連携やCIの閾値管理までは導入せず、必要になったタイミングで手動診断に留めるため
- サービスを紹介できるサービス
  - 説明
    - 自分が作成したWebサービスを紹介できるサービスがある
  - 参考
    - [https://www.service-safari.com/](https://www.service-safari.com/)
    - [http://startapp.jp/](http://startapp.jp/)
    - [https://applishow.com/](https://applishow.com/)
    - [http://creators.eightbit.jp/](http://creators.eightbit.jp/)
    - [http://webatume.net](http://webatume.net/)
    - [http://inajob.dip.jp/tsukutter/](http://inajob.dip.jp/tsukutter/)
    - [https://seekups.seekgeeks.net/](https://seekups.seekgeeks.net/)
    - [https://www.eggineer.com/](https://www.eggineer.com/)
