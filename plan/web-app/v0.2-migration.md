# Web App v0.2 Cross-App Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 未リリースの旧 `projects/web-app` を後方互換・データ移行なしで廃止し、`points.freeism.app` と `markets.freeism.app` を独立した Cloudflare Workers アプリとして構築し、`main` push だけで staging 検証から production まで直列デプロイできる状態へ移行する。

**Architecture:** `projects/points-web-app` と `projects/markets-web-app` は、それぞれ TanStack Start の静的 SPA shell + 4固定route prerender、Hono Worker、Workers Static Assets、D1 を持つ独立アプリとする。Points–Markets 間は Service Binding を transport に使うが、OAuth bearer token を必須とし、Points 所有の OpenAPI から生成した型/Client だけを契約境界にする。Worker/binding/named environment/custom-domain route は Wrangler/Vite plugin、DNS/apex redirect/Access/WAF/rate-limit は Terraform が所有し、同じ Cloudflare resource を二重管理しない。

**Tech Stack:** Node `24.18.0`、pnpm `10.33.3`、Vite Plus `0.2.4`、TanStack Start `1.168.27`、React `19.2.7`、Hono `4.12.28`、Drizzle ORM `0.45.2`、D1、Better Auth、Workers Static Assets、Durable Objects、Workflows、OpenAPI、Terraform `1.15.7` / Cloudflare Provider `5.21.1`、GitHub Actions。

---

## 0. この計画の範囲

### この計画が実装するもの

- repository root の Node/pnpm/Vite+ toolchain と supply-chain policy
- Points/Markets の greenfield shell、Hono health API、明示的な SPA shell routing
- Points 所有の Points–Markets OpenAPI と生成物差分検査
- Cloudflare resource の所有境界、Terraform edge IaC、環境 inventory
- PR/merge queue CI、main ruleset 用 declarative payload、`main` push staging → production workflow
- release/rollback/Time Travel/cutover/decommission runbook と受入証跡
- 旧 `projects/web-app`、旧 Vercel/Supabase/Upstash workflow/config/runtime の repository cutover
- Cloudflare 受入後の Vercel/Supabase/Upstash/旧画像 R2 の別 checkpoint 撤去

### 子計画へ委譲し、この計画へ重複記載しないもの

- Points の認証、評価軸、FIX、台帳、未受領 FIX、所有権、CSV、D1 schema/API/UI:
  [`projects/points-web-app/plan/v0.2-implementation.md`](../../projects/points-web-app/plan/v0.2-implementation.md)
- Markets の認証、Auction、bid/AutoBid、AuctionRoom、Workflow、settlement/proof/UI:
  [`projects/markets-web-app/plan/v0.2-implementation.md`](../../projects/markets-web-app/plan/v0.2-implementation.md)

子計画は本計画の shell、OpenAPI、package-script interface を消費する。子計画が本計画と異なる cross-app contract を必要とした場合は、実装で吸収せず、先に canonical docs と本計画を同じ PR で更新する。

## 1. Canonical input と優先順位

実装開始前に次をすべて読む。旧 v0.1、archive、旧 `response-format.md` 内の現行観測が衝突する場合は、以下の後発 v0.2 正本を優先する。

1. [`docs/web-app/v0.2/decision-register.md`](../../docs/web-app/v0.2/decision-register.md)
2. [`docs/web-app/v0.2/architecture.md`](../../docs/web-app/v0.2/architecture.md)
3. [`docs/web-app/v0.2/authentication.md`](../../docs/web-app/v0.2/authentication.md)
4. [`docs/web-app/v0.2/points-markets-contract.md`](../../docs/web-app/v0.2/points-markets-contract.md)
5. [`docs/web-app/v0.2/security-and-delivery.md`](../../docs/web-app/v0.2/security-and-delivery.md)
6. Points/Markets 各 `docs/v0.2/`

Decision Registerで`承認対象`の行は、仕様案としてdocsに保持しても実装入力にはしない。該当DECが利用者の承認により`採用`へ変わるまで、その値に依存する子Taskだけを開始せず、別の値を実装者判断で補わない。

DEC-243、DEC-252、DEC-254、DEC-256〜DEC-264は利用者承認により`採用`済みであり、その確定値を子計画の実装入力とする。今後新しい`承認対象`を追加した場合は、個別列挙がなくても上記の一般規則を適用する。

この計画では次を不変条件として扱う。

- 後方互換、旧 URL/API/schema/session fallback、旧データ移行を実装しない。
- Task/Group/一般 member/notification/PWA/image upload/Next.js/Auth.js/Prisma/Supabase/Upstash/SSE を新実装へ持ち込まない。
- Points と Markets は別 Better Auth、別 host-only Cookie、別 D1、別 user ID、別 session を持つ。
- Marketsは独立したListing resourceを持たず、商材fieldとAuction条件を単一のAuction／不変Auction revisionへ統合する。旧Listing ID、schema、route、alias、redirectを作らない。
- Points の Google/GitHub は login と explicit link の双方に同じ Provider 集合を使う。Provider 単位の link-only 独自 hook は作らない。
- Better Auth のメール一致 implicit link は禁止し、本人は `providerId + accountId` で識別する。
- Service Binding は認可根拠ではない。Points Resource APIとMarkets BFFはBetter Auth標準Resource Clientのconfidential remote introspectionを使い、scope／audienceと、期待する利用者用またはM2M用Client ID、利用者`sub`有無から導出したprincipal classを常に検証する。credentialはWorker Secretだけに置いてbrowserへ出さず、独自token-class claimを追加しない。
- Points OAuth Providerは`disableJwtPlugin: true`とし、利用者／Client Credentials Access Tokenをopaqueにする。環境ごとにMarkets利用者用、M2M用、Settlement retry用Client IDを分け、各Clientのgrant／scopeを限定する。MarketsとPoints Resource APIはBetter Auth標準RFC 7662 remote introspectionを使い、利用者Tokenの`active`、pairwise `sub`、client、audience/resource、scope、期限を保存済み連携と一致させる。Access TokenをJWTと仮定してlocal decode／JWKS検証せず、ID TokenをResource API Bearerとして受理しない（DEC-089、DEC-232、DEC-263）。
- opaque Client Credentials TokenはMarkets Worker自身を表すM2M専用とし、利用者`sub`、残高参照、新規予約、通常unlink、Settlement管理権限を持たせない（DEC-263）。
- opaque user token、標準introspection、pairwise subject、grant/scope分離のいずれかが標準 Better Auth OAuth Provider で成立しない場合は、独自Provider／独自introspection／独自Token形式を実装せずreleaseを停止して再承認を求める。
- 即時購入は指定数量を1 commandで全量lockし、`BUY_NOW` settlementを直ちに開始する。restoreは外部作用前、決定的reservation拒否でID 0件、または全reservation未capture＋ACTIVE分release完了の3証拠だけで許可し、結果不明はholdを維持してmanual actionとする。capture後はproofへforward finalizeする。endAt時の未終端holdは終了時planを遅延し、全hold終端後に復元済み残数で収束する（DEC-264）。
- runtime SSR、TanStack server functions、Next Server Actions を使わない。
- SPA shell は `/index.html`。Cloudflare の汎用 `not_found_handling: "single-page-application"` は使わない。
- `/` は shell hydrate後に描画するSPA topで、top本体のSSGとは扱わない。SSGは`/terms`、`/privacy`、`/help`、`/docs`だけを`*.html`へ明示生成し、自動discovery／crawlを無効にする。
- 4固定routeはlocale別URL／query／HTMLへ分けず、各HTMLに日本語正本と英語参照訳の双方を静的renderする。JavaScript無効時は両言語を読め、JavaScript有効時は保存済み選択→browser言語→日本語の順で表示言語を決める。
- 日本語正本を機械翻訳または英語側から自動更新しない。英語参照訳はproduction release前のbilingual reviewとlocale別content hash一致を必須gateにする。
- Static Assets は asset-first、`not_found_handling: "none"`、`html_handling: "auto-trailing-slash"`。`assets_navigation_has_no_effect`を明示し、asset miss 後に Worker が GET/HEAD の HTML navigation だけ Asset Bindingのcanonical `/`からshellを返す。存在しない API/JS/CSS/image は 404 にする。
- `CLOUDFLARE_ENV=staging|production` は build 時に選択し、生成された flattened Wrangler config を直後に deploy する。build 済み artifact を `wrangler deploy --env` だけで切り替えない。
- `main` push 以外に production trigger、`workflow_dispatch`、手動 environment approval を置かない。
- `main`の直接更新は行わず、docs、実装、正式版bump、production後evidenceのすべてをbranch／PR／merge queue経由で反映する。
- 状態変更 workflow は `cancel-in-progress: false` で直列化する。
- Better Auth `1.7.0-rc.1` は開発/staging 限定。最初の production は Better Auth `1.7.0` 正式版への exact bump と全認証回帰成功まで進めない。
- pnpm `minimumReleaseAge: 4320` はroot／Points／Marketsの全installで維持する。dependency追加後の標準 `pnpm install --no-frozen-lockfile` が公開後72時間のcooldownを適用し、拒否された場合は設定で除外せず時間経過後に再実行する。

## 2. Exact version lock

直接依存に `^`、`~`、`latest`、Git URL、tarball URLを使わない。次を initial implementation lock とする。

| Tool/package                      | Exact version | Use                                                            |
| --------------------------------- | ------------: | -------------------------------------------------------------- |
| Node                              |     `24.18.0` | local/CI runtime。root `.node-version` と engines を一致させる |
| pnpm                              |     `10.33.3` | root package manager                                           |
| Vite Plus                         |       `0.2.4` | dev/build/check/test task runner                               |
| Vite                              |       `8.1.4` | Vite plugin peer/runtime                                       |
| Vitest                            |      `4.1.10` | Vite+ 同梱版と Cloudflare pool peer を一致                     |
| `@tanstack/react-start`           |    `1.168.27` | SPA/build-time prerender                                       |
| `@tanstack/react-router`          |    `1.170.17` | Start `1.168.27` の direct dependency と一致                   |
| `@tanstack/react-query`           |     `5.101.2` | memory-only server state                                       |
| React / React DOM                 |      `19.2.7` | UI runtime                                                     |
| `@types/react`                    |     `19.2.17` | React TypeScript declarations                                  |
| `@types/react-dom`                |      `19.2.3` | React DOM TypeScript declarations                              |
| `@types/node`                     |     `24.13.3` | Node 24 declaration line                                       |
| `@vitejs/plugin-react`            |       `6.0.3` | Vite React plugin                                              |
| Hono                              |     `4.12.28` | same-origin Worker/BFF                                         |
| Drizzle ORM                       |      `0.45.2` | D1 ORM。子計画で導入                                           |
| Drizzle Kit                       |     `0.31.10` | D1 migration generation。子計画で導入                          |
| Wrangler                          |     `4.108.0` | Worker/D1/DO/Workflow deploy                                   |
| `@cloudflare/vite-plugin`         |      `1.43.2` | Workers build/static assets                                    |
| `@cloudflare/vitest-pool-workers` |      `0.18.2` | Workers integration tests                                      |
| Better Auth runtime packages      |  `1.7.0-rc.1` | development/staging only                                       |
| Better Auth runtime packages      |       `1.7.0` | first production target; publish + regression completion gate  |
| standalone Better Auth CLI `auth` |  `1.7.0-rc.1` | schema generation in development/staging                       |
| standalone Better Auth CLI `auth` |       `1.7.0` | first production schema regeneration target                    |
| `openapi-typescript`              |      `7.13.0` | canonical OpenAPI type generation                              |
| TypeScript                        |       `6.0.3` | Vite Plus `0.2.4` catalog (`^6.0.0`)に合わせる                 |
| `tsx`                             |      `4.23.0` | TypeScript verification scripts                                |
| Happy DOM                         |     `20.10.6` | browserless React unit tests only                              |
| `@playwright/test`                |      `1.61.1` | browser E2E                                                    |
| Terraform                         |      `1.15.7` | Cloudflare edge IaC CLI                                        |
| Cloudflare Terraform Provider     |      `5.21.1` | DNS/redirect/Access/WAF/rate-limit                             |

両appは`better-auth`、`@better-auth/drizzle-adapter`とstandalone CLI `auth`を同一exact versionで宣言する。OAuth Providerを所有するPointsだけが`@better-auth/oauth-provider`を宣言し、Marketsは未使用のProvider packageを追加しない。Drizzle利用時のruntime／CLI auth instanceは公式の`better-auth/minimal` entrypointを使い、未使用のKysely adapterをWorker bundleへ含めない。runtime factoryとauth optionsを共有するCLI専用の具体auth export `src/backend/auth/auth-cli.ts`を各appに用意する。共通options builderはdatabaseを任意注入にし、runtimeだけが実D1のDrizzle adapterを渡す。CLI exportはschema生成専用のdummy origin／Provider credential／versioned secretをfile内に持ち、databaseを渡さない。`auth generate --adapter drizzle --dialect sqlite`がCLI標準mock adapterを使用するため、CLI評価時にD1 binding、生成前schema、Worker Secret、networkを要求しない。schema生成は各appのexact devDependencyから`pnpm --filter <app> exec auth generate --config ./src/backend/auth/auth-cli.ts --adapter drizzle --dialect sqlite --yes ...`を実行し、CLI dummy configをruntimeへimportせず、factory-only module、偶然のglobal binary、`npx`、`better-auth generate`へfallbackしない。

GitHub Actions は tag ではなく次の full commit SHA を使う。

| Action                                   | Full SHA                                   |
| ---------------------------------------- | ------------------------------------------ |
| `actions/checkout` v6                    | `df4cb1c069e1874edd31b4311f1884172cec0e10` |
| `voidzero-dev/setup-vp` v1 target commit | `250f29ce396baf5e8f24498e17c0dfdebabc26eb` |
| `hashicorp/setup-terraform` v3           | `b9cd54a3c349d3f38e8881555d616ced269862dd` |

## 3. Cross-app interfaces

### 3.1 Workspace/package interface

両 app の package 名は次で固定する。

- `@freeism/points-web-app`
- `@freeism/markets-web-app`

子計画完了時に両 package は次の script 名を同じ意味で提供する。

| Script                  | Contract                                                          |
| ----------------------- | ----------------------------------------------------------------- |
| `check`                 | format/lint/typecheck を副作用なしで実行                          |
| `test`                  | React/domain unit/property test                                   |
| `test:worker`           | Workers Vitest integration                                        |
| `test:e2e`              | Playwright browser E2E。固定ページのJS無効／keyboard／ARIAも含む  |
| `build:staging`         | `CLOUDFLARE_ENV=staging` を build 前に設定                        |
| `build:production`      | `CLOUDFLARE_ENV=production` を build 前に設定                     |
| `build:assert`          | flattened config、environment、shellと4固定HTMLを検証             |
| `db:migrate:staging`    | named staging D1 に forward migration を適用                      |
| `db:migrate:production` | bookmark 記録後に production D1 migration を適用                  |
| `deploy:staging`        | staging build artifact の直後に generated config を deploy        |
| `deploy:production`     | production build artifact の直後に generated config を deploy     |
| `smoke:staging`         | custom domain/TLS/assets/API/auth/reconciliation の staging smoke |
| `smoke:production`      | read-only/低リスクの production smoke                             |

`deploy:*` は別 environment の既存 build artifact を再利用しない。D1/DO/Workflow 固有処理は子計画で実装する。

### 3.2 Worker/static interface

- logical Worker base: `points-worker` / `auction-worker`
- named environment deployment: `*-staging` / `*-production`
- domain: `staging.points.freeism.app`、`staging.markets.freeism.app`、`points.freeism.app`、`markets.freeism.app`
- Worker first paths: `/api/*`、`/.well-known/*`
- Static Assets binding: `ASSETS: Fetcher`
- SPA shell: `/index.html`
- fixed prerender assets: `/terms.html`、`/privacy.html`、`/help.html`、`/docs.html`
- fixed-page locale contract: 4 HTMLそれぞれが`lang="ja"`と`lang="en"`の全contentを含み、localeによるpath／query／cache key分岐を持たない
- API health: `GET /api/health`
- API 404: RFC 9457 Problem Details JSON。HTML を返さない

### 3.3 Points–Markets contract interface

Canonical OpenAPI は `docs/web-app/v0.2/points-markets.openapi.json` とし、operationId を次で固定する。

| Method/path                                                      | operationId                           | Token class |
| ---------------------------------------------------------------- | ------------------------------------- | ----------- |
| `GET /api/v1/point-package-revisions/{pointPackageRevisionId}`   | `getPublicPointPackageRevision`       | public      |
| `POST /api/v1/point-package-auction-eligibility-checks`          | `checkPointPackageAuctionEligibility` | M2M         |
| `POST /api/v1/oauth/link-attempts`                               | `createPointsLinkAttempt`             | M2M         |
| `POST /api/v1/oauth/link-attempts/{linkAttemptId}/finalizations` | `finalizePointsLinkAttempt`           | M2M         |
| `GET /api/v1/me/connection`                                      | `getPointsConnection`                 | user        |
| `POST /api/v1/me/connection-deactivations`                       | `deactivatePointsConnection`          | unlink-only |
| `POST /api/v1/me/balance-checks`                                 | `checkPointBalance`                   | user        |
| `POST /api/v1/me/point-reservations`                             | `createPointReservation`              | user        |
| `POST /api/v1/point-reservations/status`                         | `getPointReservationStatus`           | M2M         |
| `POST /api/v1/settlements/{settlementId}/capture`                | `capturePointSettlement`              | M2M         |
| `POST /api/v1/point-reservations/release`                        | `releasePointReservation`             | M2M         |

生成先:

- `projects/points-web-app/src/generated/points-markets-api.ts`
- `projects/markets-web-app/src/generated/points-markets-api.ts`

両 app は相手の source/Hono RPC/DB schema を import しない。Markets の typed client 実装は Markets 子計画が生成 `paths`/`operations` を使って作る。

### 3.4 Environment/binding ownership

| Resource                                                                                                          | Owner                                                                 |
| ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Worker、Static Assets、D1 binding、DO migration/namespace、Workflow binding、Service Binding、custom-domain route | app ごとの `wrangler.jsonc` + Vite plugin                             |
| apex/www DNS、apex/www redirect、staging Access、managed/custom WAF、edge IP rate-limit、Turnstile widget         | Terraform                                                             |
| app/user/resource 単位の正確な rate-limit、idempotency、authorization                                             | Hono/D1/DO。子計画                                                    |
| OAuth provider/client secret、Better Auth versioned secrets                                                       | environment 別 Worker Secret。repository/Terraform state に保存しない |
| Terraform state                                                                                                   | 新規専用 R2 bucket。旧画像 R2 と別 resource                           |

## 4. File map

### Create

- `.node-version`
- `.npmrc`
- `scripts/web-app/assert-worker-build.mjs`
- `scripts/web-app/generate-static-security-headers.mjs`
- `scripts/web-app/check-staged.mjs`
- `scripts/web-app/bootstrap-terraform-state.mjs`
- `scripts/web-app/assert-production-release.mjs`
- `scripts/web-app/assert-legacy-removed.mjs`
- `tests/web-app/toolchain.test.mjs`
- `tests/web-app/spec-boundaries.test.mjs`
- `tests/web-app/openapi-contract.test.mjs`
- `tests/web-app/release-gates.test.mjs`
- `docs/web-app/v0.2/points-markets.openapi.json`
- `docs/web-app/v0.2/static-pages/translation-review.json`
- `projects/points-web-app/package.json`
- `projects/points-web-app/README.md`
- `projects/points-web-app/tsconfig.json`
- `projects/points-web-app/vite.config.ts`
- `projects/points-web-app/vitest.config.ts`
- `projects/points-web-app/vitest.worker.config.ts`
- `projects/points-web-app/wrangler.jsonc`
- `projects/points-web-app/worker-configuration.d.ts`
- `projects/points-web-app/worker/index.ts`
- `projects/points-web-app/worker/spa-fallback.ts`
- `projects/points-web-app/worker/index.worker.test.ts`
- `projects/points-web-app/src/router.tsx`
- `projects/points-web-app/src/routes/__root.tsx`
- `projects/points-web-app/src/routes/index.tsx`
- `projects/points-web-app/src/routes/index.test.tsx`
- `projects/points-web-app/src/routes/terms.tsx`
- `projects/points-web-app/src/routes/privacy.tsx`
- `projects/points-web-app/src/routes/help.tsx`
- `projects/points-web-app/src/routes/docs.tsx`
- `projects/points-web-app/src/styles.css`
- `projects/points-web-app/src/generated/points-markets-api.ts`
- `projects/points-web-app/public/favicon.svg`
- `projects/points-web-app/public/_headers`
- `projects/points-web-app/.dev.vars.example`
- `projects/markets-web-app/package.json`
- `projects/markets-web-app/README.md`
- `projects/markets-web-app/tsconfig.json`
- `projects/markets-web-app/vite.config.ts`
- `projects/markets-web-app/vitest.config.ts`
- `projects/markets-web-app/vitest.worker.config.ts`
- `projects/markets-web-app/wrangler.jsonc`
- `projects/markets-web-app/worker-configuration.d.ts`
- `projects/markets-web-app/worker/index.ts`
- `projects/markets-web-app/worker/spa-fallback.ts`
- `projects/markets-web-app/worker/index.worker.test.ts`
- `projects/markets-web-app/src/router.tsx`
- `projects/markets-web-app/src/routes/__root.tsx`
- `projects/markets-web-app/src/routes/index.tsx`
- `projects/markets-web-app/src/routes/index.test.tsx`
- `projects/markets-web-app/src/routes/terms.tsx`
- `projects/markets-web-app/src/routes/privacy.tsx`
- `projects/markets-web-app/src/routes/help.tsx`
- `projects/markets-web-app/src/routes/docs.tsx`
- `projects/markets-web-app/src/styles.css`
- `projects/markets-web-app/src/generated/points-markets-api.ts`
- `projects/markets-web-app/public/favicon.svg`
- `projects/markets-web-app/public/_headers`
- `projects/markets-web-app/.dev.vars.example`
- `infra/cloudflare/modules/web-app-edge/versions.tf`
- `infra/cloudflare/modules/web-app-edge/variables.tf`
- `infra/cloudflare/modules/web-app-edge/main.tf`
- `infra/cloudflare/modules/web-app-edge/outputs.tf`
- `infra/cloudflare/modules/web-app-edge/tests/edge.tftest.hcl`
- `infra/cloudflare/backend.tf`
- `infra/cloudflare/.terraform.lock.hcl`
- `infra/cloudflare/versions.tf`
- `infra/cloudflare/providers.tf`
- `infra/cloudflare/variables.tf`
- `infra/cloudflare/main.tf`
- `infra/cloudflare/outputs.tf`
- `infra/cloudflare/environments/staging.tfvars.example`
- `infra/cloudflare/environments/production.tfvars.example`
- `infra/github/main-ruleset.json`
- `.github/workflows/web-app/deploy.yml`
- `plan/web-app/runbooks/cloudflare-bootstrap.md`
- `plan/web-app/runbooks/deployment-and-rollback.md`
- `plan/web-app/runbooks/legacy-decommission.md`
- `plan/web-app/runbooks/observability-and-alerts.md`
- `plan/web-app/evidence/cloudflare-bootstrap-staging.md`
- `plan/web-app/evidence/staging-acceptance.md`
- `plan/web-app/evidence/production-acceptance.md`
- `plan/web-app/evidence/decommission-v0.2.md`

### Modify

- `package.json`
- `pnpm-workspace.yaml`
- `pnpm-lock.yaml`
- `.gitignore`
- `.husky/pre-commit`
- `.vscode/settings.json`
- `README.md`
- `docs/README.ja.md`
- `docs/web-app/README.md`
- `docs/web-app/doc-migration-manifest.md`
- `docs/web-app/v0.2/static-pages/README.md`
- `docs/web-app/v0.2/static-pages/terms.ja.md`
- `docs/web-app/v0.2/static-pages/terms.en.md`
- `docs/web-app/v0.2/static-pages/privacy.ja.md`
- `docs/web-app/v0.2/static-pages/privacy.en.md`
- `docs/web-app/v0.2/static-pages/help.ja.md`
- `docs/web-app/v0.2/static-pages/help.en.md`
- `docs/web-app/v0.2/static-pages/docs.ja.md`
- `docs/web-app/v0.2/static-pages/docs.en.md`
- `.github/CODEOWNERS`
- `.github/ISSUE_TEMPLATE/01-bug-web-app.md`
- `.github/ISSUE_TEMPLATE/06-feature-request.md`
- `.github/PULL_REQUEST_TEMPLATE/web-app.md`
- `.github/workflows/web-app/ci.yml`

### Delete at repository cutover

- `projects/web-app/`
- `mise.toml`
- `.github/actions/web-app-setup/`
- `.github/workflows/web-app/production-return-auction-deposit-points.yml`
- `.github/workflows/web-app/production-scheduled-notifications.yml`
- `.github/workflows/web-app/production-update-auction-status-to-active.yml`
- `.github/workflows/web-app/production-update-auction-status-to-completed.yml`
- `.github/workflows/web-app/test-return-auction-deposit-points.yml`
- `.github/workflows/web-app/test-scheduled-notifications.yml`
- `.github/workflows/web-app/test-update-auction-status-to-active.yml`
- `.github/workflows/web-app/test-update-auction-status-to-completed.yml`
- `.github/workflows/web-app/vercel-preview-deploy.yml`
- `.github/workflows/web-app/vercel-production-deploy.yml`

---

## Task 0: Preflight と実装 branch の停止条件

**Files:** 承認済みdocs／plan差分だけを最初の独立commitにする。実装fileは変更しない。

- [ ] **Step 1: 隔離worktree／branchを固定し、`main`なら停止する**

```bash
test "$PWD" = "/private/tmp/freeism-v0.2"
test "$(git branch --show-current)" = "codex/web-app-v0.2-implementation"
test "$(git branch --show-current)" != "main"
git worktree list --porcelain
```

Expected: 4 commandすべてexit `0`で、現在pathとbranchが一致する。別checkout、detached HEAD、`main`では実装を開始せず、利用者が指定した隔離worktree／branchへ戻る。`main`を直接更新しない。

- [ ] **Step 2: canonical docs と子計画の存在を検査する**

```bash
test -f docs/web-app/v0.2/decision-register.md
test -f docs/web-app/v0.2/architecture.md
test -f docs/web-app/v0.2/authentication.md
test -f docs/web-app/v0.2/points-markets-contract.md
test -f docs/web-app/v0.2/security-and-delivery.md
test -f projects/points-web-app/plan/v0.2-implementation.md
test -f projects/markets-web-app/plan/v0.2-implementation.md
```

Expected: 全 command が exit `0`。子計画が未作成ならここで停止し、当該子計画を先に作成する。

- [ ] **Step 3: dirty worktree の所有権を確認する**

```bash
git status --short --branch
git diff --name-status
git diff --cached --name-status
```

Expected: 既存の docs 移動・利用者変更を識別できる。無関係な変更を stash/reset/checkout しない。

- [ ] **Step 4: 承認済みdocs／planだけを独立commitする**

現在の承認済み差分を次の明示pathだけでstageする。directory全体、`git add -A`、`git add .`は使わない。

```bash
git add -- docs/web-app/README.md docs/web-app/v0.2/architecture.md docs/web-app/v0.2/authentication.md docs/web-app/v0.2/decision-register.md docs/web-app/v0.2/naming-convention.md docs/web-app/v0.2/points-markets-contract.md docs/web-app/v0.2/security-and-delivery.md docs/web-app/v0.2/static-pages/help.en.md docs/web-app/v0.2/static-pages/help.ja.md plan/web-app/v0.2-migration.md projects/documentation/src/freeism.en.md projects/documentation/src/freeism.ja.md projects/markets-web-app/docs/readme/README.ja.md projects/markets-web-app/docs/v0.2/details-ja/auction.md projects/markets-web-app/docs/v0.2/details-ja/markets-domain.md projects/markets-web-app/docs/v0.2/details-ja/realtime-and-settlement.md projects/markets-web-app/docs/v0.2/index.ja.md projects/markets-web-app/plan/v0.2-implementation.md projects/points-web-app/docs/readme/README.ja.md projects/points-web-app/docs/v0.2/details-ja/csv-export.md projects/points-web-app/docs/v0.2/details-ja/evaluation-criteria-management.md projects/points-web-app/docs/v0.2/details-ja/points-domain.md projects/points-web-app/docs/v0.2/details-ja/profile-setting.md projects/points-web-app/docs/v0.2/index.ja.md projects/points-web-app/plan/v0.2-implementation.md
git diff --cached --name-status
git diff --cached --check
git commit -m "docs(web-app): approve v0.2 migration contract"
```

Expected: staged setが上記docs／planだけ、diff check PASS、実装file 0件で独立commitになる。未承認pathが混ざる、または上記以外の承認済みfileが存在する場合はcommitせずstage対象を利用者と再確認する。

- [ ] **Step 5: 以後のstageを各TaskのFile mapへ限定する**

各実装commitの直前に、そのTaskの`Files`とFile mapに列挙した明示pathだけを`git add -- ...`し、次でstaged setを照合する。

```bash
git diff --cached --name-status
git diff --cached --check
```

Expected: 対象Task外のdocs、plan、別project、利用者変更が0件。混入時はcommitせず、対象pathだけをstageし直す。stash/reset/checkoutで利用者変更を消さない。

- [ ] **Step 6: 現runtimeをexact guardし、相違時は実装前に停止する**

```bash
command -v node
command -v pnpm
test "$(node --version)" = "v24.18.0"
test "$(pnpm --version)" = "10.33.3"
```

Expected: 全command exit `0`。Nodeまたはpnpmが不在／相違する場合はここで停止する。agentは`corepack enable`／`corepack prepare`、`npm install -g`、Homebrew、`sudo`等でglobal runtimeを変更せず、利用者が承認した既存version manager／provisioningでexact runtimeが用意された後に同じguardから再開する。

- [ ] **Step 7: registry のexact versionをread-only確認する**

```bash
env npm_config_cache=/private/tmp/freeism-npm-cache pnpm view vite-plus@0.2.4 version
env npm_config_cache=/private/tmp/freeism-npm-cache pnpm view @tanstack/react-start@1.168.27 version
env npm_config_cache=/private/tmp/freeism-npm-cache pnpm view wrangler@4.108.0 version
```

Expected: 指定3 package のexact version。releaseが消失／deprecatedの場合は依存を変更せず停止する。72時間cooldownはTask 1でrootの `minimumReleaseAge: 4320` を設定し、最初の標準installから適用する。

## Task 1: Root toolchain と pnpm標準policy

**Files:**

- Create: `.node-version`, `.npmrc`, `tests/web-app/toolchain.test.mjs`
- Modify: `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `plan/web-app/v0.2-migration.md`

- [ ] **Step 1: failing toolchain test を作る**

`tests/web-app/toolchain.test.mjs` は最低限次を検査する。

- `.node-version === "24.18.0"`
- root `package.json.packageManager === "pnpm@10.33.3"`
- root engines が Node `24.18.0` / pnpm `10.33.3` の exact
- root `devDependencies` が `openapi-typescript@7.13.0`、`tsx@4.23.0`、`typescript@6.0.3`、`vite-plus@0.2.4` の4件だけ
- root `package.json` に `pnpm` blockがない
- `.npmrc` が `engine-strict=true` と `save-exact=true` の2行だけ
- `pnpm-workspace.yaml` が `packages`、`minimumReleaseAge: 4320`、`blockExoticSubdeps: true`、`onlyBuiltDependencies` の最小全文と一致する
- 独自license／audit／release-age実装4ファイルが存在しない

完成形が小さいため、YAML libraryや手書きの汎用YAML parserは使わず、改行を正規化した実ファイル全文一致で検査する。

```bash
env MISE_IGNORED_CONFIG_PATHS=/private/tmp/freeism-v0.2 mise x node@24.18.0 -- node --test tests/web-app/toolchain.test.mjs
```

Expected: FAIL。余分なroot依存、root `pnpm` block、peer設定、独自filesのいずれかを具体的に指して失敗する。

- [ ] **Step 2: root version/pnpm policy を最小実装する**

`.node-version`:

```text
24.18.0
```

`.npmrc`:

```ini
engine-strict=true
save-exact=true
```

`pnpm-workspace.yaml` は次の全文だけにする。

```yaml
packages:
  - projects/*
minimumReleaseAge: 4320
blockExoticSubdeps: true
onlyBuiltDependencies:
  - esbuild
  - workerd
```

root `package.json` は既存 documentation 等の script を保持し、次を追加/更新する。

```json
{
  "packageManager": "pnpm@10.33.3",
  "engines": {
    "node": "24.18.0",
    "pnpm": "10.33.3"
  },
  "devDependencies": {
    "openapi-typescript": "7.13.0",
    "tsx": "4.23.0",
    "typescript": "6.0.3",
    "vite-plus": "0.2.4"
  }
}
```

既存scriptsは保持する。rootに`vite`、`happy-dom`、`ws`を置かず、必要なPoints／Markets packageのshell Taskでだけ宣言する。root `pnpm` blockや例外設定は作らず、旧appの一時的なpeer contextによるlockfile差分は標準pnpmの解決結果として受け入れる。

- [ ] **Step 3: 固定Nodeでtoolchain testをGREENにする**

```bash
env MISE_IGNORED_CONFIG_PATHS=/private/tmp/freeism-v0.2 mise x node@24.18.0 -- node --test tests/web-app/toolchain.test.mjs
```

Expected: 全test PASS、warning/errorなし。

- [ ] **Step 4: pnpm標準の72時間cooldownでinstallする**

rootの`minimumReleaseAge: 4320`を使う標準installを固定Node／pnpmで実行する。公開後72時間未満のdependencyで拒否された場合は設定で除外せず、時間経過後に同じinstallを再実行する。

```bash
env npm_config_cache=/private/tmp/freeism-npm-cache COREPACK_HOME=/private/tmp/freeism-corepack MISE_IGNORED_CONFIG_PATHS=/private/tmp/freeism-v0.2 mise x node@24.18.0 -- corepack pnpm@10.33.3 --filter freeism install --no-frozen-lockfile
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
env npm_config_cache=/private/tmp/freeism-npm-cache COREPACK_HOME=/private/tmp/freeism-corepack MISE_IGNORED_CONFIG_PATHS=/private/tmp/freeism-v0.2 mise x node@24.18.0 -- corepack pnpm@10.33.3 --filter freeism install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
env npm_config_cache=/private/tmp/freeism-npm-cache COREPACK_HOME=/private/tmp/freeism-corepack MISE_IGNORED_CONFIG_PATHS=/private/tmp/freeism-v0.2 mise x node@24.18.0 -- corepack pnpm@10.33.3 ignored-builds
```

Expected: 標準installでlockfileが更新され、frozen再install後もSHA-256が不変。`pnpm ignored-builds`に`msw`等が表示されてもblockedのまま維持し、実行が必要なdependencyだけ個別reviewする。`onlyBuiltDependencies`は`esbuild`／`workerd`から広げない。

- [ ] **Step 5: 最終検証を行う**

```bash
env MISE_IGNORED_CONFIG_PATHS=/private/tmp/freeism-v0.2 mise x node@24.18.0 -- node --test tests/web-app/toolchain.test.mjs
git diff --check
git status --short
```

先行実装、peer既定値上書き、advisory例外の残存参照は用途別に全文検索する。test内のnegative assertion文字列は必要な契約として許容し、plan内の独自script実行／正当化とroot設定内の禁止keyが0件であることを確認する。

Expected: 全test PASS、`git diff --check`成功、禁止された実装／設定の残存0件。変更scopeがTask 1だけであることを確認する。

- [ ] **Step 6: commit**

```bash
git add -- .node-version .npmrc package.json pnpm-workspace.yaml pnpm-lock.yaml plan/web-app/v0.2-migration.md tests/web-app/toolchain.test.mjs
git commit -m "build(web-app): pin v0.2 toolchain"
```

削除対象4ファイルは未追跡の先行実装なのでcommitへ含めず、空になった`config/`と`scripts/web-app/`も残さない。

## Task 2: Points greenfield shell と Static Assets routing spike

**Files:** Points shell の File map 記載 create files、`.gitignore`、`pnpm-lock.yaml`。

既存 `projects/points-web-app/docs` と `plan/archive` を保持する。scaffolder で directory 全体を上書きしない。

このgreenfield directoryでは`vp migrate`／`vp create`を実行せず、manifestとVite Plus／TanStack Start／Cloudflare設定を直接作る。app code/configは`defineConfig`を`vite-plus`、test APIを`vite-plus/test`からimportし、peer providerとして`vite@8.1.4`と`vitest@4.1.10`もexact宣言する。`src/routeTree.gen.ts`はRouter plugin生成artifactを初回commitし、手編集しない。`src/generated/points-markets-api.ts`の初回生成はTask 4だけが所有する。

Task 2のdirect dependencyは`@tanstack/react-query@5.101.2`、`@tanstack/react-router@1.170.17`、`@tanstack/react-start@1.168.27`、`hono@4.12.28`、`react@19.2.7`、`react-dom@19.2.7`、`react-markdown@10.1.0`、devDependencyは`@cloudflare/vite-plugin@1.43.2`、`@cloudflare/vitest-pool-workers@0.18.2`、`@playwright/test@1.61.1`、`@types/node@24.13.3`、`@types/react@19.2.17`、`@types/react-dom@19.2.3`、`@vitejs/plugin-react@6.0.3`、`happy-dom@20.10.6`、`typescript@6.0.3`、`vite@8.1.4`、`vite-plus@0.2.4`、`vitest@4.1.10`、`wrangler@4.108.0`だけをexact宣言する。Vite plugin `1.43.2`は利用者が明示承認し、残る3点は同じcooldown方針を維持するため直前の成熟済みpatchへ固定した。plugin／Workers poolのpeer範囲は`vite@8.1.4`／`wrangler@4.108.0`／`vitest@4.1.10`を許容する。cooldown未達versionへ戻さず、`minimumReleaseAgeExclude`も追加しない。

- [ ] **Step 1: package／test harnessを直接作り、scoped installを確定する**

最初に`projects/points-web-app/package.json`、`tsconfig.json`、`vitest.config.ts`、`vitest.worker.config.ts`とtest setupだけをexact version tableで作る。package名は`@freeism/points-web-app`とし、この時点では`worker/index.ts`、SPA fallback、route componentを実装しない。package／harnessが存在する状態で次を実行する。greenfieldのため`vp migrate`は実行しない。

```bash
git diff --name-only -- projects/points-web-app
pnpm --filter freeism --filter @freeism/points-web-app install --no-frozen-lockfile
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
pnpm --filter freeism --filter @freeism/points-web-app install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
pnpm ignored-builds
```

Expected: 変更はPoints appのpackage／config／test harnessだけ。rootの`minimumReleaseAge: 4320`を使う標準scoped install後にPoints importerがroot lockfileへ現れ、frozen installでlockfile hash不変となる。72時間cooldownで拒否された場合は除外せず時間経過後に再実行する。`ignored-builds`に表示されたdependencyは許可せず、実行が必要な場合だけ個別reviewする。docsや他projectが変更された場合はrouting test作成前に停止する。

- [ ] **Step 2: routing/build の failing tests を作る**

`worker/index.worker.test.ts` に次の table test を作る。

| Request                                                          | Expected                                         |
| ---------------------------------------------------------------- | ------------------------------------------------ |
| `GET /api/health`                                                | `200`, JSON, service=`points-worker`             |
| `GET /api/missing`                                               | `404`, `application/problem+json`, HTML ではない |
| `GET /.well-known/missing`                                       | `404`, HTML ではない                             |
| `GET /missing.js`                                                | `404`, HTML ではない                             |
| `GET /missing.css`                                               | `404`, HTML ではない                             |
| `GET /missing.png`                                               | `404`, HTML ではない                             |
| `POST /dashboard` + `Accept:text/html`                           | `404`                                            |
| `GET /dashboard` + `Accept:application/json`                     | `404`                                            |
| `GET /dashboard` + `Accept:text/html`, `Sec-Fetch-Mode:navigate` | `200`, `/index.html` shell                       |
| `HEAD /dashboard` + HTML navigation headers                      | `200`, bodyなし                                  |

`src/routes/index.test.tsx` は Points shell heading と domain link を検査する。
固定route testは[`docs/web-app/v0.2/static-pages`](../../docs/web-app/v0.2/static-pages/README.md)を正本にし、`/terms`の日本語5節／英語5節、`/privacy`の日本語7節／英語7節、`/help`／`/docs`の両localeの全heading／paragraph／list item hashが両appのrender結果と一致することを検査する。各localeのcanonical MarkdownはUTF-8／LFへ正規化してSHA-256を計算し、片方だけの欠落、同じlocaleの重複、route componentへの本文複製を失敗させる。旧Next.js sourceからは日本語本文だけを意味損失なく移し、Next import、cache API、Tailwind classは移植しない。`/`がclient hydrate後のtopであることも検査する。

固定routeのcomponent／browser testには次を追加する。

- prerender HTMLに`lang="ja"`と`lang="en"`のcontent containerが1つずつあり、どちらもcanonical sourceの全contentを持つ
- 各HTMLに日本語正本／英語参照訳という位置付けが日本語と英語で表示される
- Playwrightの`test.use({ javaScriptEnabled: false })`を使うJavaScript無効contextでは、両containerと明示言語toggleの説明を閲覧できる
- JavaScript有効時は、同一originに保存済みの有効値、`navigator.languages`内で最初に現れる対応言語、日本語の順で初期表示が決まる。未知／破損した保存値は無視する
- URL、query、history、network requestを変更せず、Tab／Enter／Spaceで日本語／Englishを切り替えられ、選択状態をaccessible name／stateでscreen readerへ通知する
- `toMatchAriaSnapshot`で言語controlと表示中のheading構造を検証し、切替前後とも各content containerの`lang`が維持される
- `page.on("console")`でhydration errorを含むbrowser console errorを失敗させ、hydrate前後で両言語のsemantic contentとsource hashが変わらない

```bash
pnpm --filter @freeism/points-web-app test
pnpm --filter @freeism/points-web-app test:worker
```

Expected: test harnessは起動し、未実装のHono health／404／navigation fallbackまたはroute import assertionでFAILする。package不在、script不在、test runner不在をREDの理由にしない。

- [ ] **Step 3: exact package と minimal TanStack route を作る**

`projects/points-web-app/package.json` はStep 1のpackage interfaceとexact version tableを維持する。initial shell の scripts は `dev`, `preview`, `check`, `test`, `test:worker`, `test:e2e`, `build`, `build:staging`, `build:production`, `build:assert`, `types:worker` とする。D1がまだないため`db:migrate:staging`／`deploy:staging`は作らない。Points子計画Task 1が`db:generate`／`db:migrate:local`、Cloudflare resource確定後の親Task 6A直前staging bootstrapが`db:migrate:staging`／`deploy:staging`、production migrate/deploy/smokeは親Task 9とPoints子計画後半が所有する。

`vite.config.ts` の必須部分:

```ts
export default defineConfig({
  plugins: [
    cloudflare({ viteEnvironment: { name: "ssr" } }),
    tanstackStart({
      spa: {
        enabled: true,
        prerender: {
          outputPath: "/index.html",
          crawlLinks: false,
          retryCount: 0,
        },
      },
      prerender: {
        enabled: true,
        autoStaticPathsDiscovery: false,
        autoSubfolderIndex: false,
        crawlLinks: false,
        failOnError: true,
      },
      pages: [
        {
          path: "/terms",
          prerender: { enabled: true, outputPath: "/terms.html" },
        },
        {
          path: "/privacy",
          prerender: { enabled: true, outputPath: "/privacy.html" },
        },
        {
          path: "/help",
          prerender: { enabled: true, outputPath: "/help.html" },
        },
        {
          path: "/docs",
          prerender: { enabled: true, outputPath: "/docs.html" },
        },
      ],
    }),
    react(),
  ],
});
```

`src/routes` は `/`、shell layout、4固定routeだけを作り、runtime SSR/server function を追加しない。`/`はtop本体をSSGせずshell hydrate後に描画する。固定routeは8つのcanonical Markdownをbuild-time content moduleへ変換し、各HTMLへ日本語・英語の全contentとlocale別source hashを1回ずつ出力する。route componentへ本文を再入力しない。favicon だけ旧 `projects/web-app/public/favicon.svg` から複製し、旧 component/domain code を bulk copy しない。

言語切替は同一originの`localStorage` key `freeism.fixed-page-language.v1`へ`ja|en`だけを保存する。初期値resolverは保存済み有効値、`navigator.languages`内で最初に現れる`ja|en`、`ja`の順で決定し、URL／query／Cookie／server content negotiationは参照しない。静的HTMLのdefault CSSは両localeを表示する。CSP hash対象の小さいpre-hydration scriptが有効な場合だけrootの表示言語属性を設定し、CSSで非選択containerを隠すため、script無効／失敗時は両言語が残る。Reactは同じresolverと属性へ接続し、content nodeを削除・再生成せず表示状態だけを変更する。

toggleは日本語／Englishの明示label、keyboard focus、選択状態のaccessible announcementを持たせる。日本語containerは`lang="ja"`、英語containerは`lang="en"`を常に保持し、非表示側だけをaccessibility treeから除外する。切替はnavigation、追加fetch、route preloadを発生させない。

`docs/web-app/v0.2/static-pages/translation-review.json`はTask 2で作らず、人手review済みとも扱わない。日本語と英語の双方を確認したreviewerによるmanifestとproduction eligibilityは後続production gateが所有する。staging buildは未reviewの翻訳を検証できるが、productionへは進めない。英語の修正から日本語Markdownを生成・上書きするscriptは作らない。

`public/_headers`のbase ruleではhashed assetを`public, max-age=31536000, immutable`、5つのHTMLを`no-store`にする。build後に`generate-static-security-headers.mjs`が5 HTMLのexecutable inline scriptを列挙してSHA-256 hashを計算し、選択environmentのexact hostを使う[security header正本](../../docs/web-app/v0.2/security-and-delivery.md#51-http-security-header)を成果物directoryの`_headers`へ生成する。inline scriptがなければhash sourceを出力しない。scriptの`unsafe-inline`／`unsafe-eval`や外部originを追加して通さない。Hono共通middlewareはAPI、Problem Details、OAuth／callbackへ同じbaselineとno-store ruleを付ける。

- [ ] **Step 4: Wrangler asset-first config を作る**

`wrangler.jsonc` の root は local resource、`env.staging` / `env.production` は非共有 resource と custom domain route を明記する。shell 段階では binding ID を fake で埋めず、子計画/Cloudflare bootstrap で確定する resource block をコメントではなく未追加のままにする。

Static Assetsは次の形にし、Cloudflareの既定値に暗黙依存せず`not_found_handling: "none"`を明示する。汎用`single-page-application` fallbackは使わない。

```jsonc
{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "points-worker",
  "main": "./src/server.ts",
  "compatibility_date": "2026-07-12",
  "compatibility_flags": [
    "nodejs_compat",
    "assets_navigation_has_no_effect",
    "global_fetch_strictly_public",
  ],
  "workers_dev": false,
  "preview_urls": false,
  "observability": {
    "enabled": true,
    "logs": { "head_sampling_rate": 1 },
    "traces": { "enabled": true, "head_sampling_rate": 1 },
  },
  "assets": {
    "binding": "ASSETS",
    "not_found_handling": "none",
    "html_handling": "auto-trailing-slash",
    "run_worker_first": ["/api/*", "/.well-known/*"],
  },
}
```

named env の routes は staging/production domain を `custom_domain: true` で所有する。Terraform 側には同じ custom domain resource を書かない。

`observability`もnamed envで明示し、stagingはlogs／traces `1`、productionはlogs `1`／traces `0.05`へflattenする。子計画はapp／environment別の`OPS_METRICS` Analytics Engine dataset、固定verified destinationだけへ送れる`OPS_ALERT_EMAIL` Email binding、5分Cronを追加する。local testではin-memory stubを使い、production binding名やdestinationをfake値で埋めない。

- [ ] **Step 5: Hono navigation fallback を最小実装する**

`worker/spa-fallback.ts` の predicate は次をすべて満たす時だけ true にする。

- method が GET/HEAD
- path が `/api`、`/api/*`、`/.well-known`、`/.well-known/*` ではない
- 最終 path segment が `.js`, `.css`, `.png` 等の拡張子を持たない
- `Accept` に `text/html` がある
- `Sec-Fetch-Mode` が存在する場合は `navigate`
- `Sec-Fetch-Dest` が存在する場合は `document`

TanStack Startのcustom server entryはlocalhostのbuild-time prerenderだけをStart handlerへ渡し、staging／production hostのrequestはHonoへ渡す。Hono `notFound` はpredicateがtrueの時だけ `c.env.ASSETS.fetch(new Request(new URL('/', c.req.url), ...))` でcanonical root assetを取得する。それ以外は `404` Problem Details。`html_handling`による`/index.html -> /` redirectを内部fallbackへ混ぜない。HEAD は shell response header/status を保持し body を返さない。

- [ ] **Step 6: importer／lockfileを実装後にも再検証する**

```bash
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
pnpm --filter freeism --filter @freeism/points-web-app install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
pnpm ignored-builds
git diff --name-only -- projects/points-web-app pnpm-lock.yaml
```

Expected: Points importerのexact direct dependencyとfrozen installを確認し、lockfile hash不変。blocked lifecycle scriptのallowlistは広げない。変更はPoints app、Task 2のshared script/test、親／子plan、version同期対象の`docs/web-app/v0.2/architecture.md`／`security-and-delivery.md`、root `pnpm-lock.yaml`だけとし、`projects/documentation`、`projects/calc-contrib`、`projects/depchecker`を変更しない。greenfield直接構成後もSPA `/index.html`とCloudflare plugin設定のinvariantを維持する。

- [ ] **Step 7: UI test と Workers test を pass させる**

```bash
pnpm --filter @freeism/points-web-app check
pnpm --filter @freeism/points-web-app test
pnpm --filter @freeism/points-web-app test:worker
```

Expected: 全 PASS。Happy DOM config と Workers pool config は別 file であり、Workers test に Happy DOM custom environment を共有しない。

- [ ] **Step 8: staging/production build selection を検証する**

先に`scripts/web-app/assert-worker-build.mjs`を実装する。引数はapp path、expected environment、expected deployed Worker name。generated `wrangler.json`を探索し、configが選択環境へflatten済みであること、assets directory、`index.html`、`terms.html`、`privacy.html`、`help.html`、`docs.html`の実在、`/_shell.html`の不在、`not_found_handling === "none"`、`html_handling === "auto-trailing-slash"`、`assets_navigation_has_no_effect`、`workers_dev === false`、`preview_urls === false`、Hono entry/static route contractを検査する。4固定HTMLがshellと異なり、各HTMLが日本語・英語両方のcanonical Markdownのsection／paragraph／listとlocale別source hashを1回ずつ保持し、locale別URL／queryへlinkしないことも検査する。生成済み`_headers`は選択host、inline script hash、CSP、nosniff、Referrer／Permissions Policy、環境別HSTSを含み、`unsafe-eval`、scriptの`unsafe-inline`、開発／他environment originを含まないことを検査する。staging configへproductionを指定した時とその逆は必ず非0で終了させる。

```bash
pnpm --filter @freeism/points-web-app build:staging
node scripts/web-app/assert-worker-build.mjs projects/points-web-app staging points-worker-staging
pnpm --filter @freeism/points-web-app test:e2e
pnpm --filter @freeism/points-web-app build:production
node scripts/web-app/assert-worker-build.mjs projects/points-web-app production points-worker-production
```

Expected: 各buildのgenerated Wrangler configは選択環境だけにflattenされ、5つのHTMLが意図した内容で存在し、4固定HTMLは両localeのcontent／hashを持ち、`/_shell.html`がなく、asset configとcompatibility flagが一致する。built Workerを通すHTTP testで`/terms`は単一URLの固定二言語本文、`/terms/`は`/terms`への307、未prerenderの`/profiles/pusr_test`はshell、missing asset／APIは非HTML 404、HEAD shellはbodyなしとなる。Static Assets直配信とAsset Binding fallbackとHono responseの全てでsecurity headerが正本と一致し、OAuth／callbackは成功・失敗ともno-storeになる。staging／productionの`workers.dev`／preview URLは到達不能で、custom server entryはlocalhost prerender以外をHono `worker/index.ts`へ委譲し、deployed hostでruntime SSRを実行しない。

- [ ] **Step 9: commit**

```bash
git add -- projects/points-web-app projects/markets-web-app/plan/v0.2-implementation.md .gitignore scripts/web-app/assert-worker-build.mjs scripts/web-app/generate-static-security-headers.mjs tests/web-app/worker-build-tools.test.mjs plan/web-app/v0.2-migration.md docs/web-app/v0.2/architecture.md docs/web-app/v0.2/security-and-delivery.md pnpm-lock.yaml
git commit -m "feat(points): add Cloudflare greenfield shell"
```

## Task 3: Markets greenfield shell を同じ contract で作る

**Files:** Markets shell の File map 記載 create files、`.gitignore`、`pnpm-lock.yaml`。

- [ ] **Step 1: package／test harnessを直接作り、scoped installを確定する**

Pointsと同様に`projects/markets-web-app/package.json`、`tsconfig.json`、`vitest.config.ts`、`vitest.worker.config.ts`とtest setupだけを直接作り、package名を`@freeism/markets-web-app`へ固定する。routing／Worker実装はまだ追加せず、次を実行する。greenfieldのため`vp migrate`は実行しない。

```bash
git diff --name-only -- projects/markets-web-app
pnpm --filter freeism --filter @freeism/markets-web-app install --no-frozen-lockfile
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
pnpm --filter freeism --filter @freeism/markets-web-app install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
pnpm ignored-builds
```

Expected: 直接作成した変更はMarkets appだけ。rootの`minimumReleaseAge: 4320`を使う標準scoped installでMarkets importerがroot lockfileへ追加され、frozen installでhash不変となる。72時間cooldownで拒否された場合は除外せず時間経過後に再実行する。`ignored-builds`に表示されたdependencyは許可せず、実行が必要な場合だけ個別reviewする。docs／他project変更やrange／Git／tarball specがあればrouting test前に停止する。

- [ ] **Step 2: Points と同じ routing table の failing tests を作る**

service/domain expectation だけ `auction-worker` / `markets.freeism.app` に変え、API/asset/navigation 404 matrix、日本語・英語双方のcanonical固定本文／source hash、JavaScript無効時の両言語可読性、保存値→browser言語→日本語fallback、keyboard／ARIA snapshot、hydration console error 0、environment別security header、`workers_dev=false`／`preview_urls=false`は同一にする。

```bash
pnpm --filter @freeism/markets-web-app test
pnpm --filter @freeism/markets-web-app test:worker
```

Expected: test harnessは起動し、routing／Hono Worker／fallback未実装のassertionでFAILする。package／script／runner不在をREDの理由にしない。

- [ ] **Step 3: minimal shell を実装する**

Task 2 と同じ exact dependencies/config separation と固定公開ページの二言語render／toggle contractを使う。`wrangler.jsonc` の base name は `auction-worker`、custom domains は `staging.markets.freeism.app` / `markets.freeism.app`。共通 runtime package を新設せず、小さい fallback predicate は両 Worker に明示実装し、同じ contract test で drift を防ぐ。Markets側も同じ8 canonical Markdownをbuild時に読むだけとし、本文を複製しない。

- [ ] **Step 4: importer／lockfileを実装後にも再検証する**

```bash
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
pnpm --filter freeism --filter @freeism/markets-web-app install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
pnpm ignored-builds
git diff --name-only -- projects/markets-web-app pnpm-lock.yaml
```

Expected: Markets importerのexact direct dependencyとfrozen installを確認し、lockfile hash不変。blocked lifecycle scriptのallowlistは広げない。変更はMarkets appとroot `pnpm-lock.yaml`だけで既存docsは保持される。

- [ ] **Step 5: tests/build を pass させる**

```bash
pnpm --filter @freeism/markets-web-app check
pnpm --filter @freeism/markets-web-app test
pnpm --filter @freeism/markets-web-app test:worker
pnpm --filter @freeism/markets-web-app build:staging
node scripts/web-app/assert-worker-build.mjs projects/markets-web-app staging auction-worker-staging
pnpm --filter @freeism/markets-web-app test:e2e
pnpm --filter @freeism/markets-web-app build:production
node scripts/web-app/assert-worker-build.mjs projects/markets-web-app production auction-worker-production
```

Expected: 全 PASS。`/index.html`、両localeを含む4固定HTML、locale別source hash、keyboard／screen reader／JavaScript無効／hydration test、Static／fallback／Hono security header、OAuth no-store、workers.dev／preview URL到達不能とflattened env assertionsもPointsと同一。

- [ ] **Step 6: commit**

```bash
git add -- projects/markets-web-app .gitignore pnpm-lock.yaml
git commit -m "feat(markets): add Cloudflare greenfield shell"
```

## Task 4A: wire contract completion

Task 4のOpenAPIを推測なしで実装できるよう、Markdownのwire正本と横断仕様を先に確定する。コード、OpenAPI、generated type、package scriptはこのTaskで変更しない。

**Files:**

- Modify: `docs/web-app/v0.2/points-markets-contract.md`
- Modify: `docs/web-app/v0.2/response-format.md`
- Modify: `docs/web-app/v0.2/authentication.md`
- Modify: `docs/web-app/v0.2/decision-register.md`
- Modify: `plan/web-app/v0.2-migration.md`

- [ ] **Step 1: Points–Markets wire正本を完成させる**

`points-markets-contract.md`へJSON object、opaque ID／hash／UTC instant、安全整数／integer string、success envelope、Problem Details、cacheの共通規則を集約する。既存11 operationのpath、operationId、auth、scopeを変えず、各operationのexact request／response required field、enum、nullable、sort、success status、request body byte上限、`Idempotency-Key`要否を記載する。既存のbusiness rule、lease、ownership、atomicity、ETag／content hash、error開示制限を削らない。

- [ ] **Step 2: 横断文書と認証endpointを整合させる**

`response-format.md`は初回HTTP statusを保つidempotency replay、retryごとのrequest ID再発行、exact cache値だけを更新し、11 operationのpayload schemaを複製しない。`authentication.md`はPoints Better Auth issuer／base URL `https://points.freeism.app/api/auth`、標準authorize／token／introspect／revoke／discovery path、protected／OAuth cache、通常user／M2M scope allowlistだけを更新する。独自OAuth endpointを作らず、Task 6Aのlive feasibility gateで標準実装と不一致ならreleaseを停止する。

- [ ] **Step 3: Decision RegisterとTask 4を整合させる**

`decision-register.md`へTask 4Aの補完決定を1件追加し、schema全文は`points-markets-contract.md`だけを参照する。続くTask 4は、eligibility、link-attempt create／finalize、connection deactivate、reservation create、capture、releaseだけ`Idempotency-Key`必須、GET／balance-check／reservation-statusは不要とする。eligibility／status／capture／releaseは1,048,576 bytes、それ以外のJSON POSTは65,536 bytesとし、success statusとreplay statusをwire正本どおり検査する。

- [ ] **Step 4: Markdownと整合性を検証する**

repo既存のMarkdown format／checkを確認して上記5文書だけへ実行する。`rg`でreplayを常に200とする旧status文言、旧OAuth path、M2M eligibility scope欠落、11 operation matrixの欠落がないことを確認し、`git diff --check`を実行する。

- [ ] **Step 5: commit**

```bash
git add -- docs/web-app/v0.2/points-markets-contract.md docs/web-app/v0.2/response-format.md docs/web-app/v0.2/authentication.md docs/web-app/v0.2/decision-register.md plan/web-app/v0.2-migration.md
git commit -m "docs(web-app): complete Points Markets wire contract"
```

## Task 4: Points-owned OpenAPI と generated contract

**Files:**

- Create: `docs/web-app/v0.2/points-markets.openapi.json`, `tests/web-app/openapi-contract.test.mjs`, 両 app の `src/generated/points-markets-api.ts`
- Modify: root `package.json`, `pnpm-lock.yaml`

- [ ] **Step 1: failing OpenAPI invariant test を作る**

test は JSON を parse し、次を検査する。

- OpenAPI `3.1.0`
- 11 endpoint と 11 operationId が重複なく存在
- publicな不変Package Revision endpointは`pointPackageId`、Revision ID、status、name、description、related URL、`totalWeight`、`packageTick`、`contentHash`、componentの評価軸ID／revision ID／name／`displayOrder`／`weight`／文字列`minimumUnitScaled`／`buyNowEnabled`を返し、RFC 8785＋component安定sortのhash、strong `ETag`、1年immutable cache、304を定義する
- Point Package Auction eligibility operationはClient Credentialsの専用scope `points.packages.auction-eligibility`、最大1,000 item／1MiB、Client ID・Auction command ID／hash・全Package ID／Revision ID／content hashへ束縛した30秒receipt、全件成功またはreceipt 0件、同じ冪等keyで期限非延長を定義する
- 利用者用Authorization Code／Refresh、M2M用Client Credentials、Settlement retry用Authorization Codeの3 OAuth Client/security schemeとscopeが分離
- `disableJwtPlugin: true`によりAuthorization Code／RefreshとClient Credentialsで発行するAccess Tokenはopaqueで、OpenAPI／Points／Markets実装がJWT claim decode／JWKS検証へ依存しない。ID TokenはResource API Bearerとして拒否する（DEC-263）
- Markets BFFとPoints Resource APIがWorker Secretのconfidential credentialでBetter Auth標準`/api/auth/oauth2/introspect`をremote呼出しし、利用者Tokenの`active`、pairwise `sub`、利用者用Client ID、audience/resource、scope、期限を保存済みconnectionと照合する。別client／別environment／inactive／scope不足を拒否し、credentialをbrowserへ出さない
- opaque Client Credentials TokenはM2M operationだけに使い、user subject／user scopeを持たず、connection read／balance read／reservation create／unlink／管理step-upを実行できない（DEC-263）
- M2M用Client Credentials scope `points.connection.link-attempt.create`／`finalize`が2 operationだけに使われ、opaque attemptを利用者用／M2M用Client ID、Markets user／Session state hash／PKCE／redirect／scopeへ束縛する。Token交換後はpending grantとし、Markets local保存後のconfirmでremote introspection済みissuer／pairwise subject／利用者用Client IDをattemptへ照合して対応M2M用Client IDとともに保存しACTIVEにする。user token予約は対応M2M用Client IDを所有clientへ保存する。cancel／TTLはapp-owned grantをlive拒否し、raw token保持時だけRFC 7009でbest-effort revokeする
- Settlement retry専用Client IDのAuthorization Code + PKCE scope `points.admin.settlement.retry`が利用者用／M2M用Clientから分離される
- 通常unlink用の専用Authorization Code + PKCE scope `points.connection.unlink`が通常user／Refresh／M2M scopeから分離され、`deactivatePointsConnection`だけに使われる
- user operation に M2M scope、M2M operation に user scope がない
- `Idempotency-Key`はeligibility、link-attempt create／finalize、connection deactivate、reservation create、capture、releaseでrequired、GET、balance-check、reservation-statusで不要
- price/quantity/safe-integer upper bound は `9_007_199_254_740_991`
- point amount に JSON floating number schema を使わない
- success `{data, meta.requestId}` と RFC 9457 Problem Details + `code`
- protected/private responseの`Cache-Control: private, no-store`と、Public Package Revisionだけのimmutable cache例外
- Point Package Auction eligibility／status／capture／releaseは1,048,576 bytes、それ以外のJSON POSTは65,536 bytesのrequest上限
- success statusはpublic／user GET、balance／status／finalize／deactivate／capture／releaseが200、link-attempt／eligibility receipt／reservation createが201、public revisionだけ304を許可する。idempotency replayは初回HTTP statusを保持し、初回201ならreplayも201とする

```bash
node --test tests/web-app/openapi-contract.test.mjs
```

Expected: FAIL。OpenAPI file 不在。

- [ ] **Step 2: canonical contract を OpenAPI 3.1 として実装する**

[`points-markets-contract.md`](../../docs/web-app/v0.2/points-markets-contract.md) のendpoint、request/response、Package eligibility 30秒lease、reservation 900秒lease、status transition、scope、idempotencyをlosslessに記述する。security schemesは利用者用Clientのopaque user Access Tokenを発行するAuthorization Code + PKCE/Refreshと通常unlink用Authorization Code + PKCE、Settlement retry専用ClientのAuthorization Code + PKCE、M2M用ClientのClient Credentialsを別Client／flow／scopeとして表現する。標準Better Auth discovery／token／introspection endpointをapp独自OpenAPI operationとして再実装せず、contract testから標準remote endpointを呼ぶ。unlinkはGoogle fresh、ACTIVE reservation 0、app-owned grant無効化、revocation outbox、receipt後Markets closeを、管理assertionはPoints ADMIN、Google fresh、対象ID・reason hash・Markets Sessionへの束縛、60秒期限、`jti`一回消費を別の認証contract testで検証する。

Better Auth標準OAuth Providerでopaque利用者Token、RFC 7662 confidential remote introspectionのpairwise `sub`、3つのstatic confidential client、Client単位scope分離、Client CredentialsのM2M `sub`不存在が同時に成立しない場合は、このTaskで独自Token verifier／introspection endpoint／Providerを追加しない。Task 6Aのlive feasibility gateをblockedにし、canonical docsとDecision Registerの再承認を先に行う。

経済 amount は曖昧な小数 JSON number を使わず、`priceTicks`/`quantity` は safe integer、scale 済み amount は integer string schema を使う。`capture` は winner 全件を1 request にする。

v0.2は未公開で後方互換が不要なため、expand-firstはTask 4の初回OpenAPI実装へ適用しない。Task 4Aで確定した11 operationのfieldを最初からrequiredで実装し、未確定optional fieldを中間contractとして追加しない。初回deploy後の同一v0.2 Points → Markets rolling deployで将来fieldを追加する場合だけexpand-firstとし、旧field／operationの削除は新旧Workerが同じversionへ揃った後の別deployにする。

- [ ] **Step 3: generated types を作る**

root script を次の interface にする。

```json
{
  "contract:web-app:generate": "openapi-typescript docs/web-app/v0.2/points-markets.openapi.json -o projects/points-web-app/src/generated/points-markets-api.ts && openapi-typescript docs/web-app/v0.2/points-markets.openapi.json -o projects/markets-web-app/src/generated/points-markets-api.ts",
  "contract:web-app:check": "pnpm contract:web-app:generate && git diff --exit-code -- projects/points-web-app/src/generated/points-markets-api.ts projects/markets-web-app/src/generated/points-markets-api.ts"
}
```

```bash
pnpm contract:web-app:generate
node --test tests/web-app/openapi-contract.test.mjs
pnpm contract:web-app:check
```

Expected: test PASS、生成後 diff `0`。

- [ ] **Step 4: child app contract tests を接続する**

Points 子計画は handler response が OpenAPI schema と一致する test、Markets 子計画は generated type で client request を compile する test を追加する。本計画では handler/client の domain 実装を重複しない。

- [ ] **Step 5: commit**

```bash
git add -- docs/web-app/v0.2/points-markets.openapi.json tests/web-app/openapi-contract.test.mjs projects/points-web-app/src/generated/points-markets-api.ts projects/markets-web-app/src/generated/points-markets-api.ts package.json pnpm-lock.yaml
git commit -m "feat(web-app): define Points Markets OpenAPI"
```

## Task 5: 子計画 Phase A（Cloudflare不要のlocal実装）

**Files:** 子計画自身が列挙する files。本計画から個別 domain code を編集しない。

- [ ] **Step 1: Points Task 1〜13をlocal fixtureだけで実行する**

[`projects/points-web-app/plan/v0.2-implementation.md`](../../projects/points-web-app/plan/v0.2-implementation.md) のTask 1〜13を順番どおり実行する。Google／OAuth／Points–Marketsの外部callはfixture server／fetch stub、D1／Workersはlocal runtimeだけを使う。Cloudflare resource、staging credential、live OAuthをまだ要求しない。子計画のGoogle fresh live spikeはTask 6Aへ移し、Task 14以降を先に実装しない。

DEC-263のopaque／standard introspection／M2M分離はTask 13までdomain principal fixtureだけで境界を作り、Provider feasibilityを成功扱いしない。DEC-264の`BUY_NOW`はMarkets子計画とともにPhase Bへ置き、Phase Aで架空のhold／settlement成功を作らない。

Task 1はBetter Auth標準Accountの既存Account再利用までを検査し、永久`providerId + accountId -> Points userId`対応を保証したとは扱わない。Task 9でapp-owned永久対応table、`(providerId, accountId)`複合一意制約、競合拒否、Account close後の元ユーザー再開を完成させる。Task 9未完了またはそのmigration／回帰testが失敗する状態ではTask 13完了、Task 6A、production releaseのいずれにも進まない。

Points Task 1が`better-auth`、`@better-auth/drizzle-adapter`、Points専用`@better-auth/oauth-provider`、standalone CLI `auth`等をmanifestへ追加した直後、rootの`minimumReleaseAge: 4320`を使う標準 `pnpm install --no-frozen-lockfile` を実行する。72時間cooldownで拒否された場合はTask 1を停止し、設定で除外したり別versionへ置換したりせず、時間経過後に再実行する。

- Points D1 migration chain が `0000` から空 DB に適用可能
- Better Auth Google/GitHub login、explicit link、implicit email link禁止をfixtureで検証
- Task 9のapp-owned永久OAuth主体対応、一意制約、別ユーザー再割当拒否、close後の元ユーザー再開をfixtureで検証
- Google fresh policyの899／900／901秒、provider subject一致、Session rotationをlocal contract testで検証。ただしlive Googleでの成立はまだ主張しない
- fixed decimal、Package、CSV、FIX、ownership、ledger、transfer／exchange、substitution／distribution、vector reservation／captureのTask 1〜13 invariant testsがgreen
- Task 13完了時点では外部OAuth／reservation HTTP routeを公開しない
- 親Task 6A直前のstaging bootstrapで初回作成した`db:migrate:staging`／`deploy:staging`と、Task 2の`build:staging`／`build:assert`が存在し、Task 6Aでnamed staging D1をmigrateして同じflattened configをbuild直後にdeployできる

- [ ] **Step 2: Phase Aのlocal gateを実行する**

```bash
pnpm contract:web-app:check
pnpm --filter @freeism/points-web-app check
pnpm --filter @freeism/points-web-app test
pnpm --filter @freeism/points-web-app test:worker
pnpm --filter @freeism/points-web-app build:staging
pnpm --filter @freeism/points-web-app build:assert
```

Expected: Points Task 1〜13のlocal test／buildが全PASSし、live OAuth未実証を成功扱いしない。失敗をmaster plan側のcompatibility shimや独自認証で隠さない。Markets子計画とPoints Task 14以降はTask 6Aまで開始しない。

## Task 6: Cloudflare edge IaC と environment bootstrap

**Files:** `infra/cloudflare/**`, `scripts/web-app/bootstrap-terraform-state.mjs`, `plan/web-app/runbooks/cloudflare-bootstrap.md`, `plan/web-app/evidence/cloudflare-bootstrap-staging.md`, `projects/points-web-app/wrangler.jsonc`, `projects/points-web-app/worker-configuration.d.ts`, `.gitignore`。

- [ ] **Step 0: Terraform CLI `1.15.7`を使用前にguardする**

Task 5まではTerraform不在でも進められるが、このTaskの最初の`terraform` commandより前に次を実行する。

```bash
command -v terraform
terraform version -json | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const v=JSON.parse(s).terraform_version;if(v!=="1.15.7"){console.error(`expected Terraform 1.15.7, got ${v}`);process.exit(1)}})'
```

Expected: 両方exit `0`。不在または相違時はTask 6を停止する。agentはHomebrew、`sudo`、global installerを自動実行しない。利用者が承認したversion manager、またはHashiCorp公式archiveをchecksum／signature検証して利用者管理の隔離pathへ配置する方法で`1.15.7`が用意された後、同じguardから再開する。導入方法の承認がない間はTerraform test、init、state bootstrap、Cloudflare mutationを行わない。

- [ ] **Step 1: failing Terraform test を作る**

`edge.tftest.hcl` は mock provider でstaging／productionを分けて次を assert する。shared workspaceは追加しない。

- production workspaceだけにapex `freeism.app` と`www`のproxied DNSが存在し、両host/path/queryを保持せず`https://points.freeism.app/`へpermanent redirectする
- production workspaceだけがzone-wide WAF managed rules、IP/path edge rate limit、Email Routing有効化を所有する
- staging workspaceは上記zone-wide resourceを0件とし、staging 2 hostのAccess application/policyだけを所有する
- production と staging の hostname/resource name が混ざらない
- Points/Markets Worker custom domain resource は Terraform に存在しない
- incident／5xx／usageのCloudflare native Notification policy 3件と、Email Routingのverified ops destination inventoryがenvironment別に存在する

```bash
terraform -chdir=infra/cloudflare/modules/web-app-edge init -backend=false
terraform -chdir=infra/cloudflare/modules/web-app-edge test
```

Expected: FAIL。module 未実装。

- [ ] **Step 2: Terraform version/provider と Remote R2 backend を実装する**

root は `required_version = "= 1.15.7"`、`cloudflare/cloudflare = "5.21.1"` とする。`infra/cloudflare/backend.tf` は HashiCorp S3 backend を使い、次を固定する。

```hcl
terraform {
  backend "s3" {
    bucket                      = "freeism-terraform-state"
    key                         = "terraform.tfstate"
    workspace_key_prefix        = "web-app"
    region                      = "auto"
    use_lockfile                = true
    skip_credentials_validation = true
    skip_region_validation      = true
    skip_requesting_account_id  = true
    skip_metadata_api_check     = true
    skip_s3_checksum            = true
  }
}
```

R2 endpoint は実行時に `AWS_ENDPOINT_URL_S3="https://${CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com"`、bucket-scoped Object Read & Write credential は `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` として渡す。access key/secret key は HCL、tfvars、plan、artifact に書かない。workspace は `staging` / `production` だけを許し、`terraform.workspace === var.environment` の `check` block で取り違えを拒否する。

新規 state bucket は旧画像 bucket と別の `freeism-terraform-state`。`scripts/web-app/bootstrap-terraform-state.mjs` が pinned Wrangler/API で one-time create と list/get verification を行い、通常 IaC module は自分の backend bucket を管理・削除しない。これは D1 scheduled backup ではない。

- [ ] **Step 3: edge module を最小実装する**

Terraform は canonical ownership に限定する。

- production workspaceだけがapex/www DNS、root-only permanent redirect、managed WAF、zone rate limit、Email Routing有効化を所有する。staging workspaceと二重所有しない
- staging Access
- Turnstile widget
- incident／5xx／usage thresholdのnative Notification policy
- appの`OPS_ALERT_EMAIL`だけが使用するEmail Routing destinationの有効化とverified destination inventory

user + Auction、ADMIN + 評価軸等の exact actor/resource limits は Terraform へ擬似実装せず子 app の Hono/D1/DO に残す。rate values は `security-and-delivery.md` §8 を使用する。

Cloudflareの現行公式alert type inventoryとProvider schemaではWorker script runtime exception専用typeを確認できないため、未確認typeを捏造しない。environment-wideの`incident_alert`、`http_alert_edge_error`、`billing_usage_alert`を最小構成とし、app固有runtime exceptionはWorkers Logs／TracesとD1 monitorで相関する。

- [ ] **Step 4: format/validate/test を pass させる**

```bash
terraform fmt -recursive infra/cloudflare
terraform -chdir=infra/cloudflare/modules/web-app-edge init -backend=false
terraform -chdir=infra/cloudflare/modules/web-app-edge validate
terraform -chdir=infra/cloudflare/modules/web-app-edge test
terraform -chdir=infra/cloudflare init -backend=false
terraform -chdir=infra/cloudflare validate
terraform -chdir=infra/cloudflare providers lock -platform=darwin_arm64 -platform=linux_amd64
```

Expected: format diff なし、validate/test PASS。provider lockfile は Cloudflare Provider `5.21.1` の両 platform checksum を持つ。

`.gitignore` は `.terraform/`, `*.tfplan`, crash log を無視し、`.terraform.lock.hcl` は無視しない。

- [ ] **Step 5: bootstrap runbook を実装する**

runbook は secret 値を記載せず、次の順を固定する。

1. Workers Paid と D1 Time Travel 30日を API/dashboard で確認
2. `scripts/web-app/bootstrap-terraform-state.mjs` で Terraform state R2 bucket を one-time 作成
3. bucket-scoped Object Read & Write credential を作り、値を GitHub Environments `web-app-staging` / `web-app-production` の `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_ENDPOINT_URL_S3` に保存
4. 両 GitHub Environment は required reviewer/wait timer を0にし、production手動approvalを作らない
5. staging/production 別 Cloudflare API token を作成
6. Task 6Aのlive gateに必要なPoints staging Worker／D1／custom domain、staging Google OAuth App、Markets利用者用／M2M用／Settlement retry用の3 static confidential OAuth Client、各redirect URI、pairwise secret、Worker Secretsだけを先に作る。DO／Workflow／production resourceはまだ作らない
7. staging E2E用Cloudflare Access Service Tokenを作り、`CF_ACCESS_CLIENT_ID_STAGING`／`CF_ACCESS_CLIENT_SECRET_STAGING`としてstaging Environmentだけに保存する
8. Task 6Aのstandard Better Auth live gateを完了し、PASS後だけPoints Task 14以降とMarkets子計画を再開する
9. 子計画後半のbinding定義が確定してから、staging／production別D1、DO、Workflow、Service Binding、OAuth App／Client／redirect URI／Worker Secretsの残りを作る
10. environment別`OPS_METRICS` dataset名、5分monitor Cron、verified staging／production alert destinationをchild configへ設定する
11. `wrangler types`でbinding typesを更新し、resource inventoryのID／hashだけをevidenceへ残す

runbookを書くだけで次へ進まない。repository ownerがaccount／environment／resource inventoryと作成差分を確認した後、items 1〜7をこのTask内で実行する。Terraform state bucketはscriptのread-only check →明示apply →再checkの順とし、同じinterfaceをrunbookへ記載する。

```bash
node scripts/web-app/bootstrap-terraform-state.mjs --check
node scripts/web-app/bootstrap-terraform-state.mjs --apply
node scripts/web-app/bootstrap-terraform-state.mjs --check
```

最初の`--check`は対象bucket不在をread-onlyに報告してexit 0、既存bucketがある場合はaccount／name／ownershipを一致確認してexit 0とする。不一致やread errorは非0で停止する。`--apply`は確認済み対象が不在の場合だけ作成し、最後の`--check`は存在／ownership／list-getを検証する。

続いてrunbookに固定したpinned Wrangler／Cloudflare API commandで、Points staging Worker／D1／custom domain、staging Google OAuth App、Markets利用者用／M2M用／Settlement retry用の3 static confidential OAuth Client、pairwise secret、必要最小限のWorker Secrets、staging E2E用Access Service Tokenだけを作成・照合する。各create前後にread APIでaccount／environment／name／IDを照合し、secret値をartifactへ出さない。

Points staging D1の実`database_name`／`database_id`とcustom domainを`projects/points-web-app/wrangler.jsonc`の`env.staging`へ反映し、production blockやsecret値は追加しない。続いてstaging configからbinding型を再生成・checkする。

```bash
pnpm --filter @freeism/points-web-app exec wrangler types worker-configuration.d.ts --config wrangler.jsonc --env staging
pnpm --filter @freeism/points-web-app exec wrangler types worker-configuration.d.ts --config wrangler.jsonc --env staging --check
pnpm --filter @freeism/points-web-app check
```

`cloudflare-bootstrap-staging.md`はこのTaskで先に作り、owner確認、account/environment hash、state bucket hash、Points Worker／D1／domain hash、用途別3 OAuth Client hash、Access application/service-token hash、実行command／時刻／exit statusだけを記録する。secret、token、OAuth code、raw resource IDは書かない。owner確認、state bucket、staging runtime inventory、Points config／generated typeのどれかが欠ける場合はTask 6Aへ進まない。

- [ ] **Step 6: Remote R2 state lock を staging で実証する**

backend credentialを環境変数へ読み込んだ後、staging workspaceを作る。

```bash
terraform -chdir=infra/cloudflare init -reconfigure
terraform -chdir=infra/cloudflare workspace select staging
```

初回だけ `workspace select` が失敗したら `terraform workspace new staging` を実行する。Terminal Aで次を実行し、apply確認promptで待つ（`yes`は入力しない）。

```bash
terraform -chdir=infra/cloudflare apply -var-file=environments/staging.tfvars.example -lock-timeout=0s
```

Terminal Bで同時に次を実行する。

```bash
terraform -chdir=infra/cloudflare plan -var-file=environments/staging.tfvars.example -lock-timeout=0s
```

Expected: Terminal Bは `Error acquiring the state lock` で失敗し、R2上の `.tflock` 情報を返す。Terminal Aには `no` を入力して変更0で終了する。その後通常planが成功することも確認し、secretを除いたcommand／時刻／workspace／lock error code／lock ID hashを`cloudflare-bootstrap-staging.md`へ残す。この実証が終わるまでCIのTerraform applyを有効化しない。`force-unlock` は、実行中processがないと確認できる事故復旧時だけ使う。

- [ ] **Step 7: staging bootstrap applyをreviewして実行し、productionはplanだけに留める**

```bash
terraform -chdir=infra/cloudflare workspace select staging
terraform -chdir=infra/cloudflare plan -var-file=environments/staging.tfvars.example -out=staging.tfplan
terraform -chdir=infra/cloudflare show -no-color staging.tfplan
# repository ownerがTerraform所有resourceのcreate/update/deleteを確認した後だけ実行する
terraform -chdir=infra/cloudflare apply -lock-timeout=5m staging.tfplan
terraform -chdir=infra/cloudflare plan -var-file=environments/staging.tfvars.example -detailed-exitcode
terraform -chdir=infra/cloudflare workspace select production
terraform -chdir=infra/cloudflare plan -var-file=environments/production.tfvars.example -out=production.tfplan
```

production workspace が未作成なら、plan 前に `terraform -chdir=infra/cloudflare workspace new production` を一度だけ実行する。Expected: staging applyはTask 6Aに必要なTerraform所有のstaging Access、staging Turnstile／native Notification／verified destination inventoryだけを作り、zone-wide DNS／redirect／WAF／rate limit／Email Routing settingsを0件とし、直後のstaging planはexit 0。Wrangler 所有の Worker/custom-domain/D1/DO/Workflow を create/update/deleteせず、secret値がplanに出ない。review済みplan hash、apply時刻、resource address／ID hash、post-apply exit 0を`cloudflare-bootstrap-staging.md`へ追記する。production planだけがzone-wide resourceを含み、review済みplanだけを残す。production初回applyはTask 11の正式版bump merge後に起動するmain pipelineから行う。staging applyを行わずにTask 6AのAccess越しlive gateへ進まない。

- [ ] **Step 8: commit**

```bash
git add -- infra/cloudflare scripts/web-app/bootstrap-terraform-state.mjs plan/web-app/runbooks/cloudflare-bootstrap.md plan/web-app/evidence/cloudflare-bootstrap-staging.md projects/points-web-app/wrangler.jsonc projects/points-web-app/worker-configuration.d.ts .gitignore
git diff --cached --check
git commit -m "infra(web-app): define Cloudflare edge resources"
```

## Task 6A: Better Auth staging feasibility gate と子計画 Phase B

**Files:** Points live spike／contract test／secret-free evidenceと、Points／Markets子計画が列挙するfiles。本計画から独自認証endpointを追加しない。

- [ ] **Step 1: Points Task 1／2のauth実装をstagingへdeployする**

Task 5で完了したPoints Task 1／2のGoogle／GitHub login、Google fresh policyとshellだけを、Task 6で作ったstaging D1／Google OAuth App／Secretへ接続する。Points OAuth Provider／Resource APIのTask 14実装はまだ追加しない。buildとdeployを同じjob／shellで連続実行する。

```bash
pnpm --filter @freeism/points-web-app db:migrate:staging
pnpm --filter @freeism/points-web-app build:staging
node scripts/web-app/assert-worker-build.mjs projects/points-web-app staging points-worker-staging
pnpm --filter @freeism/points-web-app deploy:staging
```

Expected: flattened staging configだけがdeployされ、production secret／resourceへアクセスせず、Task 1／2の標準Better Auth login／fresh endpointがstaging Access越しに到達する。Task 14のpartial implementation、custom token／introspection endpointが混ざる場合はdeployせず停止する。

- [ ] **Step 2: 子計画のGate A／Gate Bを順番どおり実行する**

まずPre-Task 14 Gate Aで、install済みBetter Auth `1.7.0-rc.1`のdocumented API、type／export、isolated D1を使い、DEC-089／DEC-232／DEC-248／DEC-263の`disableJwtPlugin: true`、opaque user／M2M Access Token、利用者用／M2M用／Settlement retry用Clientのscope分離、標準Resource Clientによるconfidential remote introspection、pairwise subject、ID Token非Bearer、PENDING→標準OAuth→confirm、cancel／TTL live拒否とraw token保持時のbest-effort revocationを検証する。続いてGate Bでstaging Google freshをlive検証する。

```bash
pnpm --filter @freeism/points-web-app test -- test/contract/google-fresh-authorization-params.contract.test.ts test/contract/better-auth-oauth-feasibility.contract.test.ts
pnpm --filter @freeism/points-web-app exec tsx scripts/verify-better-auth-oauth-feasibility.ts
pnpm --filter @freeism/points-web-app exec tsx scripts/verify-google-fresh-live.ts
```

Gate A／Bは最低限次を実測する。

- Google ID Tokenの`auth_time`、nonce、issuer、audience、同一`sub`、899／900秒許可と901秒拒否、Session rotation
- Authorization Code + PKCEで得る利用者Access Tokenがopaqueであり、Marketsがtoken本文をdecodeせず標準RFC 7662 introspectionを呼ぶ
- introspectionの`active`、pairwise `sub`、client、audience/resource、scope、期限が保存済みconnectionと一致し、別client／別environment／inactive／scope不足を拒否する
- 同じPoints userでもclient／environmentが違えばpairwise `sub`が異なる
- 利用者／Client Credentials Access Tokenの双方がopaqueで、利用者用Clientのresponseはpairwise `sub`あり＋利用者scopeだけ、M2M用Clientのresponseは利用者`sub`なし＋M2M scopeだけとなる。Settlement retry用Clientは`points.admin.settlement.retry`だけを持つ。Client間のscope混在／矛盾／未知scopeを拒否し、独自`token_class`／`grant_type` claimを追加しない。Points Resource APIとMarkets BFFはWorker Secretのconfidential credentialで標準remote introspectionを使い、ID TokenはResource API Bearerとして拒否する
- app-owned attempt／`PENDING_MARKETS_CONFIRMATION`の1対1 uniqueを標準OAuth開始前に確定し、Markets local PENDING後のM2M `CONFIRM`でだけACTIVEにする。CONFIRMは利用者用Clientのstandard remote introspectionで得たissuer／pairwise subject／Client IDをattemptへ照合し、connectionへ対応M2M用Client IDも保存する。Authorization Code／token familyをapp-owned transactionへ参加させず、途中失敗・crash・10分TTL超過はapp-owned grantのlive拒否へ収束し、Marketsがraw tokenを持つ場合だけRFC 7009 outbox、未知tokenは自然失効とする
- Service Binding相当のserver-to-server callでもBearer／scope／audience検証を省略しない

Expected: すべてPASS。opaque発行、標準introspectionのpairwise `sub`、confidential remote introspection認証、別Clientのscope＋`sub`有無からのprincipal分離、3つのstatic confidential client、raw token保持時の標準revocation、app-owned PENDING／confirm／TTL live拒否、Google freshのどれかが標準Better Authで成立しない場合は、独自Provider、独自Token、独自introspection、発行hook、内部table直接更新、独自claim補完を実装せず、子計画のstable blocker codeで全子計画を停止し、canonical docs／Decision Registerを再承認する。

- [ ] **Step 3: Gate A／Gate Bのsecret-free evidenceを別commitにする**

evidenceには実行日時、staging host、Better Auth exact version、Git SHA、test command、opaque判定、introspection field名とPASS／FAILだけを記録する。Access／Refresh／Client Credentials Token、code、Cookie、Secret、pairwise `sub`実値、個人情報を保存しない。

```bash
git add -- projects/points-web-app/scripts/verify-better-auth-oauth-feasibility.ts projects/points-web-app/test/contract/better-auth-oauth-feasibility.contract.test.ts artifacts/web-app/better-auth-oauth-feasibility/1.7.0-rc.1.json
git commit -m "test(points): prove standard oauth feasibility"
git add -- projects/points-web-app/scripts/verify-google-fresh-live.ts artifacts/web-app/google-fresh-live
git commit -m "test(points): prove google fresh oidc live contract"
```

- [ ] **Step 4: Points Task 14以降とMarkets子計画を順に実行する**

live gate PASS後にだけPoints Task 14〜18を実行し、Points OpenAPI／Resource APIをgreenにする。その後、[`projects/markets-web-app/plan/v0.2-implementation.md`](../../projects/markets-web-app/plan/v0.2-implementation.md)をTask 1〜17の順に実行する。Marketsはopaque利用者Tokenをlocal decodeせず標準introspectionでpairwise subjectを照合し、Client CredentialsをM2M operationだけに使う。settlement integrationはPoints Task 14がgreenになるまで開始しない。

Markets Task 1が`better-auth`、`@better-auth/drizzle-adapter`、standalone CLI `auth`等をmanifestへ追加した直後も、rootの`minimumReleaseAge: 4320`を使う標準 `pnpm install --no-frozen-lockfile` を実行する。Marketsへ未使用`@better-auth/oauth-provider`を追加しない。以後、子Taskがdirect dependencyを増やすたびに同じ標準installで72時間cooldownを適用し、拒否された場合は設定で除外せず時間経過後に再実行する。

子計画のversion／package表現と衝突する場合は、この親計画の境界を優先する。Phase Bでは両appの`better-auth`／`@better-auth/drizzle-adapter`／standalone CLI `auth`をexact `1.7.0-rc.1`、Pointsだけの`@better-auth/oauth-provider`もexact `1.7.0-rc.1`とし、正式`1.7.0`への変更はTask 11だけで行う。Markets Task 1ではmanifest追加直後に次を実行し、root lockfileを同じcommitへ含める。

```bash
pnpm --filter @freeism/markets-web-app install --no-frozen-lockfile
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
pnpm --filter @freeism/markets-web-app install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
pnpm ignored-builds
git add -- projects/markets-web-app/package.json pnpm-lock.yaml
```

72時間cooldownで標準installが拒否された場合、MarketsにProvider packageが存在する場合、CLI `auth`が欠落する場合、frozen installでlockfile hashが変化する場合はMarkets Task 1のtest／commitへ進まない。`ignored-builds`に表示されたdependencyはその場でallowlistへ追加しない。

Markets子計画Task 17の`@playwright/test`「追加」は、Task 3のgreenfield shellで既に追加・lock済みのexact `1.61.1`を維持・検証する意味とする。Task 17で新規dependencyとして追加しない。欠落している場合はTask 17で補わずTask 3のpackage boundaryへ戻り、rootの72時間cooldownを使うscoped no-frozen/frozen installとlockfile commitをやり直す。

Marketsの即時購入はDEC-264に従い、pure reducerのholdで完了扱いにしない。HTTP commandの同じD1 transactionで指定数量を全量lockし、deterministic `BUY_NOW` settlement plan／outboxを作って通常精算と同じreservation／capture Workflowを即時開始する。proof非依存の`restoreBuyNowHold`は、外部作用前preflight、同じidempotency keyの決定的reservation拒否でID 0件、または全reservation未capture＋ACTIVE分release完了という相互排他的なevidenceだけで全数量を`FAILED_RESTORED`へ進め、結果不明ではholdを維持してmanual actionとする。capture後はproof migration適用後の`settleBuyNowHold`で`CAPTURED_PENDING_FINALIZE`からrestore／release／refundせずimmutable proofへforward finalizeする。endAt時の未終端holdは終了時planを遅延し、全hold終端後の復元済み残数で収束する。retry、crash、複数hold順序、endAt競合、capture境界をWorker／Workflow／E2Eで固定する。

- [ ] **Step 5: 全子計画cross gateを実行する**

```bash
pnpm contract:web-app:check
pnpm --filter @freeism/points-web-app check
pnpm --filter @freeism/points-web-app test
pnpm --filter @freeism/points-web-app test:worker
pnpm --filter @freeism/markets-web-app check
pnpm --filter @freeism/markets-web-app test
pnpm --filter @freeism/markets-web-app test:worker
```

Expected: 全PASS。Pointsはopaque user token／standard introspection／M2M分離、Marketsは即時購入lock→BUY_NOW settlement→capture境界、両appはgenerated contractだけで接続する。失敗をcompatibility shimや独自認証で隠さない。

## Task 7: Rollback/cutover/decommission runbooks を先に完成させる

**Files:**

- Create: `plan/web-app/runbooks/deployment-and-rollback.md`, `plan/web-app/runbooks/legacy-decommission.md`, `plan/web-app/runbooks/observability-and-alerts.md`, evidence files
- Create: `tests/web-app/release-gates.test.mjs`, `scripts/web-app/assert-production-release.mjs`

- [ ] **Step 1: failing release-gate test とRC拒否の期待成功wrapperを作る**

test は3 runbook／evidence fileの存在と必須 checklistを検査する。observability runbookは両appのbinding、threshold、5分monitor、180日cleanup、staging drill command、production drill拒否を欠けば失敗させる。production gate scriptは両appの`better-auth`／`@better-auth/drizzle-adapter`／standalone CLI `auth`、およびPointsだけの`@better-auth/oauth-provider`が正式`1.7.0` exactでない場合、またはMarketsが未使用Provider packageを宣言した場合にstable code `BETTER_AUTH_FINAL_REQUIRED`をstderrへ出して非0終了する。

`assert-production-release.mjs --mode assert`はpre-merge／local用で、RC manifestをstable code付き非0として拒否する。`--mode workflow --github-output <path>`はmain workflow専用とし、exact RCで得た`BETTER_AUTH_FINAL_REQUIRED`だけを`eligible=false`／`reason=BETTER_AUTH_FINAL_REQUIRED`としてoutputへ書いてexit 0に変換する。正式`1.7.0`では`STAGING_LIVE_RESULT`を必須にし、同じGit SHA／versionとDEC-263の全boolean／result hashを照合後だけ`eligible=true`でexit 0にする。正式版で`eligible=false`、live result欠落／不一致、未知code、その他assertion failureはoutputを成功扱いせず非0終了する。

`release-gates.test.mjs`はRC manifest fixtureでassert modeをchild process実行し、非0終了とstable codeが一致した場合をtest PASSとしてassertする。さらに一時`GITHUB_OUTPUT`相当fileを使い、workflow modeのRCが`eligible=false`でexit 0、正式版＋一致live resultが`eligible=true`でexit 0、正式版のlive result欠落／SHA不一致と未知errorが非0になることを検査する。raw production assertionを「Expected failure」として直接実行する手順にはしない。

```bash
node --test tests/web-app/release-gates.test.mjs
```

Expected: 最初はrunbook／wrapper不在でtest FAIL。実装後はtest自体がPASSし、その中で開発中`1.7.0-rc.1`がproductionから拒否され、正式版fixtureだけが許可される。

- [ ] **Step 2: deployment/rollback runbook を実装する**

最低限、次を exact command と判断表で記載する。

- deploy 前の `wrangler deployments list` と D1 Time Travel current bookmark 保存
- code/assets-only failure は `wrangler rollback "$PREVIOUS_VERSION_ID" --name points-worker-production --message "v0.2 rollback"` 相当で直前 version へ戻す
- DO migration、binding deletion、非互換 schema を跨ぐ Worker rollback は禁止
- D1 migration は自動 down migration しない
- 経済状態破損時だけ traffic 停止後、`wrangler d1 time-travel info` と `wrangler d1 time-travel restore --bookmark` を二者確認で実行
- restore が返す `previous_bookmark` を必ず保存し、undo 経路を残す
- Points production 成功/Markets production 失敗時は contract-compatible な旧 Markets を維持し、Points を自動 rollback しない
- capture 後は refund/release せず、Markets forward finalize/reconciliation を再開する
- `BUY_NOW`は外部作用前、決定的reservation拒否でID 0件、または全reservation未capture＋ACTIVE分release完了のevidenceだけで全quantity lockをrestoreし、結果不明はhold維持、capture後はrestoreしない。複数holdとendAt cutoff barrierを含め、未終端hold 0での完売close／endAt前の残数あり`OPEN`／endAt後の終了時plan判定をreconciliationから再実行しても同じ結果へ収束させる

Expected: 状態 rollback と code rollback が別手順であり、「失敗したら常に D1 restore」になっていない。

- [ ] **Step 3: acceptance evidence template を実装する**

staging/production evidence は timestamp、Git SHA、Worker version ID、D1 migration version/bookmark、test command/run URL、reconciliation result、runtime call inventory を持つ。token、secret、CSV/HTML本文は記録しない。

- [ ] **Step 4: observability／alert runbookを実装する**

[セキュリティ仕様12.1](../../docs/web-app/v0.2/security-and-delivery.md#121-observabilityと運用alert)を正本として、次をexact binding名、query、判断表で記載する。

- Points／Markets、staging／production別のWorkers Logs／Traces samplingと7日保持確認、`OPS_METRICS` dataset、`OPS_ALERT_EMAIL` verified destination
- structured log／Analytics Engineの許可field、salted resource ID hash、credential／個人情報redaction、correlation IDからWorkers LogsとSQL APIを照合するquery
- app別全alertのOPEN／RESOLVED条件、同key dedupe、1時間継続通知、送信失敗再送、Cloudflare native runtime error／5xx／usage alertとの責務分離
- `OPEN`非削除、`RESOLVED`の180日境界、1日1回cleanup lease、append-only cleanup auditの確認query
- Pointsは`pnpm --filter @freeism/points-web-app drill:ops-alert:staging`、Marketsは`node projects/markets-web-app/scripts/drill-ops-alert.mjs staging`を、各staging deploy／smoke後に実行する。両scriptのproduction入力拒否と、全fixtureのOPEN→同key dedupe→RESOLVED、D1／log／metric／test Email receiptをevidenceへ保存する
- monitor／Email／Analytics Engineの一部障害時もdomain transactionを再実行しないincident triage、未通知rowの再送、reconciliationへの遷移

- [ ] **Step 5: legacy decommission runbook の read-before-delete gate を実装する**

削除前に Vercel/Supabase/Upstash/旧 R2 の list/get API response を inventory し、Cloudflare production smoke と runtime call 0 の evidence link を要求する。削除 API は main deploy workflow に入れない。

- [ ] **Step 6: commit**

```bash
git add -- plan/web-app/runbooks plan/web-app/evidence tests/web-app/release-gates.test.mjs scripts/web-app/assert-production-release.mjs
git commit -m "docs(web-app): add release and recovery runbooks"
```

## Task 8: PR/merge queue CI と main ruleset payload

**Files:** `.github/workflows/web-app/ci.yml`, `infra/github/main-ruleset.json`, root scripts/package。

- [ ] **Step 1: CI contract を failing local test にする**

`tests/web-app/spec-boundaries.test.mjs` で workflow YAML text/structure を最低限検査する。

- canonical docs/child plans の相対 local Markdown link が0 broken
- `pull_request` と `merge_group`
- workflow-level `paths` filter なし。unrelated PR でも固定 required job が成功して返る
- `pull_request_target` なし
- mutable `uses: ...@v*` なし
- production secret 名なし
- required aggregator job 名 `web-app / required`
- old `projects/web-app` path なし

```bash
node --test tests/web-app/spec-boundaries.test.mjs
```

Expected: 現行 CI の old path/mutable action で FAIL。

- [ ] **Step 2: CI workflow を置換する**

Actionはexact SHA tableを使う。`setup-vp`は`version: "0.2.4"`, `node-version-file: .node-version`, `run-install: false`とし、ephemeral runner上でNode／package manager setupを完了させる。Corepackの存在を前提にせず、`corepack enable`／`corepack prepare`を実行しない。install前に次のguardをworkflowへ置く。

```bash
test "$(node --version)" = "v24.18.0"
command -v pnpm
test "$(pnpm --version)" = "10.33.3"
pnpm --filter freeism --filter @freeism/points-web-app --filter @freeism/markets-web-app install --frozen-lockfile
```

Expected: Node／pnpm exact guardとfrozen installがPASSする。dependency追加時の72時間cooldownはroot設定を使う標準no-frozen installでlockfile生成時に適用済みとする。setup後もpnpmが不在／相違する場合はCIをfailさせ、workflow内でglobal installへfallbackしない。Terraform jobはpinned `hashicorp/setup-terraform`で`terraform_version: "1.15.7"`をephemeral runnerへ用意し、最初のTerraform command前にTask 6と同じ`terraform version -json` guardを通す。

jobs:

1. `toolchain-contract`: root tests、OpenAPI generated diff
2. `points`: check/unit/Workers/build routing assertion
3. `markets`: check/unit/Workers/build routing assertion
4. `terraform`: fmt/validate/test（remote credential/apply なし）
5. `web-app / required`: `if: always()` で全 needs の成功を集約

CI workflow は `pull_request` / `merge_group` で常時起動し、workflow-level `paths` を置かない。対象外変更を軽量成功へする場合も `web-app / required` を必ず生成する。deploy job/cache/artifact と production secret は CI に置かない。`merge_group` でも required aggregator が同名で走る。

- [ ] **Step 3: main ruleset payload を作る**

`infra/github/main-ruleset.json` は default branch を対象に次を宣言する。

- enforcement active、bypass actor 0
- deletion/non-fast-forward/direct update 禁止
- PR 必須、approval 0、last-push approval 不要（一人運用中）
- strict required check `web-app / required`
- merge queue 必須、branch up-to-date
- admin bypass なし

二人目の maintainer 追加時は別 PR で approval を1へ変更する。

- [ ] **Step 4: local/PR CI を pass させる**

```bash
node --test tests/web-app/spec-boundaries.test.mjs
pnpm contract:web-app:check
terraform fmt -check -recursive infra/cloudflare
```

Expected: 全 PASS。

- [ ] **Step 5: workflow／payloadを先にcommitする**

```bash
git add -- .github/workflows/web-app/ci.yml infra/github/main-ruleset.json tests/web-app/spec-boundaries.test.mjs package.json pnpm-lock.yaml
git commit -m "ci(web-app): enforce v0.2 merge gates"
```

- [ ] **Step 6: branchをpushし、実際のcheck名を確認する**

```bash
git push -u origin codex/web-app-v0.2-implementation
gh pr create --base main --head codex/web-app-v0.2-implementation --title "ci(web-app): enforce v0.2 merge gates" --body "v0.2 implementationとmerge gateの差分・検証結果をレビューしてください。"
gh pr checks --watch
```

既存PRがある場合だけ、base=`main`／head=`codex/web-app-v0.2-implementation`が同一であることを`gh pr view`で確認して`gh pr create`を省略する。PR作成成功後に`pull_request` runのcheck一覧を取得する。Expected: GitHub上の実check名がexact `web-app / required`でgreen。同名が観測できない、job名にworkflow prefixが追加される、または`merge_group`で同名が生成されない構造ならrulesetを適用せずworkflow／local contractを修正して再pushする。

- [ ] **Step 7: 実check確認後だけGitHub APIでrulesetを適用する**

```bash
gh api repos/{owner}/{repo}/rulesets
gh api --method POST repos/{owner}/{repo}/rulesets --input infra/github/main-ruleset.json
gh api repos/{owner}/{repo}/rulesets
```

既存rulesetがある場合はPOSTで重複作成せず、IDを取得してPATCHする。Expected: active ruleset 1件、bypass 0、required checkがStep 6で観測したexact `web-app / required`。この外部mutationはrepository ownerがpayload diffを確認してから実行する。

- [ ] **Step 8: 同じPRをmerge queueへ入れる**

ruleset適用後、同じPRをmerge queueへ入れ、`merge_group`でも`web-app / required`がgreenになることを確認してmergeする。直接push、admin bypass、ruleset一時無効化で`main`へ入れない。Task 9はこのmerge完了後、最新`main`から新しいbranchで開始する。

## Task 9: `main` push staging → production workflow

**Files:** `.github/workflows/web-app/deploy.yml`, release gate scripts/tests。

Task 8のCI／ruleset PRがmerge済みの最新`main`から`codex/web-app-v0.2-deploy-workflow` branchを作って実施する。`main`上で編集・commit・pushしない。

- [ ] **Step 1: workflow の failing assertion を追加する**

test は次を検査する。

- trigger は `push.branches: [main]` だけ。`workflow_dispatch`/schedule/pull_request なし
- paths は Points/Markets runtime、canonical cross docs、IaC、root lock、workflowだけでunrelated projectを含めず、`plan/web-app/evidence/**`のevidence-only changeを明示除外する
- concurrency `cancel-in-progress: false`
- staging → staging E2E → production の needs 順
- staging acceptanceがE2E前にexact Playwrightから`install --with-deps chromium`を実行する
- production の前に `assert-production-release.mjs`
- `assert-production-release.mjs` がbilingual review manifestの8 source hashを現在のcanonical Markdownと照合する
- `staging-acceptance`がsecret-freeなBetter Auth live resultをjob outputへ出し、`production-gate`がartifact downloadなしで同じSHA／versionを照合する
- RCのstable `BETTER_AUTH_FINAL_REQUIRED`だけをworkflow wrapperが`eligible=false`へ変換し、production jobsは`eligible == 'true'`の場合だけ起動する。正式版のfalse／欠落と未知errorはworkflow failureにする
- production job だけが production secrets を参照
- deploy workflow で cache restore/artifact download をしない
- Corepack commandがなく、各frozen install前にNode `24.18.0`／pnpm `10.33.3`をguardする
- Terraform jobがpinned setup action後、最初の使用前にCLI `1.15.7`をguardする
- `CLOUDFLARE_ENV` は build step に設定し、deploy stepの `--env` に依存しない
- staging/production GitHub Environment に required reviewer/wait timer がなく、manual approval を生じない

```bash
node --test tests/web-app/release-gates.test.mjs
```

Expected: deploy workflow 不在で FAIL。

- [ ] **Step 2: workflow jobs を exact sequence で実装する**

1. `validate`
2. `points-staging`: D1 migration → `build:staging` → build assertion → generated config deploy
3. `markets-staging`: D1 migration → `build:staging` → assertion → deploy
4. `edge-staging`: Terraform staging workspace apply
5. `staging-acceptance`: Access Service Token付き browser E2E、Service Binding contract、settlement/reconciliation、Points／Markets observability drill、legacy runtime call 0
6. `production-gate`: Better Auth final、standard opaque／introspection feasibility evidence、固定ページbilingual review／8 source hash、Paid/Time Travel、ruleset、advisory、staging evidence
7. `points-production`: bookmark → forward migration → `build:production` → assertion → deploy
8. `markets-production`: bookmark → forward migration → `build:production` → assertion → deploy
9. `edge-production`: Terraform production workspace apply
10. `production-smoke`: custom domain/TLS/assets/API/OAuth env isolation/reconciliation/runtime call 0

Points staging/production は Markets より先にする。各 deploy は同じ job 内で build の直後に実行し、別 job artifact を再利用しない。Task 4Aでrequiredとしたschemaをv0.2初期contractとし、初回deploy後のPoints OpenAPI変更だけをexpand-firstにするため、Pointsだけ先に更新された時間帯も同じv0.2の直前Marketsから呼べる。

各Node jobはTask 8と同じpinned `setup-vp`後にNode／pnpm exact guard、frozen installの順で実行し、Corepack／global installへfallbackしない。dependency追加時の72時間cooldownはroot設定を使う標準no-frozen installでlockfileへ反映済みとする。`edge-*` jobはpinned `setup-terraform`で`1.15.7`を指定し、`terraform version -json`がexact一致してからinit／workspace／applyへ進む。

`staging-acceptance`はlockfile install後、E2E前に`pnpm --filter @freeism/markets-web-app exec playwright install --with-deps chromium`を実行する。Points／Marketsはexact `@playwright/test@1.61.1`と同じGitHub runnerのbrowser cacheを使い、mutable browser artifactや別jobからdownloadしたartifactを再利用しない。E2E／reconciliation成功後に`pnpm --filter @freeism/points-web-app drill:ops-alert:staging`と`node projects/markets-web-app/scripts/drill-ops-alert.mjs staging`を実行し、両方のevidenceが揃うまでproduction gateへ進めない。drill scriptへproduction credentialを渡さない。

stagingへdeployしたexact Better Auth versionで`verify-better-auth-oauth-feasibility.ts`と`verify-google-fresh-live.ts`も再実行する。`staging-acceptance`の`auth-live` stepは、`version`、`gitSha`、`resultHash`、opaque user／M2M、standard introspection、pairwise subject、ID Token非Bearer、grant分離のbooleanだけを1行JSONにし、`$GITHUB_OUTPUT`へ`result=<json>`として書く。Access／Refresh／Client Credentials Token、code、Cookie、secret、raw pairwise `sub`は含めない。jobは`better-auth-live-result: ${{ steps.auth-live.outputs.result }}`として公開する。

`production-gate`は`needs.staging-acceptance.outputs.better-auth-live-result`を`STAGING_LIVE_RESULT`へ渡し、`node scripts/web-app/assert-production-release.mjs --mode workflow --github-output "$GITHUB_OUTPUT"`を実行する。正式版bump PRに含まれる`artifacts/web-app/better-auth-oauth-feasibility/1.7.0.json`とlive resultのGit SHA／`1.7.0`／result hash／全booleanを照合し、job outputに`eligible`を公開する。Points／Markets／edge productionとproduction smokeは`if: needs.production-gate.outputs.eligible == 'true'`を必須にする。RCではproduction-gate job自体はgreenの`eligible=false`、production jobsはskippedとなり、正式版でfalse／欠落／不一致ならworkflowをfailさせる。RC evidenceを正式版evidenceとして再利用せず、GitHub artifact upload/downloadでjob間受渡ししない。

- [ ] **Step 3: secrets と permissions を最小化する**

staging と production は別 Cloudflare token/R2 backend credential/OAuth test credential を使う。staging jobs は GitHub Environment `web-app-staging`、production jobs は `web-app-production` を指定するが、両 Environment に required reviewer/wait timer を設定しない。production secrets は production jobs の `env` にだけ渡す。workflow top-level env に secret を置かない。`permissions: contents: read` を基本に、不要な `id-token: write` は付けない。

staging acceptance の Access header は `CF-Access-Client-Id` / `CF-Access-Client-Secret` とし、値は `CF_ACCESS_CLIENT_ID_STAGING` / `CF_ACCESS_CLIENT_SECRET_STAGING` から注入する。productionへ同 credentialを渡さない。

`edge-staging` は `terraform workspace select staging`、`edge-production` は `terraform workspace select production` を明示し、対応する tfvars と `-lock-timeout=5m -auto-approve` を使う。workspace/environment mismatch check と Remote R2 `.tflock` のどちらかが失敗したら deploy を停止する。

- [ ] **Step 4: local structure test を pass させる**

```bash
node --test tests/web-app/release-gates.test.mjs
git grep -n "workflow_dispatch\|pull_request_target" -- .github/workflows/web-app
```

Expected: test PASS、grep 0件。

- [ ] **Step 5: commit**

```bash
git add -- .github/workflows/web-app/deploy.yml tests/web-app/release-gates.test.mjs scripts/web-app/assert-production-release.mjs
git commit -m "ci(web-app): deploy main through staging to production"
```

- [ ] **Step 6: branchをpushし、PR／merge queue経由でworkflowをmainへ入れる**

```bash
git push -u origin codex/web-app-v0.2-deploy-workflow
gh pr create --base main --head codex/web-app-v0.2-deploy-workflow --title "ci(web-app): deploy main through staging to production" --body "main deploy workflowの差分・RC拒否条件・検証結果をレビューしてください。"
gh pr checks --watch
```

PR作成後、actual required check `web-app / required`が`pull_request`と`merge_group`の双方でgreenであることを確認してmerge queueでmergeする。既存PRがある場合だけ同じbase/headを確認してcreateを省略する。direct main push／admin bypassを使わない。このmergeによる`main` pushではstaging sequenceを実際に起動・監視し、RC manifestに対してproduction-gateがgreenの`eligible=false`、production deploy jobsがskipped、workflow全体がgreenになることを確認する。Task 10はこのrunを観測してから開始する。

## Task 10: Repository cutover と旧 runtime/workflow 削除

**Files:** File map の Modify/Delete 全項目、`scripts/web-app/assert-legacy-removed.mjs`, `scripts/web-app/check-staged.mjs`。

この Task は両子計画、cross tests、runbook、CI/deploy workflow がgreenで、Task 9のworkflow PRとRC拒否runを確認した後だけ実施する。最新`origin/main`から`codex/web-app-v0.2-cutover` branchを作り、`main`上で編集・commit・pushしない。

```bash
git fetch origin
git switch -c codex/web-app-v0.2-cutover origin/main
```

- [ ] **Step 1: failing legacy-removal test を作る**

script は source/package/workflow/root config を走査し、次を fail にする。

- `projects/web-app` runtime path
- `next`, `next-auth`, Prisma generator/client, Supabase/Upstash/Vercel runtime package
- SSE endpoint/content-type
- old cron/scheduled action workflow
- old `.next`, Vercel deploy/cache/env names

canonical docs の「廃止」「履歴」と archive は scan 対象外にし、情報保持文書を誤って削除しない。

```bash
node scripts/web-app/assert-legacy-removed.mjs
```

Expected: old project/workflow/package を列挙して FAIL。

- [ ] **Step 2: old workflow と setup action を先に削除する**

File map の旧10 workflowと `.github/actions/web-app-setup/` を削除する。これにより final merge で Vercel/旧 cron が再実行されない。

- [ ] **Step 3: old monolith を削除する**

`projects/web-app/` 全体を削除する。v0.1/v0.2/v0.3 docs と旧 plan は既に各 docs/archive/plan へ移動済みであることを `doc-migration-manifest.md` 39/39 inventory で再確認してから削除する。データ export/migration は行わない。

- [ ] **Step 4: root references を2 appへ更新する**

- root README/docs tree/table
- CODEOWNERS
- issue/feature/PR templates
- root `lint:web-app` と check scripts
- `.gitignore`
- `.vscode/settings.json`: `typescript.tsdk` を root `node_modules/typescript/lib` へ変更し、単一旧appを指す `vitest.rootConfig` を削除
- root Husky hook

`check-staged.mjs` は staged path が Points/Markets の時だけ当該 app の `vp check` を実行し、documentation 等の既存 hook を保持する。旧 app の project-local Husky を再作成しない。旧 `mise.toml` は削除し、`docs/README.ja.md` の `mise trust` / `mise run init` を `.node-version` + Vite+ + pnpm の初期化手順へ同時に置換する。`.gitignore` は両 app の `dist`, `coverage`, `.wrangler`, `.dev.vars*`, `.env*` を無視するが、`.dev.vars.example` は追跡する。

- [ ] **Step 5: lockfile を再生成し、legacy test を green にする**

```bash
pnpm --filter freeism --filter @freeism/points-web-app --filter @freeism/markets-web-app install --no-frozen-lockfile
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
node scripts/web-app/assert-legacy-removed.mjs
pnpm --filter freeism --filter @freeism/points-web-app --filter @freeism/markets-web-app install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
pnpm ignored-builds
pnpm contract:web-app:check
pnpm audit --prod --audit-level high
pnpm licenses list --prod --json
```

Expected: rootの`minimumReleaseAge: 4320`を使う標準install、legacy test、frozen install、generated diffがPASSし、old runtime dependenciesがlockfile reachable graphにない。72時間cooldownで拒否された場合は除外せず時間経過後に再実行する。`ignored-builds`に表示されたdependencyは許可せず個別reviewする。旧app削除後のproduction dependenciesだけを標準audit/license commandで検査し、root dev-only toolchainは混ぜない。独自parserやadvisory除外は追加しない。

- [ ] **Step 6: repository-wide verification**

```bash
pnpm lint
pnpm --filter @freeism/points-web-app check
pnpm --filter @freeism/points-web-app test
pnpm --filter @freeism/points-web-app test:worker
pnpm --filter @freeism/markets-web-app check
pnpm --filter @freeism/markets-web-app test
pnpm --filter @freeism/markets-web-app test:worker
git diff --check
```

Expected: 全 PASS。repository に既存 baseline failure がある場合は command と unrelated path を分離して記録し、web-app failure として隠さない。

- [ ] **Step 7: commit**

```bash
git add -- projects/web-app .github/actions/web-app-setup .github/workflows/web-app README.md docs/README.ja.md docs/web-app .github/CODEOWNERS .github/ISSUE_TEMPLATE .github/PULL_REQUEST_TEMPLATE package.json pnpm-lock.yaml .gitignore .husky .vscode/settings.json mise.toml scripts/web-app
git commit -m "refactor(web-app): cut over to Points and Markets"
```

- [ ] **Step 8: cutover PRをmerge queueへ通す**

```bash
git push -u origin codex/web-app-v0.2-cutover
gh pr create --base main --head codex/web-app-v0.2-cutover --title "refactor(web-app): cut over to Points and Markets" --body "旧runtime削除とPoints/Markets cutoverの差分・検証結果をレビューしてください。"
gh pr checks --watch
```

PR作成後、`web-app / required`を`pull_request`／`merge_group`で通してmerge queueから`main`へmergeする。既存PRがある場合だけ同じbase/headを確認してcreateを省略する。Task 9のmain workflowがstaging cutoverをgreenにし、RCのためproductionを引き続きblocked／skippedにすることを確認する。Task 11はこのTask 10 merge済みの最新`main`だけをbaseにする。

## Task 11: Better Auth final gate と first `main` release

**Files:** child app package manifests/lockfile/auth regression tests、`projects/points-web-app/src/backend/infrastructure/db/schema/auth.ts`、`projects/points-web-app/drizzle/`、`projects/markets-web-app/src/backend/db/schema/auth.ts`、`projects/markets-web-app/drizzle/`、evidence files、`plan/web-app/runbooks/observability-and-alerts.md`。auth implementation details は子計画を参照。

Task 10までがmerge済みの最新`main`から`codex/web-app-v0.2-better-auth-final` branchを作る。正式版bumpをcommit／PR／mergeする前にproduction acceptanceを始めず、`main`を直接更新しない。

- [ ] **Step 1: Better Auth `1.7.0` 正式 publish を確認する**

```bash
env npm_config_cache=/private/tmp/freeism-npm-cache pnpm view better-auth@1.7.0 version dist.integrity
env npm_config_cache=/private/tmp/freeism-npm-cache pnpm view @better-auth/drizzle-adapter@1.7.0 version dist.integrity
env npm_config_cache=/private/tmp/freeism-npm-cache pnpm view @better-auth/oauth-provider@1.7.0 version dist.integrity
env npm_config_cache=/private/tmp/freeism-npm-cache pnpm view auth@1.7.0 version dist.integrity
```

Expected: 4 packageが正式releaseとして存在しintegrityが返る。未公開ならproduction releaseはblockedのままにし、RCを本番へ出さない。このread-only確認だけでは72時間経過を仮定しない。

- [ ] **Step 2: release branchでmanifestだけをexact bumpする**

両appの`better-auth`、`@better-auth/drizzle-adapter`、standalone CLI `auth`を`1.7.0`へ同時にbumpし、Pointsだけの`@better-auth/oauth-provider`も`1.7.0`へbumpする。MarketsへProvider packageを追加しない。この時点ではinstallもcommitも行わない。`minimumReleaseAgeExclude`を追加せず、RCと正式版を混在させない。

- [ ] **Step 3: 標準72時間cooldownでlockfile／schemaを更新・reviewする**

```bash
pnpm --filter freeism --filter @freeism/points-web-app --filter @freeism/markets-web-app install --no-frozen-lockfile
lock_hash_before="$(shasum -a 256 pnpm-lock.yaml)"
pnpm --filter freeism --filter @freeism/points-web-app --filter @freeism/markets-web-app install --frozen-lockfile
lock_hash_after="$(shasum -a 256 pnpm-lock.yaml)"
test "$lock_hash_before" = "$lock_hash_after"
pnpm ignored-builds
pnpm --filter @freeism/points-web-app exec auth generate --config ./src/backend/auth/auth-cli.ts --adapter drizzle --dialect sqlite --output ./src/backend/infrastructure/db/schema/auth.ts --yes
pnpm --filter @freeism/points-web-app db:generate --name=points-auth
pnpm --filter @freeism/markets-web-app exec auth generate --config ./src/backend/auth/auth-cli.ts --adapter drizzle --dialect sqlite --output ./src/backend/db/schema/auth.ts --yes
pnpm --filter @freeism/markets-web-app db:generate --name=markets-auth
```

Expected: rootの`minimumReleaseAge: 4320`を使う標準installが、両appのruntime package、PointsだけのProvider package、両appのCLI `auth`へ72時間cooldownを適用する。Points／Markets importerとlockfileは正式`1.7.0`だけを参照し、Marketsに`@better-auth/oauth-provider`がなく、frozen installでhash不変。72時間未達なら設定で除外せず待って再実行する。`ignored-builds`に表示されたdependencyもその場でallowlistへ追加しない。standalone CLI `auth`で両appのschema sourceを再生成した後、Drizzle migrationも再生成し、Provider、explicit link、opaque token、standard introspection、pairwise subject、Client Credentials分離、encryption behaviorのrelease note／source／schema／migration diffをreviewする。予期しない破壊的schema差分はcommitせず停止する。

- [ ] **Step 4: full auth regression と production assertion**

```bash
pnpm --filter @freeism/points-web-app test -- test/contract/better-auth-oauth-feasibility.contract.test.ts
pnpm --filter @freeism/points-web-app exec tsx scripts/verify-better-auth-oauth-feasibility.ts
pnpm --filter @freeism/points-web-app test
pnpm --filter @freeism/points-web-app test:worker
pnpm --filter @freeism/markets-web-app test
pnpm --filter @freeism/markets-web-app test:worker
pnpm audit --prod --audit-level high
pnpm licenses list --prod --json
node scripts/web-app/assert-production-release.mjs --mode assert
```

Expected: feasibility scriptが正式`1.7.0`で`artifacts/web-app/better-auth-oauth-feasibility/1.7.0.json`をsecretなしで新規生成し、そのartifactを使うproduction assertionがPASSする。標準audit/license commandは旧app削除後のproduction dependenciesだけを対象にし、root dev-only toolchainや独自parserを混ぜない。Google/GitHub login/link、implicit email link拒否、Google fresh、opaque user／M2M Access Token、Markets標準introspectionによるpairwise subject照合、ID Token bearer拒否、M2M-only Client Credentials、token encryption/refresh rotation、Markets Google/explicit Points linkの全回帰もPASS。RC artifactのrename／再利用は禁止する。正式版で標準機能が成立しない場合も独自認証へfallbackせずreleaseを停止する。

- [ ] **Step 5: 正式版bumpをproduction前にcommitする**

```bash
git add -- projects/points-web-app/package.json projects/markets-web-app/package.json pnpm-lock.yaml projects/points-web-app/src/backend/infrastructure/db/schema/auth.ts projects/points-web-app/drizzle projects/markets-web-app/src/backend/db/schema/auth.ts projects/markets-web-app/drizzle projects/points-web-app/test projects/markets-web-app/test scripts/web-app/assert-production-release.mjs artifacts/web-app/better-auth-oauth-feasibility/1.7.0.json
git diff --cached --check
git commit -m "build(web-app): promote better auth 1.7 final"
```

Expected: package／lockfile／実際に生じたschema migration／auth regressionだけを含む。evidence templateやproduction結果をこのcommitへ先書きしない。

- [ ] **Step 6: 正式版bump PRをmerge queueへ通す**

```bash
git push -u origin codex/web-app-v0.2-better-auth-final
gh pr create --base main --head codex/web-app-v0.2-better-auth-final --title "build(web-app): promote better auth 1.7 final" --body "Better Auth正式版bump、schema/migration、auth regression、feasibility evidenceをレビューしてください。"
gh pr checks --watch
```

PR作成後に`web-app / required`がgreen、ruleset activeを確認し、merge queue経由で`main`へ入れる。既存PRがある場合だけ同じbase/headを確認してcreateを省略する。direct push／admin bypassを使わない。このmergeによる`main` pushだけがfirst production workflowを起動する。

- [ ] **Step 7: main workflow を最後まで監視する**

Expected sequence:

```text
validate
  -> Points staging
  -> Markets staging
  -> edge staging
  -> staging acceptance/reconciliation
  -> production gate
  -> Points production
  -> Markets production
  -> edge production
  -> production smoke
```

staging failure は production job を `skipped` にする。run を rerun する場合も同じ Git SHA を使い、同じ D1 migration/idempotency key で収束する。

staging acceptanceでは[セキュリティ仕様12.1](../../docs/web-app/v0.2/security-and-delivery.md#121-observabilityと運用alert)に従い、Points ownership lag／reconciliation mismatch、Markets Auction transition／Workflow stuck、共通alert delivery failureをfixtureでOPEN→同key dedupe→RESOLVEDへ進める。Workers Logsのstructured field、Analytics Engine SQL APIのdata point、専用test destinationへのEmailを確認する。production deploy前にnative Worker error／5xx／usage policyのdelivery testも完了させ、productionへのsynthetic business alertは送らない。

- [ ] **Step 8: production後に新しいevidence branch／PRを作る**

production smoke成功後、最新`origin/main`から新しい`codex/web-app-v0.2-production-evidence` branchを作る。正式版bump branchを再利用せず、`main`で直接編集しない。

```bash
git fetch origin
git switch -c codex/web-app-v0.2-production-evidence origin/main
```

`staging-acceptance.md`／`production-acceptance.md`へGit SHA、workflow URL、version ID、bookmark、migration version、opaque/introspection live result、BUY_NOW capture境界、reconciliation、legacy runtime call 0を記録する。token、secret、pairwise subject実値、個人情報を記録しない。

- [ ] **Step 9: evidenceだけをcommit／PR／merge queueへ通す**

```bash
git add -- plan/web-app/evidence/staging-acceptance.md plan/web-app/evidence/production-acceptance.md
git commit -m "chore(web-app): record v0.2 production acceptance"
git push -u origin codex/web-app-v0.2-production-evidence
gh pr create --base main --head codex/web-app-v0.2-production-evidence --title "chore(web-app): record v0.2 production acceptance" --body "production後のsecret-free acceptance evidenceだけをレビューしてください。"
gh pr checks --watch
```

PR作成後に`web-app / required`を通してmerge queueでmergeする。既存PRがある場合だけ同じbase/headを確認してcreateを省略する。workflow path filterはevidence-only changeをdeploy対象に含めないため、この`main` pushで再deployしないことも確認する。package manifest／lockfileをevidence PRへ含めない。

## Task 12: Vercel/Supabase/Upstash/旧画像 R2 を別 checkpoint で撤去する

**Files:** `plan/web-app/evidence/decommission-v0.2.md`。通常 deploy workflow は編集しない。

production acceptance evidence PRがmerge済みの最新`origin/main`から`codex/web-app-v0.2-decommission-evidence` branchを作ってから実施する。これは不可逆な外部mutationであり、branch作成はDELETEの承認を意味しない。自動化agentは対象ID/domainのinventoryとowner確認なしにDELETEを実行せず、`main`上でevidenceを編集・commit・pushしない。

```bash
git fetch origin
git switch -c codex/web-app-v0.2-decommission-evidence origin/main
```

- [ ] **Step 1: runtime call 0 を再確認する**

Cloudflare Worker logs、GitHub code search、deployed asset/source map、repository secrets/workflows を確認し、Vercel/Supabase/Upstash endpoint/key の runtime call が0であることを evidence に記録する。

- [ ] **Step 2: Vercel を GET → domain detach → project delete**

必要環境変数: `VERCEL_TOKEN`, `VERCEL_PROJECT_ID`, 任意の `VERCEL_TEAM_ID`。

```bash
curl --fail-with-body --silent --show-error -H "Authorization: Bearer $VERCEL_TOKEN" "https://api.vercel.com/v9/projects/$VERCEL_PROJECT_ID"
curl --fail-with-body --silent --show-error -H "Authorization: Bearer $VERCEL_TOKEN" "https://api.vercel.com/v9/projects/$VERCEL_PROJECT_ID/domains"
curl --fail-with-body --silent --show-error -H "Authorization: Bearer $VERCEL_TOKEN" "https://api.vercel.com/v9/projects/$VERCEL_PROJECT_ID/env"
```

inventory した Freeism domain だけを `DELETE /v9/projects/{idOrName}/domains/{domain}` で detach し、Cloudflare DNS/TLS が active のままか再 smoke する。その後 `DELETE /v9/projects/{idOrName}`。最後の GET は `404` が expected。team-owned resource は全 request に `teamId` を付ける。

- [ ] **Step 3: Supabase test/production project を GET → DELETE**

必要環境変数: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_TEST_PROJECT_REF`, `SUPABASE_PRODUCTION_PROJECT_REF`。

各 ref に対して先に `GET https://api.supabase.com/v1/projects/{ref}` を実行し、name/ref/status を inventory と一致させる。データ migration/export は行わないことを再確認後、`DELETE /v1/projects/{ref}` を実行する。fine-grained token は `project_admin_write` を持つ必要がある。削除後 GET `404` を記録する。

- [ ] **Step 4: Upstash Redis を GET/list → DELETE**

必要環境変数: `UPSTASH_EMAIL`, `UPSTASH_API_KEY`, `UPSTASH_REDIS_DATABASE_ID`。Basic auth は `username:password` 形式で、credential を command output/evidence に出さない。

対象 DB ID/name/endpoint を read API で inventory し、`DELETE https://api.upstash.com/v2/redis/database/{id}` の response `"OK"` を確認する。削除後は SSE/Redis env/secret を GitHub/Vercel/Cloudflare から撤去する。

- [ ] **Step 5: 旧画像 R2 と旧 credentials を削除する**

旧 Vercel env から inventory した legacy image bucket だけを削除する。新しい `freeism-terraform-state` bucket と D1 Time Travel は削除しない。bucket empty/ownership を確認し、旧 R2 access key を revoke する。

- [ ] **Step 6: legacy GitHub/Cloudflare secrets を削除する**

`TEST_DATABASE_URL`, `DIRECT_URL`, `NEXT_PUBLIC_SUPABASE_*`, `SUPABASE_*`, `UPSTASH_*`, `VERCEL_*`, 旧 VAPID/Resend/image R2 secret のうち新 app が使わないものだけを inventory と照合して削除する。Google/GitHub/Points OAuth/new Cloudflare/Terraform state secret は保持する。

- [ ] **Step 7: decommission evidence と post-delete smoke**

削除対象 ID の hash、API status、実行時刻、実行者、Cloudflare smoke/reconciliation結果を `decommission-v0.2.md` に記録する。token/secret/個人データは書かない。

```bash
node scripts/web-app/assert-legacy-removed.mjs
pnpm --filter @freeism/points-web-app smoke:production
pnpm --filter @freeism/markets-web-app smoke:production
```

Expected: 全 PASS、Cloudflare から旧 provider への runtime call 0。

- [ ] **Step 8: commit evidence**

```bash
git add -- plan/web-app/evidence/decommission-v0.2.md
git commit -m "docs(web-app): record legacy platform decommission"
```

- [ ] **Step 9: decommission evidence PRをmerge queueへ通す**

```bash
git push -u origin codex/web-app-v0.2-decommission-evidence
gh pr create --base main --head codex/web-app-v0.2-decommission-evidence --title "docs(web-app): record legacy platform decommission" --body "不可逆操作後のsecret-free decommission evidenceだけをレビューしてください。"
gh pr checks --watch
```

PR作成後に`web-app / required`を通してmerge queueでmergeする。既存PRがある場合だけ同じbase/headを確認してcreateを省略する。deploy workflowのpath filterが`plan/web-app/evidence/**`だけの変更を除外し、このmergeの`main` pushで再deployしないことを確認する。外部DELETEの再実行やmanifest／lockfile変更をこのPRへ含めない。

## Task 13: Final verification と完了条件

**Files:** 原則変更なし。検証で見つかった問題だけ最小修正し、該当 Task の test/commit boundary へ戻す。

- [ ] **Step 1: root/cross verification**

```bash
node --test tests/web-app/*.test.mjs
pnpm contract:web-app:check
pnpm audit --prod --audit-level high
pnpm licenses list --prod --json
pnpm ignored-builds
node scripts/web-app/assert-production-release.mjs --mode assert
node scripts/web-app/assert-legacy-removed.mjs
```

Expected: 全PASS。旧app削除後のproduction dependenciesだけをpnpm標準audit/license commandで検査し、root dev-only toolchainを混ぜない。`ignored-builds`に表示されたdependencyはblockedのまま維持し、allowlistを`esbuild`／`workerd`から広げない。

- [ ] **Step 2: both app verification**

```bash
pnpm --filter @freeism/points-web-app check
pnpm --filter @freeism/points-web-app test
pnpm --filter @freeism/points-web-app test:worker
pnpm --filter @freeism/points-web-app build:staging
pnpm --filter @freeism/points-web-app build:assert
pnpm --filter @freeism/points-web-app test:e2e
pnpm --filter @freeism/markets-web-app check
pnpm --filter @freeism/markets-web-app test
pnpm --filter @freeism/markets-web-app test:worker
pnpm --filter @freeism/markets-web-app build:staging
pnpm --filter @freeism/markets-web-app build:assert
pnpm --filter @freeism/markets-web-app test:e2e
```

Expected: 全 PASS。Workers integration は実 D1/DO/Workflow runtime pool を使い、UI Happy DOM config と混ぜない。

- [ ] **Step 3: IaC/CI verification**

```bash
command -v terraform
terraform version -json | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const v=JSON.parse(s).terraform_version;if(v!=="1.15.7")process.exit(1)})'
terraform fmt -check -recursive infra/cloudflare
terraform -chdir=infra/cloudflare/modules/web-app-edge init -backend=false
terraform -chdir=infra/cloudflare/modules/web-app-edge validate
terraform -chdir=infra/cloudflare/modules/web-app-edge test
git grep -n "pull_request_target\|workflow_dispatch" -- .github/workflows/web-app
git diff --check
git status --short
```

Expected: Terraform PASS、禁止 trigger 0件、diff check PASS。status には意図した変更だけが残る。

- [ ] **Step 4: production completion checklist**

- GitHub main ruleset/merge queue active、bypass 0
- `main` push だけが production deploy trigger
- docs、実装、Better Auth正式版bump、post-production evidenceがすべてbranch／PR／merge queue経由で、direct main updateが0件
- staging failure で production が停止する実績あり
- Points/Markets custom domain/TLS/static/API/auth が green
- 4固定HTMLが日本語・英語双方のcanonical content／hashを持ち、JavaScript無効時の両言語可読性、keyboard／screen reader、hydration error 0が両appでgreen
- `translation-review.json`が現在の8 canonical Markdown hashと一致し、日本語正本・英語参照訳のbilingual review記録がある
- Workers Logs／traces、app別Analytics Engine、D1 alert、Email Routing、native Worker alertのstaging drillがgreen
- `/dashboard` 等の navigation は index、missing API/JS/CSS/image は 404
- Points利用者Access Tokenがopaque、Markets標準introspectionのpairwise subject照合がgreen、Client CredentialsがM2M-onlyで、独自認証endpointが0件
- Better Auth runtime packageとstandalone CLI `auth`が正式`1.7.0` exact、`@better-auth/oauth-provider`はPointsだけに存在し、rootの標準72時間cooldownを使うinstallがgreen
- Points–Markets OAuth/Service Binding/OpenAPI/reconciliation が green
- 即時購入が全数量lock→`BUY_NOW` settlement→通常reservation/captureへ進み、3種restore evidenceだけ全量復元、結果不明はhold維持、capture後immutable proof、proof migration順の分割RPC、複数hold／endAt barrierをWorker／Workflow／E2Eで満たす
- D1 Time Travel restore drill 実証済み
- Vercel/Supabase/Upstash/旧画像 R2/旧 workflows/secrets 撤去証跡あり
- archive/旧仕様情報は保持され、runtime からは参照されない

すべて満たした時だけ v0.2 migration を complete とする。

---

## Commit boundary summary

1. `docs(web-app): approve v0.2 migration contract`（既存docs／planだけの最初の独立commit）
2. `build(web-app): pin v0.2 toolchain`
3. `feat(points): add Cloudflare greenfield shell`
4. `feat(markets): add Cloudflare greenfield shell`
5. `docs(web-app): complete Points Markets wire contract`
6. `feat(web-app): define Points Markets OpenAPI`
7. Points Task 1〜13の小さいlocal domain commits
8. `infra(web-app): define Cloudflare edge resources`
9. `test(points): prove standard oauth feasibility`
10. `test(points): prove google fresh oidc live contract`
11. Points Task 14〜18とMarkets子計画の小さいcommits
12. `docs(web-app): add release and recovery runbooks`
13. `ci(web-app): enforce v0.2 merge gates`（push後に実check名を確認してからruleset適用）
14. `ci(web-app): deploy main through staging to production`
15. `refactor(web-app): cut over to Points and Markets`
16. `build(web-app): promote better auth 1.7 final`（production前の専用branch／PR）
17. `chore(web-app): record v0.2 production acceptance`（production後の別branch／PR）
18. `docs(web-app): record legacy platform decommission`

各 commit は直前 Task の test が green になってから作る。外部 resource mutation は repository commit と同一 transaction とみなさず、inventory/evidence/checkpoint を別に残す。
