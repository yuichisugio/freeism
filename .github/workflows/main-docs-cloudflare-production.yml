name: Deploy portal and docs to Cloudflare production

on:
  push:
    branches:
      - main
    paths:
      - projects/main-web-app/**
      - projects/docs-web-app/**
      - scripts/sites/**
      - tests/sites/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - .node-version
      - .github/workflows/main-docs-cloudflare-production.yml

permissions:
  contents: read

concurrency:
  group: freeism-main-docs-production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy portal and docs to production
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    environment: web-app-production
    env:
      CI: "true"
      WRANGLER_SEND_METRICS: "false"

    steps:
      - name: Check out the main commit
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Set up pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4.4.0
        with:
          version: 10.33.3
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version-file: .node-version
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test portal
        run: pnpm --filter main-web-app test

      - name: Test docs
        run: pnpm --filter docs-web-app test

      - name: Check portal
        run: pnpm --filter main-web-app check

      - name: Check docs
        run: pnpm --filter docs-web-app check

      - name: Build portal
        run: pnpm --filter main-web-app build

      - name: Build docs
        run: pnpm --filter docs-web-app build

      - name: Test site delivery contracts
        run: node --test tests/sites/*.test.mjs

      - name: Deploy portal
        run: pnpm --filter main-web-app deploy:production
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Smoke test portal
        run: pnpm --filter main-web-app smoke:production

      - name: Deploy docs
        run: pnpm --filter docs-web-app deploy:production
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Smoke test docs
        run: pnpm --filter docs-web-app smoke:production
