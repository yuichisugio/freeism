# アカウント統合サービス v0.1

- [アカウント統合サービス v0.1](#アカウント統合サービス-v01)
  - [説明](#説明)
  - [存在意義・独立したサービスにする理由](#存在意義独立したサービスにする理由)
  - [責務](#責務)
  - [v0.1で実装する機能](#v01で実装する機能)
  - [未決事項](#未決事項)
  - [文書化要件](#文書化要件)
  - [実装](#実装)

## 説明

- あらゆるアカウントのIDを統合して管理できるサービス

## 存在意義・独立したサービスにする理由

1. 無料主義の貢献度アップロードのアカウント管理を簡単にする
   - サービスの実装側・利用側の両方で、利用を簡単にしたい
2. アカウント統合の処理はとても複雑なので、責務の分離の為に切り出す
3. 他の無料主義サービスが、同じ内容を実装する必要がない状態にしたい
4. ポイント管理サービス以外からでも使用できると思う

## 責務

- Accountsは、Accounts内のユーザーと証明済みの外部アカウントの対応を管理する
- Pointsは、貢献データとポイントを管理する
- 照合結果に複数のAccountsユーザーが含まれる場合のポイント帰属先は、Points側で決定する
- 貢献データはPointsへアップロードし、PointsがAccounts APIへ貢献者の照合を依頼する
- Accountsは、貢献内容やポイント情報を保存しない

## v0.1で実装する機能

1. アカウントのIDの表示
   - `accounts.freeism.app`内のユーザーIDを、公開された固定IDとして表示する
   - 誰でも、公開されたAccountsユーザーIDを貢献データの貢献者として指定できる
   - AccountsユーザーIDを公開しても、連携した外部アカウントの一覧は自動的に公開しない
2. 貢献者の識別
   - 貢献データでは、次のどちらかで貢献者を指定できる
     1. 外部サービスのプロフィールURL
     2. Accounts内のユーザーID
   - プロフィールURLが指定された場合は、証明済みの外部アカウントと照合する
   - Accounts APIは、指定されたプロフィールURLに紐付き、問い合わせ元のOAuthクライアントへの公開が許可されたAccountsユーザーをすべて返す
   - 同じURLにAccountsユーザーA・Bが紐付き、両方が問い合わせ元への公開を許可している場合は、A・Bの両方を返す
3. Accountsへのログイン
   - v0.1では、Google OAuthとGitHub OAuthをログイン手段にする
   - 紐付け済みのGoogle・GitHubアカウントでAccountsへログインできる
   - 後から追加したGoogle・GitHubアカウントでも、紐付け先のAccountsユーザーへログインできる
   - 認証したGoogle・GitHubアカウントが複数のAccountsユーザーに紐付いている場合は、OAuth認証後に選択画面を表示し、ユーザーがログイン先を選ぶ
   - 通常の設定画面では、各AccountsユーザーにGoogle・GitHubアカウントを合計で最低1件残し、最後のログイン手段の連携解除を許可しない
   - GitHubアカウントが残っていれば最後のGoogleアカウントを解除でき、Googleアカウントが残っていれば最後のGitHubアカウントを解除できる
4. 外部アカウントの所有権証明
   - 1つのAccountsユーザーへ、Google・GitHubそれぞれ複数の外部アカウントを紐付けられる
   - Google・GitHubの連携アカウント数に上限を設けない
   - 同じ外部プロフィールURLやOAuthアカウントを、所有を証明できた複数のAccountsユーザーへ紐付けられる
   - 証明が成功した時点で、そのAccountsユーザーとの紐付けを即時に追加し、他のAccountsユーザーとの既存の紐付けも維持する
   - Google OAuth・GitHub OAuth・bi-directional linkのいずれも、紐付けの追加によって、すでにPointsへ帰属した貢献データやポイントは変更しない
   1. Google OAuth
      - ログインに使用したGoogleアカウントに加えて、別のGoogleアカウントもOAuthで所有を証明して紐付けられる
   2. GitHub OAuth
      - GitHub OAuthで所有を証明して紐付けられる
   3. bi-directional link
      - 相互リンクは紐付けの登録時に確認する
      - 証明後の紐付けは、本人がAccountsで解除するまで有効とする
      - 外部ページから相互リンクが削除された後も紐付けを維持し、定期確認は行わない
      - 所有関係は現在の紐付けとして管理し、所有期間は保存しない
5. OAuth Provider
   - `points.freeism.app`から権限付与できるようにする
   - 今後は、複数の`accounts.freeism.app`のようなサービスから、どれかを選べるようにしたいのでOAuth Providerにする
6. 閲覧権限の制御
   - 外部アカウントの公開範囲は、OAuthクライアントごとに管理する
   - OAuthクライアントへ初めて権限を付与するときは、すべての外部アカウントを非公開にする
   - ユーザーが同意画面で選択した外部アカウントだけを、そのOAuthクライアントへ公開する
   - 後から追加した外部アカウントは、ユーザーが明示的に公開するまで既存のOAuthクライアントへ公開しない
   - 公開された外部アカウントに限り、そのOAuthクライアントはプロフィールURLからAccountsユーザーを照合できる
   - ユーザーは、OAuthクライアントに対する外部アカウントの公開を後から解除できる
   - 公開を解除した外部アカウントは、それ以後の照合結果に含めない
   - 公開解除前にPointsへ帰属した貢献データやポイントは変更しない
7. ドメインは、`accounts.freeism.app`
   - 一旦は、freeism向けに提供したいため
8. データ移行を簡単にしたい
   - インポート、エクスポートを簡単にできるようにしたい
9. OSSとしてすべてを公開

## 未決事項

- 照合結果が複数ある場合のポイント帰属ルールは、後続のPoints側の要件整理で決定する

## 文書化要件

- 要件の聞き出しが完了した後、貢献者照合の流れをシーケンス図で記載する

## 実装

- UIはリッチにしない
- 最低限の機能のみ
- 使用するライブラリやデプロイ環境は、他と一緒
- `points.freeism.app`に実装しているアカウント連携の機能は、`accounts.freeism.app`に移動させたい
