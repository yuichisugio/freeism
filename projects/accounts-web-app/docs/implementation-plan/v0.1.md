# Accounts v0.1 実装計画

## 実装の入力と進め方

[Accounts v0.1仕様](../specification/v0.1/main.ja.md)と[URL検証仕様](../specification/v0.1/verify-url.ja.md)の確定事項・受け入れ条件を入力とし、以下の実装・移管・検証を進める。
要件と振る舞いは仕様書に集約し、この文書では作業順序、導入手順、確認方法を管理する。
未決事項は対応する実装に着手する前に確定し、テーブル構造を更新してからmigrationを作成する。
既存コードは移管対象を調べる資料として扱い、採用する構成は現行仕様と標準機能から決める。
実装の構造や細部は、利用者の目的と保守性を満たす、より良い方法を柔軟に選ぶ。サブエージェントには成果物と担当範囲を渡し、調査・実行の進め方は担当へ任せる。
Sol 6のサブエージェントは調査、文書記載、実装、レビュー、PR更新を担当する。Astra 6のメインエージェントは全体を管理し、レビュー結果と実装差分を敵対的にレビューする。指摘は現実的な利用フローでトリアージし、修正要否を判断する。
この要件定義の対話中は、回答を仕様書へ反映してレビューを省略する。実装・PRなどのレビュー方針は、その作業時に適用する。
認証の追加拡張が必要になった場合は、標準機能で満たせない理由と追加案を整理し、ユーザーの承認後に実装する。

- v0.1の実装は専用の`codex/`ブランチで進め、PRを作成し、そのPRのブランチへ実装・テスト・文書の変更をpushする。
- 初期の実装差分をdraft PRとして共有し、以後のpushで更新する。PRに仕様参照、検証結果、Preview URLを記載する。
- フェーズごとに最小の動作単位を実装・検証する。ユーザーの権限、所有者の一意性、保存結果に必要なチェックをバックエンドに置く。
- Pointsとの接続変更は[Points実装計画](../../../points-web-app/plan/v0.2-implementation.md)と連携する。

## 移行する機能と既存実装

表のリンクはPoints側の移行元を示す。
経済処理と同じファイルに実装されている場合は、Accountsの責務に属する処理を切り出して移行する。

| 機能 | Accountsへ移行・整備する内容 | 主な移行元 |
| --- | --- | --- |
| Google・GitHubの認証と追加連携、ORCIDの追加 | 認証フックから外部アカウント管理部分を切り出し、Accounts用のOAuth設定・保存処理・追加連携を実装する。Pointsの独立認証との境界を検証する | [create-auth.ts](../../../points-web-app/src/backend/auth/create-auth.ts)、[auth-options.ts](../../../points-web-app/src/backend/auth/auth-options.ts)、[github-identity-grant.ts](../../../points-web-app/src/backend/auth/github-identity-grant.ts) |
| 外部アカウントの一意性と解除 | 外部サービス名・固有ID・URLの対応、一意性、連携解除、所有者の更新 | [permanent-oauth-subject-repository.ts](../../../points-web-app/src/backend/infrastructure/db/permanent-oauth-subject-repository.ts)、[deactivate-github-ownership.ts](../../../points-web-app/src/backend/usecases/deactivate-github-ownership.ts)、[reactivate-github-ownership.ts](../../../points-web-app/src/backend/usecases/reactivate-github-ownership.ts) |
| Webページの取得とリンク検証 | [URL検証仕様](../specification/v0.1/verify-url.ja.md)に対応するURL正規化、取得、証拠抽出、プロフィールURLとの照合 | [normalize-identity-url.ts](../../../points-web-app/src/backend/domain/ownership/normalize-identity-url.ts)、[safe-page-fetcher.ts](../../../points-web-app/src/backend/ownership/safe-page-fetcher.ts)、[profile-link-evidence.ts](../../../points-web-app/src/backend/ownership/profile-link-evidence.ts) |
| 所有権の確認と保存 | 検証の開始・成功・失敗、現在の紐付け、本人による解除 | [verify-web-ownership.ts](../../../points-web-app/src/backend/usecases/verify-web-ownership.ts)、[d1-ownership-repository.ts](../../../points-web-app/src/backend/infrastructure/db/d1-ownership-repository.ts)、[ownership.ts](../../../points-web-app/src/backend/infrastructure/db/schema/ownership.ts) |
| 貢献者の識別と照合 | [Accounts API](../specification/v0.1/main.ja.md#accounts-api)に従ってAccountsの共通照合処理を実装し、移行元の照合呼出箇所から必要な接続契約を確認する | [validate-fix-csv.ts](../../../points-web-app/src/backend/usecases/validate-fix-csv.ts)の照合呼出箇所 |
| プロフィールと公開先の設定 | Accountsの表示名、外部アカウントの識別情報、一般公開・OAuthクライアントごとの公開対象 | [update-profile.ts](../../../points-web-app/src/backend/usecases/update-profile.ts)、[read-public-profile.ts](../../../points-web-app/src/backend/usecases/read-public-profile.ts)、[profile-settings-form.tsx](../../../points-web-app/src/client/components/profile/profile-settings-form.tsx) |
| 外部アカウント管理の画面・API | 追加連携、URL登録、検証実行、結果表示、連携解除と設定の導線 | [ownership-panel.tsx](../../../points-web-app/src/client/components/profile/ownership-panel.tsx)、[ownership-routes.ts](../../../points-web-app/src/backend/http/routes/ownership-routes.ts)、[provider-buttons.tsx](../../../points-web-app/src/client/components/auth/provider-buttons.tsx) |
| 情報提供の認可とクライアント管理 | Accountsのアカウント連携画面での同意・公開許可、照合APIの認可、Client ID・公開鍵管理 | [points-oauth-provider.ts](../../../points-web-app/src/backend/auth/points-oauth-provider.ts)、[register-points-oauth-clients.ts](../../../points-web-app/src/backend/auth/register-points-oauth-clients.ts)、[consent-panel.tsx](../../../points-web-app/src/client/components/oauth/consent-panel.tsx)を責務分離・再利用の参考にする |

一般公開・OAuthクライアントごとの外部アカウント選択など、Accountsの仕様に必要な機能は、移行元の実装状況を確認して整備する。
外部アカウントの管理機能に付随するDB定義、設定値、OAuthコールバック、シークレット、監視、テストも移行対象の棚卸しに含める。

## 1. 開発基盤と構成を整える

- Valibot、Hono、Drizzle、Better Auth、TanStack Start、React、HeroUI v3、Tailwind CSS、Vite Plus、linkify-itを導入する。依存関係は既存のpnpm運用で管理し、必要に応じて適切な版へ更新する。
- [提供・技術要件](../specification/v0.1/main.ja.md#提供技術要件)に従い、HonoのHTTP境界、機能ごとの処理、DrizzleによるDBアクセスを分ける。外部接続の差し替えとテストに必要な境界に絞ってクリーンアーキテクチャを適用する。
- フロントエンドは機能単位のディレクトリにビューとフックを分ける。API呼出し、状態管理、保存・検証操作を機能ごとのフックに集約し、ビューは受け取った状態と操作を描画へ結び付ける。
- 入出力のValibotスキーマを共通モジュールへ置き、画面入力とHonoの入力境界の両方で同じ検証を実行する。セッションの本人確認とDB上の権限確認はバックエンドで行う。
- TypeScriptの`strict`を有効にし、Better Authの`$Infer`とHonoのルート型から型を取得する。
- Hono 4.13.0以降の`app.query()`を使い、APIのPATHとハンドラーを同じ宣言にまとめる。機能別のルートファイルは`app.route()`で接続し、型推論を保つ。[Honoの構成ガイド](https://hono.dev/docs/guides/best-practices)を参照する。
- [Cloudflare Vite PluginとTanStack Startの公式構成](https://developers.cloudflare.com/workers/framework-guides/web-apps/tanstack-start/)を接続する。TanStack StartのSPA・事前生成設定を使用し、画面の生成物をassetsへ接続する。`src/index.ts`のHonoはAPI・認証・公開プロフィールのSSRを担当し、Vite Plus経由の開発・ビルド・プレビューを整える。

### URL検証の採用部品と参考実装のライセンス

[URL検証仕様の参考資料](../specification/v0.1/verify-url.ja.md#参考資料)に挙げた実装と、URL検証で採用する部品のライセンスを次のとおり記録する。

| 対象 | ライセンス | 扱い |
| --- | --- | --- |
| linkify-it | MIT | 最新安定版を依存部品として採用する。`fuzzyLink: false`とし、`https:`スキームの一致だけを採用する |
| doipjs | Apache-2.0 | ライセンスの条件に従って参照・流用できる |
| Harborの検証器 | MIT（`apps/`以外） | `apps/`以外のコードをライセンスの条件に従って参照・流用できる |
| Keytrace、Divine Identity Verification Service | ライセンス未設定 | 設計の参照のみとし、コードは流用しない |

### Honoミドルウェアの接続

| 導入するもの | 作業と検証 |
| --- | --- |
| [`hono/basic-auth`](https://hono.dev/docs/middleware/builtin/basic-auth) | テスト環境の画面と表示に必要な静的ファイルへ`basicAuth`を接続する。資格情報は環境別のSecretから取得し、対象パスの保護を確認する |
| [`hono/csrf`](https://hono.dev/docs/middleware/builtin/csrf) | Cookieを利用する状態変更ルートに接続する。Better Authの標準のOrigin検証と適用範囲を整理し、外部サービスの認証callbackやクライアント認証APIとの接続を確認する |
| [`hono/etag`](https://hono.dev/docs/middleware/builtin/etag) | 再検証を許可した公開レスポンスに接続し、公開設定の変更が次の取得に反映されることを確認する |
| [`hono/trailing-slash`](https://hono.dev/docs/middleware/builtin/trailing-slash) | アプリ画面のURL規約に接続する。OAuth callback・登録redirect URI・外部証明URLはそれぞれの契約どおりに扱う |
| [`hono/language`](https://hono.dev/docs/middleware/builtin/language) | 日本語・英語の検出結果を画面とエラー応答へ渡す |
| [`hono/secure-headers`](https://hono.dev/docs/middleware/builtin/secure-headers) | 公開画面・管理画面・APIに必要なヘッダーを設定し、静的HTML、OAuth遷移、APIドキュメントの表示を確認する |
| [`hono/pretty-json`](https://hono.dev/docs/middleware/builtin/pretty-json) | `prettyJSON({ force: true })`でJSON API応答を整形する。ダウンロード用のストリームは専用ハンドラーから返す |

## 2. Workersの実行・配信・運用環境を整える

### WranglerとBindings

- `wrangler`をアプリの`devDependencies`へ追加する。Worker Previewsに必要な版を満たす最新版を選び、プロジェクト内のCLIで実行する。
- `wrangler.jsonc`に`main: "src/index.ts"`、`minify: true`、`upload_source_maps: true`を設定する。
- `compatibility_date`を導入時の最新日付にし、`compatibility_flags: ["nodejs_compat", "global_fetch_strictly_public"]`を指定する。日付・flagを更新した差分でビルドとWorkers上のテストを実行する。
- `placement: { mode: "smart" }`を設定し、D1アクセスと公開画面の応答時間を確認する。静的アセットをWorker経由にした場合の配置の影響も計測する。
- 本番・stagingの変数、D1、Secret、認証callbackを環境別に設定し、Preview用の`previews`設定を整える。
- `pnpm exec wrangler types --env-interface CloudflareBindings`で`worker-configuration.d.ts`を生成し、`tsconfig.json`の`compilerOptions.types`へ接続する。環境設定の更新後も型を再生成する。
- 型検査scriptを`pnpm exec wrangler types --env-interface CloudflareBindings && tsc --noEmit`として登録する。
- デプロイ先の秘密値は`pnpm exec wrangler secret put`で登録する。ローカルではgit管理外の`.env.local`またはシェル環境から読み込み、必要な変数名と説明を`.env.example`へ記載する。[環境変数の公式手順](https://developers.cloudflare.com/workers/local-development/environment-variables/)に従って、選択した環境での読み込みを確認する。

### 静的ファイル・公開HTML・キャッシュ

- JS、CSS、画像、事前生成した画面をWorkersの`assets`へ接続する。出力先はCloudflare Vite Pluginが生成する配信設定と揃える。
- API、OAuth関連endpoint、`/.well-known/*`をWorkerで処理する。`/profiles/*`は公開プロフィール専用エントリーポイントへ渡し、キャッシュミス時にHonoがD1から在籍ユーザーの表示名・固定IDと一般公開を許可された外部アカウント情報を読み、HTMLをSSRして`c.html()`で返す。公開する外部リンクが0件でもプロフィールを返し、退会などでユーザーが存在しない場合とban中のユーザーは404を返す。[HonoのHTML応答](https://hono.dev/docs/api/context#html)、[Workers Cache](https://developers.cloudflare.com/workers/cache/)を参照する。
- 表示名・外部アカウントの一般公開設定の変更、外部アカウントの解除・所有者の変更・退会・ban・unbanなど、プロフィールの表示内容に影響するDB更新後、影響するプロフィールのWorkers Cacheを`ctx.cache.purge({ pathPrefixes: ["/profiles/{accountsUserId}"] })`でpurgeする。所有者の変更では旧・新双方のプロフィールを1回の呼出しで対象にする。次の取得でD1からユーザーの存在と公開対象を読み直してHTMLまたは404を返す。テンプレート変更時もキャッシュをpurgeする。Workers Cacheのpurgeは契約プランによらずFree枠の頻度制限（アカウント単位で1分5要求、bucket 25）を受けるため、公開内容が変わる更新だけを対象にする。結果の`success`が`false`の場合は`errors`をログに残し、edgeのTTLで収束させる。[Workers Cacheのpurge](https://developers.cloudflare.com/workers/cache/purge/)に従う。
- SPAモードを用いる画面は`not_found_handling: "single-page-application"`で配信し、`run_worker_first`へAPI・認証・公開プロフィールのパスを登録する。固定ページのSSG生成物はassetsから配信し、公開プロフィールの初回HTMLから`rel="me"`を読めることを確認する。
- staging・Previewでは`run_worker_first: true`により静的ファイルもWorkerへ通し、画面と表示に必要な静的ファイルへBasic認証を適用する。API・認証プロトコルのエンドポイントはそれぞれの通常の認証・公開条件で処理する。[静的ファイルのルーティング](https://developers.cloudflare.com/workers/static-assets/routing/worker-script/)を参照する。
- API・認証・管理画面の入口は`cache.enabled: false`とし、公開プロフィール専用エントリーポイントだけ`cache.enabled: true`にする。公開プロフィールの応答には、edge用に`cloudflare-cdn-cache-control: max-age=86400, must-revalidate`（TTLの上限1日）、ブラウザー用に`Cache-Control: public, no-cache`を設定し、期限後に古い応答を返さないことを確認する。静的ファイルはassetsのキャッシュを利用する。PreviewのBasic認証を通す入口もキャッシュしない。[Workers Cacheの設定](https://developers.cloudflare.com/workers/cache/configuration/)に従う。
- `cross_version_cache`を有効にする対象では、デプロイをまたいだキャッシュ共有とテンプレート変更時のpurgeを接続する。purgeはキャッシュを保持する公開プロフィール専用エントリーポイントから行い、DB更新後の呼び出しと結果を確認する。入口のキャッシュ設定とブラウザー向けのHTTPキャッシュ制御を分けて確認する。

### Previewとアクセス制御

- [Worker Previews](https://developers.cloudflare.com/workers/previews/)を利用し、PRのpush後に`pnpm exec wrangler preview`でブランチのPreviewを更新する。2026-09-23の公式案内ではWrangler 4.135.0以降が必要となる。
- Previewごとにコード・画面とURLを切り替え、D1などのBindingsは共有可能とする。共有DBへの更新やschema変更は接続先の環境へ共通して反映されるため、動作確認する変更に応じて共有・分離を選ぶ。Preview名ごとのURLとデプロイを固定したURLをPRへ記載する。
- Basic認証を有効にしたテスト環境で、画面と表示に必要な静的ファイルの保護、OAuth往復、各エンドポイントの通常の認証・公開条件によるAPI呼出しを検証する。外部の相互リンク検証サービスによる取得テストは、そのサービスが到達できる検証用URLで行う。
- 外部OAuthのローカル・Preview往復にはOAuth Proxyプラグインを接続し、Providerに登録した安定したcallback URLと許可された戻り先を確認する。

### リクエスト制限と可観測性

- Web検証・一般APIのリクエスト制限をCloudflare WAFのRate Limitingルールへ設定する。Web検証には、AccountsユーザーIDをキーとするWorkers Rate Limiting bindingも接続する。認証endpointはBetter Auth標準のDB保存型Rate Limitを`rateLimit.enabled: true`で有効にし、`advanced.ipAddress.ipAddressHeaders: ["cf-connecting-ip"]`で取得した接続元IPごとに数えることをWorkers上で確認する。
- WAFの対象ホストとパス、カウント対象、閾値、期間、429応答を実環境で確認する。1時間のカウント期間は契約プランの対応を確認してから指定し、採用できる期間と回数を仕様へ反映する。[WAFのプラン別対応](https://developers.cloudflare.com/waf/rate-limiting-rules/)を確認する。
- Workers LogsとTracesを有効にし、`head_sampling_rate`で記録量を調整する。`console.log`・`console.warn`・`console.error`で重要度を分け、仕様で許可した項目を構造化JSONとして記録する。[Logs](https://developers.cloudflare.com/workers/observability/logs/workers-logs/)と[Traces](https://developers.cloudflare.com/workers/observability/traces/)を参照する。
- [管理画面と監査](../specification/v0.1/main.ja.md#管理画面と監査)に挙げた監査イベント（ban・unbanを含む）を、仕様で許可した項目だけの構造化JSONとして記録する。
- ソースマップで例外の元ファイルを追えること、秘密情報と利用者データの出力範囲が仕様どおりであることを確認する。

### Workers内の処理

- リクエストごとの状態はハンドラーのローカル変数・引数・Hono contextで渡し、`env`は環境のBindingsの参照に使う。
- 応答に必要な保存・検証は`await`または`return`で完了を待つ。応答後に実行できる処理は`ctx.waitUntil()`へ接続し、実行期限内に完了する単位にする。
- Better Authのバックグラウンド処理もWorkerの実行contextへ接続する。Vite Plusのlintに`typescript/no-floating-promises`を設定する。
- JSON出力などの大きなダウンロードを`TransformStream`で構築し、整形したJSONを逐次書き出す。読込側と書込側の完了・中断を処理する。
- トークンなどの乱数にはWeb Cryptoの`crypto.randomUUID()`・`crypto.getRandomValues()`を使用する。秘密値の検証は認証ライブラリの標準処理へ委ね、独自比較が必要な箇所では固定長化した値をタイミングに依存しないAPIで比較する。[Workersの実行ガイド](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/)を参照する。

## 3. 認証と保存構造を接続する

- [テーブル構造の設計案](../specification/v0.1/main.ja.md#テーブル構造の設計案)から、採用設定でCLI生成したBetter Auth標準schemaとAccounts独自の6表を同じDrizzle schemaに組み合わせる。標準列は生成結果を正とし、公開`oauthClient.clientId`と内部`id`、`account.id`とProviderの`accountId`を区別する。
- Better Auth 1.7系の正式版と対応するDrizzle adapter・OAuth Providerを揃えて導入し、[1.7移行ガイド](https://better-auth.com/docs/guides/1-7-upgrade-guide)から採用版の設定を確認する。
- `better-auth/minimal`、暗号化、明示的なProvider連携と`trustedProviders`、ログイン時更新、Cookie cache、DB joins、DB保存型Rate Limit、`session.freshAge`・`user.deleteUser`、OAuth Provider・JWTを[提供・技術要件](../specification/v0.1/main.ja.md#提供技術要件)の設定表に従って設定する。
- OAuth tokenの暗号化鍵は、Better Authの`secrets`（versioned secrets。先頭がcurrent）へ環境別のWorker Secretから渡す。新しいversionを先頭に加えて切り替え、旧versionで暗号化したtokenの復号、次回保存でのcurrent versionへの更新、未知versionの拒否を確認する。切り替え手順をREADMEに記載する。
- i18n、Last Login Method、multi-session、OAuth Proxy、Open APIを画面・APIへ接続する。TestUtilsは結合テストとE2Eの準備へ接続する。Adminプラグインは`ac`・`roles`でロール`appAdmin`（`user: ["list", "get", "ban"]`）と権限を持たない`user`を定義し、`defaultRole`を`user`とする。`appAdmin`がユーザーの一覧・取得とban・unbanだけを実行でき、代理ログイン、ユーザーの作成・更新・削除、ロール変更、パスワード設定、セッションの一覧・失効を拒否されることを確認する。
- Google・GitHub・ORCIDの実通信で共通ログインフロー、同一本人・Providerの複数アカウントの明示連携、元ユーザーでの解除後の再連携を検証する。`linkSocial()`・`unlinkAccount()`が標準schema上で期待通りに動作することと、state・nonce・PKCE、暗号化されたProvider tokenの保存を標準の認証経路で確認する。ORCIDとメール未確認のGitHubアカウントも明示連携できることを確認する。`unlinkAccount()`には標準`account`の行ID（`account.id`）を`accountId`として渡す。
- 初めての外部アカウントで標準のユーザー作成とログインが完了し、表示名「仮ユーザー」で利用を開始できることを確認する。`databaseHooks.user.create.before`で`name`を「仮ユーザー」にする。`user.email`には標準処理で作成時の外部アカウントのメールアドレスが入り、その後のログイン・追加連携で変わらないことを確認する。メールを返さないORCIDは、Generic OAuthの`mapProfileToUser`で`{ORCID iD}@orcid.invalid`と`emailVerified: false`を渡す。既存ユーザーの`user.email`と同じメールの未連携の外部アカウントでのログインが`account_not_linked`でログイン画面へ戻って案内が表示され、既存ユーザーでログインして明示連携すれば連携できることを確認する。解除した外部アカウントでの新規登録が同じ結果になり、元ユーザーへの再連携と、移動先ユーザーからの明示連携ができることを確認する。`email_not_found`でも対応する案内が表示されることを確認する。
- 標準`account`の作成・更新後（`databaseHooks.account.create.after`・`account.update.after`）に、外部アカウント行・Provider識別子・`oauth`証明・表示名・本人向けのメール（`external_accounts.email`）を冪等に作成・更新する。公開選択はOFFで作成し、別の所有者の有効な識別子と衝突するユーザー名・プロフィールURLは同じbatchで今回の本人へ移動する。表示名・メールは、Googleでは保存済みのID Token、GitHubでは標準の`accountInfo`から得る。ORCIDはメールを返さないため表示名だけを保存する。書込に失敗した場合は、次回ログイン時のセッション作成後に再構成できることを確認する。
- クライアント登録時に、Accountsのバックエンドが`client_credentials_scopes: ["identities:read"]`を設定し、`clientPrivileges`で必要な`configure-client-credentials-scopes`権限を与える。登録ではアプリ名・リダイレクトURL・`private_key_jwt`用の公開鍵を必須とし、公開鍵はインラインのJWK Setとして標準の登録APIの`jwks`へ渡す。リダイレクトURLに`localhost`・`127.0.0.1`・`[::1]`のHTTPのURLを含む場合は`application_type: "native"`、それ以外は`web`をバックエンドが決め、標準の登録・更新APIへ渡す。標準の登録APIへ接続する設定と、利用者の情報提供同意を区別する。[OAuth Provider](https://better-auth.com/docs/plugins/oauth-provider)に従って認可された範囲だけ登録できること、登録したクライアントが`clientRegistrationDefaultResources`により標準の`oauthClientResource`でAccounts資源APIのresourceへ関連付くこと、`oauthProvider.scopes`を`openid`・`identities:read`とし、受け付けるscopeが、Authorization Codeフローの認可要求では`openid`、Client Credentialsのトークン要求では`identities:read`に限られること、UserInfoに`user.email`が含まれないことを確認する。
- OAuth Providerの`consentPage`を「アカウント連携」画面へ接続する。連携先の名前・紹介URL（標準`oauth2.publicClient`で取得）と、Accounts共通の定型文（提供する項目と、連携先が公開表示・照合に使いうること）を表示し、外部アカウントの選択を既存の設定保存APIで保存した後、標準の`oauth2.consent`へ接続する。認可要求と保存先クライアントの対応はバックエンドで確認し、OAuth要求の検証とコード発行はBetter Authに委ねる。[標準の同意画面](https://better-auth.com/docs/plugins/oauth-provider#consent-screen)を参照する。
- 同意画面の終了操作は、標準`oauth2.consent`の`accept: true`（同意して戻る）と`accept: false`（同意しない）の2つとする。`accept: true`は今回の連携先への情報提供同意をONで保存した後に実行し、認可コードの発行で連携が成立することを確認する。`accept: false`は`access_denied`で元のサービスへ戻り、保存済みの`oauthConsent`と`client_consents`を変更しないことを確認する。同意画面の署名付きクエリは10分で失効するため、失効後の操作では連携を最初からやり直す案内を表示する。
- 利用側が認可要求に`prompt=consent`を付けた場合は、保存済みのOAuth同意があっても同意画面を表示することを確認する。本人が情報提供同意をOFFで保存したときは、同意の保存と同じD1 batchでそのユーザーとクライアントの`oauthConsent`行を削除し、次の認可要求で同意画面が表示されることを確認する。
- 本人の提供先一覧と情報提供状態を、保存済みの同意・公開設定と現在の認可要求から組み立てる。初回選択、設定保存の失敗時、再連携時の保存済み選択を確認する。同意ONで証明済みの選択が0件のクライアントの列と理由を画面に示し、保存の検査と「保存」ボタンの非活性化は画面に表示する有効なクライアントを対象にする。存在しないClient IDの復元設定があっても保存できることを確認する。
- Client Credentialsの署名付きJWT、初回連携、scope・audience、公開鍵の登録・更新をAPIへ接続する。`private_key_jwt`とDPoPは公式OAuth Providerの設定・検証ヘルパーで構成し、利用側の標準ライブラリと接続して確認する。Authorization CodeとClient CredentialsのAccess Tokenの`expires_in`が900秒になること、`/token`が無効でセッション応答にJWTヘッダーが付かないことを確認する。
- 公開鍵の更新は、承認済みの独自拡張として、所有者本人のクライアントの`oauthClient.jwks`をAccountsの保存処理で更新する。鍵は標準の登録時と同じ検査関数（`@better-auth/oauth-provider/internal`の`validatePublicClientJwks`。公開APIの互換保証外）で検査し、JWK SetをJSON文字列で保存する。保存は鍵の検査、標準の更新APIでのアプリ情報・リダイレクトURLの更新、鍵の保存の順に行う。鍵の保存だけが失敗した場合に同じ内容の再保存で収束すること、次のクライアント認証から新しい鍵で検証されること、新旧の鍵を同じJWK Setに含めて切り替えられることを確認する。Better Authの版を更新するときは、この検査関数の挙動をテストで確認する。
- 標準の`rateLimit`表と採用プラグインのschemaを生成し、独自表のPK・FK（`ON DELETE CASCADE`）・本人内UNIQUE・有効識別子の部分UNIQUE・検索索引をmigrationへ含める。認証用の標準表には採用版の実際の検索に必要な索引を確認する。生成したDrizzle relationsをadapterに渡し、同じ表間に複数の関連がある箇所は`relationName`を対応させる。
- 未検証URLの複数人登録、本人内150件、有効識別子の一意性、証明と複数識別子の対応、再検証失敗時の成功集合維持、Web識別子だけの移動、リンク証明単独では`dns_txt`・`oauth`が支える識別子を移動しないこと、OAuth解除で他方式の証明が残る場合、支えを失った識別子が`is_active=0`の候補になること、最後のログイン手段の解除拒否を保存層の検証対象にする。
- 標準`unlinkAccount()`の`account`行削除で、対応する`oauth`証明と対象関連が外部キーのCASCADEにより同じ文で削除され、ほかの証明・識別子・外部アカウント行が残ることを確認する。標準`deleteUser`の`user`行削除では、本人の独自表データが同じ文で削除される。支える成功証明が無くなった識別子は、後続のbatchで`is_active=0`の候補にする。表示行全体の解除は標準の解除を先に行い、成功した場合に後続のbatchで`external_accounts`行を削除する。最後のログイン手段などで標準の解除が拒否された場合は、独自表を変更しない。
- すべての日時をUTCとして保存し、Drizzle・Better Auth・JSON境界で単位とUTCの往復を確認する。
- 一覧・照合の読取条件と、公開設定保存・所有者移動・復元の一括書込を[読み取りと更新の単位](../specification/v0.1/main.ja.md#読み取りと更新の単位)に合わせる。複数の値は配列をJSON文字列にした1つのバインド値で渡し、`json_each(?)`で展開する（読取は`IN (SELECT value FROM json_each(?))`、復元の書込は`INSERT … SELECT … FROM json_each(?)`）。照合は入力を500件ずつ読み、`json_each`の`key`で元の入力順へ戻す。1操作の原子性が必要な書込は1回の`batch()`に収める。

### D1のmigrationと運用コマンド

- Drizzle Kitでschema差分からmigration SQLを生成し、Wranglerが読むmigrationディレクトリに保存する。
- 独立したデータ移行SQLが必要な場合は`pnpm exec wrangler d1 migrations create <DATABASE> <MESSAGE>`で作成し、同じ履歴に含める。
- ローカルで`pnpm exec wrangler d1 migrations apply <DATABASE> --local`を実行し、stagingで`--remote --env staging`を指定して検証する。
- `pnpm exec wrangler d1 execute <DATABASE>`はSELECT、確認用SQL、シード、単発の保守処理に使い、実行環境を明示する。
- schema・索引変更後に`PRAGMA optimize`を実行する。[D1のSQL仕様](https://developers.cloudflare.com/d1/sql-api/sql-statements/#pragma-optimize)と[Wrangler D1コマンド](https://developers.cloudflare.com/workers/wrangler/commands/d1/)を参照する。
- 生成したmigrationをバージョン管理し、空DBと変更前schemaの双方から適用できることを確認する。
- D1では外部キーを無効にできず、親表を作り直すmigrationの`DROP TABLE`はCASCADEで子表の行を消しうる。該当するmigrationは、データを入れたstagingで適用前後の件数を確認する。

## 4. Accountsの機能を実装・移管する

- [URL検証仕様](../specification/v0.1/verify-url.ja.md)から、単一URL入力後の「保存して検証する」押下を起点とした未登録URLの受付、取得と証拠抽出、リンク検証が成立しない場合の同一要求内でのDNS TXT確認、結果の保存、保存済み識別情報による照合を実装する。「未検証で保存」も選択でき、両方法が不成立の場合も入力URLを未検証として保存する。移行元の既存テストを新しい条件へ対応付ける。CDNでキャッシュされた応答（`Age`・`CF-Cache-Status`を持つ応答）も通常の応答として扱う。
- セッション本人のプロフィールURLを期待値とし、入力URLの構文・取得先の安全性を検査してから公開ページを取得する。リンクが成立すればその成功結果を保存し、取得失敗・リンク不一致などで成立しなければ対象hostのDNS TXTを確認する。DNS成功時は、入力URLと同じhostの登録済みURLを含む各外部アカウントに、hostをキーとする`dns_txt`証明行を作る。外部アカウントは統合しない。直近の方法別再検証結果と有効な証明・紐付けを分け、照合APIは保存済みの識別子・証明・現在の紐付け・公開許可で判定する。
- DNS TXTは、Workersの`node:dns/promises`の`resolveTxt`（1.1.1.1へのDNS over HTTPS。subrequestとして数える）で`_accounts.{host}`を照会する。照会の期限は3秒とし、ページ取得の5秒とは別に数える。各レコードのcharacter-stringを連結してURL正規化を適用し、本人の公開プロフィールURLとの完全一致を判定する。CNAMEの行は無視する。該当するTXTが無い（NXDOMAIN・NODATA）場合は`not_verified`、SERVFAIL・timeoutなどの取得失敗と、同じAccountsサービスの異なるユーザーの公開プロフィールURLが複数ある場合は`indeterminate`とする。CNAMEを設定したサブドメインでも`_accounts.{host}`のTXTで証明できること、既存のTXTを残したまま証明できること、追加直後の不一致で再検証を促す案内が出ることを確認する。「アカウント連携」画面の入力欄の近くに、本人の公開プロフィールURL（TXTの値を兼ねる）とコピー操作を表示する。
- セッションから本人とプロフィールURLを決定し、正規化、HTML全体の許可リンク・テキスト、HTTP Link、サービス別の公開ページ解析を検証する。GitHub・GitLab・Stack Overflow・Qiita・note・Zenn・Codeberg・X・Hugging Faceのプロフィールに個別対応し、サービス名の判定、redirect追跡後の最終取得URLへのURL規則の適用によるユーザー名の抽出、そのプロフィールページの証拠による証明を実装する。組織アカウントのプロフィールURLも個人と同じ規則でサービス名とユーザー名を保存する。記事・投稿・Gist・Merge Request・Issue・質問・回答・リポジトリ・モデル・データセット・SpacesなどのコンテンツURLと、Reddit・KaggleのURLは汎用Webページとして登録・共通検証し、証明は入力URL単独に適用する。第三者投稿の読者コメントと`code`・`pre`内のコード例のURLも証拠候補に含め、異なるAccountsユーザーへのURLが併存する場合の判断不能を確認する。403などの取得制限は判断不能とする。コンテンツURLの証明が作者のユーザー名・プロフィールURLへ適用されないこと、リンク検証の結果表示がページの編集権限を確認したように見えないことも確認する。
- `HTMLRewriter`はテキストと属性値の文字参照（`&lt;`・`&amp;`・`&#x26;`など）を復号しない。テキストノードのチャンクを連結してから文字参照を復号してlinkify-itへ渡し、`href`などの属性値も復号してから相対URLを解決・正規化する。抽出はチャンクを連結したテキストノード単位で行い、要素やコメントをまたいで連結しない。`template`・`noscript`の中身は除外し、`hidden`属性やCSSによる表示状態は評価しない。`code`・`pre`内の`&lt;URL&gt;`形式のURLが一致することをテストする。
- [管理画面](../specification/v0.1/main.ja.md#管理画面と監査)の設定・アカウント連携・開発者向けへ機能を配置する。個別の公開先選択、一括指定、保存ボタンの状態、保存APIの検証を接続する。本人向けの外部アカウント一覧には、取得できた場合はOAuthのメールアドレスを、無い場合は表示名だけを表示する。
- 「アカウント連携」画面に「OAuthの認証連携だけ解除する」操作を置き、標準`unlinkAccount({ accountId })`（`accountId`は標準`account`の行ID）を呼ぶ。対応する`oauth`証明と対象関連だけが終了し、ほかの方法が支える識別子と外部アカウント行が残ること、最後のログイン手段の解除が標準どおり拒否されることを確認する。
- 公開プロフィールを未ログインで取得し、許可された情報と検証方法のバッジが初期HTMLに含まれることを確認する。
- 公開プロフィールの初回取得でD1からHTMLを生成し、再取得ではWorkers Cacheから返すことを確認する。外部リンク0件でも表示名・固定IDを返し、退会後とban中は404となることを確認する。公開設定・外部アカウント解除・所有者変更・退会・ban・unbanによるDB更新後は、対象プロフィールのpurgeと次の取得での公開内容の再評価を確認する。
- [運営者の権限](../specification/v0.1/main.ja.md#運営者の権限)に従い、運営者機能は画面を設けず、ログイン済みの`appAdmin`がAdmin APIを呼ぶ。初回の`appAdmin`は、OAuthで作成したユーザーの`user.role`を`pnpm exec wrangler d1 execute`で`appAdmin`に更新して任命する。banは期限を指定せずに行う。ban中はログインの拒否と既存セッションの削除（標準）、公開プロフィールの404、連携アカウント一覧と照合APIでユーザーが存在しない場合と同じ結果になり、unbanで元の公開設定へ戻ることを確認する。
- OAuthクライアント管理を接続し、登録件数上限、提供先としての表示、Client ID・公開鍵、登録情報の編集・削除を仕様からテストする。本番のHTTPSとloopbackのリダイレクトURLを1つのクライアントに併記できること、loopbackのHTTPはportだけを除いて一致を確認すること、`application_type`にかかわらず`private_key_jwt`・`client_credentials`・DPoPを使えることを確認する。
- [Accounts API](../specification/v0.1/main.ja.md#accounts-api)の入力・出力スキーマ、HTTPメソッド、認証・認可、不正な入力を含む場合の400応答を実装する。
- Valibotの共通スキーマを公式`@valibot/to-json-schema`で変換し、OpenAPI 3.2.1の`query`操作・認証・応答定義へ組み込む。開発者向け画面でQUERYに対応する表示UIを使い、表示と送信を確認する。Better AuthのOpen APIプラグインが出力する認証API（OpenAPI 3.1.1）とAccountsの資源APIを区別して案内する。[ValibotのJSON Schema変換](https://valibot.dev/guides/json-schema/)、[HonoのQUERY対応](https://github.com/honojs/hono/releases/tag/v4.13.0)を参照する。
- [JSONによるバックアップと移行](../specification/v0.1/main.ja.md#jsonによるバックアップと移行)から、出力前の内容確認・復元ボタン・設定反映・登録候補を実装する。存在しないクライアントIDを含む公開設定も確定した保存方式で取り込む。件数上限（`externalAccounts`は300件、各アカウントの識別子・証明は各20件、`clientConsents`は50件、`clientVisibility`は`clientConsents`にあるClient IDだけ）を入力検査に含め、`json_each`による`INSERT … SELECT`で1回の`batch()`に書き込む。
- [退会](../specification/v0.1/main.ja.md#accountsユーザーの退会)を、標準`deleteUser`による認証削除と、外部キーのCASCADEによるAccounts独自データの削除へ接続する。`session.freshAge: 0`により解除・退会がログインからの経過時間を問わず有効なセッションで実行でき、`deleteUser`が確認メールを使わず退会確認画面での確定後に即時削除することを確認する。
- 連携解除・退会の後もGoogle・GitHub・ORCIDの連携アプリ一覧にAccountsが残り、各サービスの設定から本人が取り消せることを公開ヘルプで案内する。

### OSSライセンス画面

- [Viteの`build.license`](https://vite.dev/config/build-options#build-license)を`{ fileName: "dependency-open-source-licenses.json" }`として設定する。
- ビルド後のライセンスJSONをassetsへ含め、読み込んだ`name`・`version`・`identifier`・`text`をライセンス画面に表示する。
- パッケージ名とバージョンを見出しにし、ライセンス本文はテキストとして表示する。ライセンス画面への導線とJSONの公開URLを用意する。
- フロント・サーバーのビルドに含まれる依存ライセンスの出力範囲を確認し、複数の出力がある場合は配布対象をまとめて掲載する。

## 5. 利用側との接続と移管を検証する

- 移行元の認証フック、プロフィール、CSV照合、受領処理、閉鎖処理、定期処理から呼出関係を追い、外部アカウント管理をAccountsの契約へ接続する。
- ソースコードの移管と利用者の登録手順を分け、各環境の稼働状態と保存データを確認して切り替え手順を決める。
- Points側の接続設定・画面・CSV・経済処理の変更は[Points実装計画](../../../points-web-app/plan/v0.2-implementation.md)で管理する。
- Accounts側は独立したログインと現在の提供許可に基づく一覧・照合を提供し、Pointsの連携解除時の呼出しとPoints内の対応関係を接続テストで確認する。
- 移管済み処理の画面、API、保存処理、Provider設定、テスト、Cron、監視を棚卸しし、Points側の整理対象とAccounts側の提供対象を照合する。

## 6. テスト・CI・完了確認

- 単体テストはValibotスキーマ、URL処理、権限判定、機能フックを中心にVitestで作成する。利用者の操作・認可・保存・情報提供に必要な振る舞いを期待値とし、内部構造や文言などの細部は変更に追従できる形で扱う。
- DB・認証・HTTPの結合テストには`@cloudflare/vitest-pool-workers`を使用し、WorkersランタイムとローカルBindingsで検証する。[Vitest integration](https://developers.cloudflare.com/workers/testing/vitest-integration/)に従い、通常の開発サーバー起動とテストランナー実行を別のscriptにする。
- PreviewでOAuth往復、Basic認証、公開プロフィールの初期HTML、API・OpenAPI、ライセンス画面を確認する。必要な利用フローをE2Eテストにする。
- 検証方法の追加、所有者更新、公開設定、復元をD1上で検証する。最大登録数・一括照合・復元ファイルの境界で処理時間とDB負荷を確認する。照合1,000件と、件数上限いっぱいの5MiBの復元入力を処理できること、値の長さ上限（正規化後のURLは2,048 bytes、固有ID・ユーザー名は256 bytes）と復元の件数上限を超える入力を拒否することを確認する。
- Workers上で、1MiB応答の解析CPU時間を計測し、Workers Paidプランの上限内に収まることを確認する。private IPへ解決される公開ドメインへの取得をテストし、[URL検証仕様](../specification/v0.1/verify-url.ja.md#外部ページの取得)の取得条件を満たすことを確認する。
- CIでは依存関係インストール、Bindings型生成、型検査、lint、単体・結合テスト、ビルド、生成されたOpenAPIとライセンス成果物の確認を実行する。
- Workerテストの自動設定だけに依存せず、配信用Wrangler設定の`nodejs_compat`・`global_fetch_strictly_public`、環境別Bindings、assets、ソースマップを確認する。
- PRのpushでPreviewを更新し、実行した検証コマンドと結果をPRへ記載する。失敗や未検証項目は完了済みと区別する。
- [受け入れ条件](../specification/v0.1/main.ja.md#受け入れ条件)とテスト結果を突き合わせ、独立レビューと修正後の再検証を行う。
- Markdownの相対リンク、仕様・計画・実装・OpenAPIの参照関係を検査し、確定した契約を利用側文書へ接続する。

### READMEに追加する手順

1. リポジトリで`pnpm install`を実行する。
2. Accountsのディレクトリで`pnpm exec wrangler login`を実行する。
3. `pnpm exec wrangler d1 create <DATABASE_NAME>`で環境用DBを作成する。
4. `wrangler.jsonc`の名前・DB ID・Bindings・環境設定を実際の値へ合わせる。
5. `.env.example`からローカル環境を用意し、デプロイ先へ`pnpm exec wrangler secret put <NAME> --env staging`で秘密値を登録する。
6. migrationを適用し、Bindings型を生成する。
7. 通常の画面開発はVite Plusの開発scriptで起動する。Worker単体の確認には`pnpm exec wrangler dev --env staging`を使い、遠隔の実行環境を確認する場合は対象環境を指定して`--remote`で起動する。
8. 型検査・lint・単体・Workers結合テストとビルドを実行し、ローカルpreviewで成果物を確認する。
9. `pnpm exec wrangler preview`でPRのPreviewを更新し、Basic認証とOAuth設定を含めて利用フローを確認する。
