export function AuctionManagementForm() {
  return (
    <section aria-labelledby="management-heading" className="sub-panel">
      <h2 id="management-heading">出品管理</h2>
      <p>編集・取消の可否を取得できた場合だけ操作を表示します。</p>
    </section>
  );
}
